---
id: "EF-039"
seq: 39
summary: "RE-USING A SHIPPED TRANSLATION ID TO CHANGE TEXT IS A NO-OP IN RETAIL — the engine discards the replacement literal at…"
updated: "2026-08-20"
verified: "2026-08-20"
lines: 84
---
- **RE-USING A SHIPPED TRANSLATION ID TO CHANGE TEXT IS A NO-OP IN RETAIL — the
  engine discards the replacement literal at `T()` construction** (source-read
  2026-08-02, chain prompt 9; **live control queued, see the caveat**). This is
  the fact that makes `Fix_TechDescriptionBuilding` a shipped no-op (**BUGS F98**)
  and it binds every player-visible string this pack ever writes.
  * **The mechanism.** `T(id, text)` ends with
    `if not dev and not Platform.ged and TranslationTable[id] then return
    LocIdToLightUserdata(id) end` (`CommonLua\Core\localization.lua:250-252`, and
    the sibling branch `:270-272`). `dev` is `Platform.debug or
    Platform.developer` (`:3`) — **false in retail**. So the call returns a light
    userdata carrying only the id and **your string never leaves the call**.
    Rendering then reads `TranslationTable[id]` (`:662`) and prints the shipped
    text.
  * **English is not the exception.** It is a loaded translation table like any
    other — the install ships `Local\English.fpk` beside nine more, and
    `LoadTranslationTables` (`:970-985`) hard-asserts if the folder is missing in
    a non-developer build. There is no retail configuration that reaches the
    embedded literal by this route.
  * **⭐ The three routes that DO put text on screen, exhaustively:**
    1. ~~re-use a shipped id~~ — dead, per above;
    2. **`Untranslated("…")`** — `T{str, untranslated = true}` carries no id, so
       the table lookup misses and `TDevModeGetEnglishText` returns the literal
       (`:75-92`). Works in retail; the string is whatever language you wrote;
    3. **`shipped_T .. Untranslated("…")`** to **append** to existing localized
       text with **zero loss in any language**. `TMeta.__concat` (`:373-412`)
       works on the stripped light-userdata form because
       `LightUserDataSetMetatable(TMeta)` (`:359`) installs TMeta as the shared
       light-userdata metatable; `IsTCompatible` is `return_true` in retail
       (`:23`); `_InternalTranslate` handles the resulting `TConcatMeta` (`:756`).
       **Shipped precedent: `sign..Untranslated(amount)`,
       `Lua\Buildings\Workplace.lua:293, :299`.** Concat can only ADD — it cannot
       delete a wrong sentence out of a shipped string.
    4. *(and a fourth that is not a code route)* **`ModItemLocTable`** — mods
       genuinely can ship per-language CSVs, merged into the shared
       `TranslationTable` when `loctable.language == GetLanguage()`
       (`Mod.lua:2277-2307`, `ModItem.lua:1069-1093`). ⚠️ It merges into the
       **shared** table, so an id written there is overridden for vanilla and
       every other mod too. **This is the route the owner chose 2026-08-02**, and
       it is post-release work.
  * ⚖️ **WHAT IS MEASURED AND WHAT IS NOT** (own-medicine,
    `recorded-facts-are-claims`): the mechanism above is SOURCE-READ. The
    corroborating circumstantial evidence is strong — `TMeta.__newindex` asserts
    *"in Gold Master they could be a userdata instead of a table"* (`:441`),
    which is the engine saying the same thing. ✅✅ **AND THE LIVE CONTROL WAS
    DISCHARGED THE SAME DAY IT WAS WRITTEN: `ModLog(type(T(8821, "ZZZ")))`
    printed `userdata`** (2026-08-02, log `Mars.exe-20260802-20.28.19`, recorded
    at `docs/archive/PLAYTEST_ARCHIVE.md:4507-4508`) — so the literal IS
    discarded, this entry is right, and `F98` is a shipped no-op as stated.
    ⚠️ **Corrected 2026-08-16:** this bullet still read *"a 30-second live
    control is queued … do not build a text fix on this reading before that
    runs"* fourteen days after the control had run. A hold that is already
    discharged is worse than no note, because it stops work that is allowed.
    ⛔ What remains unobserved is narrower and worth keeping: **no leg has
    watched a shipped-id string render in a non-English language**, which is the
    reading `C51` would need (see `EF-063` for the 30-second way to check
    enrolment without launching the game).
  * ✅✅ **CLOSED 2026-08-20 — WATCHED, IN GERMAN, BY THE OWNER** (link 4's
    attended sitting; logs `docs/archive/link4de_*`, report
    `reports/04_ATTENDED_SITTING.md`). The note above is discharged **positively
    and twice over**, by the two routes this entry distinguishes:
    * **Route 1 read forwards — a REPOINTED id.** `C51` pointed a control at an
      enrolled shipped id and the German pack's text appeared:
      **`TERRAFORMING-GESAMTFORTSCHRITT`**, and the rocket rollover
      **`Zurück zur Erde`** / *"Schicke die Rakete ohne Ressourcen direkt zurück
      zur Erde."* ⭐ **The heading is the decisive one**: in vanilla that control
      is a RAW Lua string with no id and no `Translate` flag
      (`TerraformingOverall.generated.lua:56`), so there is no mechanism by which
      it becomes German on its own. It is German because our code repointed it.
    * **Route 1 as a BORROWED id in a `T{tag, context}`.** `C50`'s bullet — a
      tags-and-punctuation template resolved against a context table, a form that
      appears **nowhere in the shipped game** — rendered
      **`Maximale Anzahl an Drohnen, die ein Drohnen-Hub kontrollieren kann (40)`**,
      byte-for-byte the German that `tools/flpk_extract.py` had predicted from
      the pack, with the number still resolving.
  * ⇒ ⭐ **The mechanism this entry describes is now confirmed in both
    directions**: it destroys a replacement literal (the `F98` no-op, measured
    2026-08-02) **and** it delivers the shipped translation when the id is
    enrolled (measured 2026-08-20). Route 1 is dead *for changing text* and alive
    *for reusing text*, which is exactly what `C50`/`C51` were built on.
  * ⚠️ **Still not observed, and not claimed:** the other seven non-English
    packs. German alone was watched. Nothing here licenses a claim about French,
    Polish, Russian, Schinese, Spanish, Turkish or Brazilian beyond the CSV
    extraction that says the ids are present.

