---
id: "EF-089"
seq: 89
summary: "1.1.0 mod renderer routes: mod code follows InitRenderEngine; hidden hr values require native enumeration; persistent data is not arbitrary startup config; DlcMountFolder is an untested overlay route"
updated: "2026-09-11"
verified: "2026-09-11"
lines: 38
derived_at: "game 1.1.0 build 24995074 (inferred from updated:)"
---
- **SOURCE, named tree:** every Lua citation here is
  `C:\Dev\SMR-SrcArchive\1.1.0.403908\Src\`.
  `CommonLua/Core/autorun.lua:304,314-316,332-341,434` loads storage, binds hr,
  applies startup options and calls **InitRenderEngine before ModsLoadCode**.
  A mod can still precede world loading: `Core/map.lua:482,491,236-247`.
  The native SSR registration/build body is not published in Lua.
- **SOURCE:** `CommonLua/Dlc.lua:406-414` mounts a newer shader cache and sets
  `hr.ForceShaderCacheReload=true`; its comment says next map/save load.
  **MEASURED correction 09-11:** owner legs D/E consume a forced reload during
  boot; ordinary DLC reload suppression is dead on this install (equal revisions,
  no DLC caches). See `reports/FR1_LINUX_FINDINGS_2026-09-10.md` §8-§9 and EF-090.
  `Lua/Config/render.lua:59-67` already starts with SSR off; menu Off only sets
  that main flag (`Core/options.lua:518-524`), leaving hidden tuning untouched.
- **SOURCE:** use `EnumEngineVars("hr.")` to discover real exported names and
  types. `Core/lib.lua:805-850` routes recognized fields through Get/SetEngineVar
  and errors on unknown locked fields. Native literals such as
  SSRDenoiserMode/SSRTraceHiZ/SSRForceHyperbolicDepth are **MEASURED byte leads**
  in the installed executable, not proof of accepted values or variant mapping
  (`reports/FR1_OPTIONS_2026-09-10.md` §2.1, offsets and binary hash).
- **SOURCE:** `Core/GlobalStorageTables.lua:270-328,337-356` loads LocalStorage
  as data and saves engine options. `Core/options.lua:373-403` applies known
  defaults/mappings; arbitrary persisted hr keys have no startup reader.
  Known saved GraphicsApi/adapter fields have early readers (`:34-59`), but
  alternative retail APIs must be enumerated (`:10-15,443-452`). Generic
  native command-line hr parsing remains **UNVERIFIED**; Lua `-cfg`/`-run`
  is non-goldmaster gated (`Core/autorun.lua:125-144`).
- **SOURCE / INFERRED route:** direct mounts and filesystem/OS access are
  blacklisted (`CommonLua/Modding/Mod.lua:1280-1438,1620`); ordinary mod assets
  use a mod namespace (`:866-883,1771`). The separate shipped helper
  `DlcMountFolder` (`CommonLua/Dlc.lua:379-385`) retains its engine environment
  and is absent from the examined blacklists. Calling it on a mod Shaders
  folder is a plausible **UNTESTED** root-overlay route. Mount precedence,
  retail recompilation and packaged-cache override are all unproved (EF-088).
- **SOURCE / INHERITED bench:** Proton runs the Windows build; Platform.linux
  is not a gate. No exclusive Lua-readable Wine detector was found. Explicit
  PC opt-in or a mod-parsed launch marker can gate a bench intervention;
  this is not automatic detection or pre-renderer execution. Full source
  boundaries and unrun recipes: `reports/FR1_OPTIONS_2026-09-10.md` §2-§4.
