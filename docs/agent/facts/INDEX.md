<!-- GENERATED — never hand-edit; regenerate with: python tools/doccheck.py --regen -->
<!-- split_facts.py --write is the one-time MIGRATION from the retired pre-split doc, never a regeneration; verify: python tools/doccheck.py -->

# Engine facts index — 117 facts

One file per top-level bullet of the old `docs/agent/ENGINE_FACTS.md`, in
source order; ids are stable. `updated` is git's last touch of the fact's
own lines. `verified` is the first date the fact's TEXT presents as an
observation (68 of 117 state one) — a mechanical extraction, not an
adjudication: read the fact for what was actually measured, several of
which carry their own ⚖️ "what is measured and what is not" paragraph.
The preamble that opened the old file is `_preamble.md`.

| id | summary | verified | updated | lines | fact |
|----|---------|----------|---------|-------|------|
| EF-001 | Mod code loads BEFORE the classes are built. | 2026-07-28 | 2026-08-01 | 27 | [EF-001.md](EF-001.md) |
| EF-002 | "OFF" IS THREE DIFFERENT THINGS, and only ONE of them touches the save | 2026-08-01 | 2026-09-16 | 59 | [EF-002.md](EF-002.md) |
| EF-003 | `g_Consts` is a **GameVar** (`Lua\Modifiers.lua:427`) and does not | — | 2026-09-08 | 9 | [EF-003.md](EF-003.md) |
| EF-004 | `CurrentModOptions` is PER-MOD-ENV | 2026-07-29 | 2026-07-29 | 8 | [EF-004.md](EF-004.md) |
| EF-005 | Engine Lua tolerates `#nil`/`next(nil)`/`ipairs(false)` (verified from working code | — | 2026-07-29 | 4 | [EF-005.md](EF-005.md) |
| EF-006 | Mods run in a sandbox (LuaModEnv) on ALL platforms | — | 2026-07-29 | 10 | [EF-006.md](EF-006.md) |
| EF-007 | `Msg`/`OnMsg` are PER-ENV OWN KEYS and cannot be stubbed or deleted from mod code | 2026-08-01 | 2026-08-01 | 13 | [EF-007.md](EF-007.md) |
| EF-008 | `error()` and `assert()` do NOT unwind mod code — they report and execution continues | — | 2026-08-19 | 18 | [EF-008.md](EF-008.md) |
| EF-009 | `rawset(_G, k, v)` from mod code writes only into the mod's own env table | — | 2026-07-29 | 5 | [EF-009.md](EF-009.md) |
| EF-010 | CORRECTION of an earlier "fact": `debug.getinfo` is NOT available in mod code | — | 2026-08-01 | 6 | [EF-010.md](EF-010.md) |
| EF-011 | Patch points that work: `PeriodicRepeatInfo[name]` slots (THREAD/SLEEP/FUNC/COND = | — | 2026-07-29 | 5 | [EF-011.md](EF-011.md) |
| EF-012 | A post-wrapper on a **command** method (anything ending | — | 2026-07-29 | 3 | [EF-012.md](EF-012.md) |
| EF-013 | Mod registry: every fix goes through `SMRFixPack.Register(id, {title | — | 2026-07-29 | 4 | [EF-013.md](EF-013.md) |
| EF-014 | All line numbers reference `ModTools\Src`; the game executes | 2026-07-29 | 2026-08-10 | 31 | [EF-014.md](EF-014.md) |
| EF-015 | `print` does NOT reach the log file — it goes ON-SCREEN | 2026-07-29 | 2026-09-10 | 18 | [EF-015.md](EF-015.md) |
| EF-016 | Sample mod format in `<game>\ModTools\Samples\Mods`; docs in `ModTools\Docs\index.md.html`. | — | 2026-07-29 | 1 | [EF-016.md](EF-016.md) |
| EF-017 | Replacing an EXISTING global from mod code works | — | 2026-07-29 | 6 | [EF-017.md](EF-017.md) |
| EF-018 | `OnMsg` is additive, confirmed structurally | — | 2026-07-29 | 3 | [EF-018.md](EF-018.md) |
| EF-019 | GAME-TIME THREADS PERSIST BY DEFAULT — real-time threads do not | — | 2026-09-11 | 31 | [EF-019.md](EF-019.md) |
| EF-020 | A named GLOBAL game-time thread's BODY is stored in a table you can edit, and the engine ships the restart helper | 2026-08-02 | 2026-08-02 | 23 | [EF-020.md](EF-020.md) |
| EF-021 | Every shipped popup is ASYNC — the persistable popup path is dead code | — | 2026-07-30 | 11 | [EF-021.md](EF-021.md) |
| EF-022 | A MOD-AUTHORED CLOSURE STORED ON A PERSISTED GAME OBJECT GOES INTO THE SAVE, SURVIVES UNINSTALL, AND KEEPS RUNNING | 2026-07-31 | 2026-07-31 | 28 | [EF-022.md](EF-022.md) |
| EF-023 | THE REAL RULE (measured 2026-07-31, PT-20 — supersedes the framing above): A SAVE CAPTURES EVERY GAME-TIME THREAD WITH… | 2026-07-31 | 2026-08-13 | 90 | [EF-023.md](EF-023.md) |
| EF-024 | MODS *DO* GET A PRE-SAVE HOOK — `OnMsg.SaveGameStart` and `OnMsg.SaveGameDone` reach mod code | 2026-07-31 | 2026-08-01 | 25 | [EF-024.md](EF-024.md) |
| EF-025 | ENABLING A MOD AT THE MAIN MENU IS A DIFFERENT LOAD ORDER FROM A COLD BOOT, AND IT IS THE ONE EVERY PLAYER GETS FIRST | 2026-07-31 | 2026-07-31 | 33 | [EF-025.md](EF-025.md) |
| EF-026 | `Msg` dispatches static handlers through `procall` | — | 2026-07-31 | 6 | [EF-026.md](EF-026.md) |
| EF-027 | THE BY-VALUE THREAD SERIALISATION IS DOCUMENTED, INTENTIONAL ENGINE DESIGN | 2026-07-31 | 2026-08-01 | 13 | [EF-027.md](EF-027.md) |
| EF-028 | THE SAVE/LOAD HOOK SURFACE — enumerated 2026-07-31 (F86 round 2), so no design discovers hooks one at a time again. | 2026-07-31 | 2026-07-31 | 26 | [EF-028.md](EF-028.md) |
| EF-029 | `CreateGameTimeThread` DEFERS — the body does NOT run before the creating statement continues | 2026-08-01 | 2026-08-01 | 33 | [EF-029.md](EF-029.md) |
| EF-030 | THE PRE-SAVE HOOK COVERS AUTOSAVES — `SaveGameStart`/`SaveGameDone` fire on the autosave path with `autosave=true` | 2026-08-01 | 2026-08-01 | 18 | [EF-030.md](EF-030.md) |
| EF-031 | `IsValidThread` returns NO VALUE for an invalid thread — not `false` | — | 2026-07-31 | 4 | [EF-031.md](EF-031.md) |
| EF-032 | `Wakeup(thread)` only wakes a thread sleeping in `WaitWakeup` | — | 2026-07-31 | 4 | [EF-032.md](EF-032.md) |
| EF-033 | TOOLING: never round-trip a doc through PowerShell 5.1 `Get-Content -Raw` + `WriteAllText`. | — | 2026-07-29 | 7 | [EF-033.md](EF-033.md) |
| EF-034 | Label membership: `AddToCityLabels` is a COMBINED method — every parent class that defines one contributes its labels | — | 2026-08-02 | 23 | [EF-034.md](EF-034.md) |
| EF-035 | Units and rovers are labelled through `City:AddToLabel`, which forwards to the COLONY container first | — | 2026-08-02 | 9 | [EF-035.md](EF-035.md) |
| EF-036 | `Init` and `Done` are COMBINED methods with OPPOSITE order | 2026-08-02 | 2026-08-02 | 11 | [EF-036.md](EF-036.md) |
| EF-037 | Pin release is guarded, and `TogglePin()` without `force` is a no-op for any class whose `CanBeUnpinned()` is false. | — | 2026-08-02 | 11 | [EF-037.md](EF-037.md) |
| EF-038 | The fastest PLAYER-REACHABLE game speed is 5×, not the 20× that `const.ultraGameSpeed` advertises. | — | 2026-08-02 | 15 | [EF-038.md](EF-038.md) |
| EF-039 | RE-USING A SHIPPED TRANSLATION ID TO CHANGE TEXT IS A NO-OP IN RETAIL — the engine discards the replacement literal at… | 2026-08-20 | 2026-08-20 | 84 | [EF-039.md](EF-039.md) |
| EF-040 | `GameRandom:Random` — BOTH FORMS MEASURED 2026-08-02 (PT-61 phase 4.0b), on `SessionRandom` in a live retail colony, 20… | 2026-08-02 | 2026-08-02 | 36 | [EF-040.md](EF-040.md) |
| EF-041 | UI coordinate spaces: `XWindow.box` and `terminal.GetMousePos()` are ONE space (desktop pixels); `scale` multiplies SIZ… | 2026-08-02 | 2026-08-02 | 19 | [EF-041.md](EF-041.md) |
| EF-042 | `UIL.GetSafeArea()` returns FOUR ABSOLUTE NUMBERS (x1, y1, x2, y2), and on PC it equals the whole screen. | 2026-08-02 | 2026-08-02 | 15 | [EF-042.md](EF-042.md) |
| EF-043 | `terminal.desktop.scale` is `GetUIScale`'s output INCLUDING the user's UI Scale option | 2026-08-02 | 2026-08-02 | 8 | [EF-043.md](EF-043.md) |
| EF-044 | A probe tally is BUILD-SPECIFIC — MarsDebug and retail run different probe sets, and at least one fix behaves differently by build | 2026-08-03 | 2026-08-04 | 12 | [EF-044.md](EF-044.md) |
| EF-045 | `RealTime()` does NOT advance across a loading screen — a harness cannot self-time its own load step | 2026-08-04 | 2026-08-04 | 40 | [EF-045.md](EF-045.md) |
| EF-046 | `terminal.GetMousePos()` returns VIRTUAL-DESKTOP coordinates while `terminal.desktop.box` covers only the game's own display — on a multi-monitor setup they are different spaces | 2026-08-04 | 2026-08-04 | 43 | [EF-046.md](EF-046.md) |
| EF-047 | The engine flushes a large log tail only at PROCESS EXIT — a mid-session read of the log file is a read of a PARTIAL file | 2026-08-10 | 2026-08-10 | 22 | [EF-047.md](EF-047.md) |
| EF-048 | Engine predicates return TRUTHY NON-BOOLEANS — `== true` tests read false on values that are semantically true | 2026-08-10 | 2026-08-10 | 18 | [EF-048.md](EF-048.md) |
| EF-049 | `Savegame.ListForTag("savegame")` is NOT a stable save-liveness witness — proven working 2026-08-04, returned a non-table on every call 2026-08-10, same build | 2026-08-10 | 2026-08-10 | 20 | [EF-049.md](EF-049.md) |
| EF-050 | `SaveGame`'s `savename` is written VERBATIM — the engine appends no `.savegame.sav`, so an extensionless name produces a file `LoadGame` cannot find | 2026-08-11 | 2026-08-11 | 24 | [EF-050.md](EF-050.md) |
| EF-051 | Steam Cloud RESTORED deleted savegames at launch, which is why the save-dir close-out gate 'failed' twice. ✅ CLOSED 2026-08-11 (retirement audited) — held 08-12→08-14 while the owner re-ticked cloud ON (the hold window restored all 17 named strays at the first launch, third full sample of the mechanism). ✅ HOLD LIFTED 2026-08-14: re-untick reported, strays deleted (888 MB), 76 .sav re-listed by name, zero strays; falsifier = any of the 17 returning at the next launch. ⚠️ **2026-08-15: the falsifier did NOT fire across two more launches (nothing created in the save dir at either), but the 08-14 listing was INCOMPLETE — the extensionless `U2RT1` was and is present, missed because the sweep globbed `*.sav` and EF-050 lets this project write savegames with no extension.** ⚠️ Baselines must be recorded as NAMES, never a count — and enumerate the DIRECTORY, not the glob | 2026-08-14 | 2026-08-15 | 176 | [EF-051.md](EF-051.md) |
| EF-052 | `SetGameSpeed` accepts factors far above the UI's 20× ultra, but the EFFECTIVE rate is throughput-bound and NON-MONOTONIC — MEASURED 50× at request 50×, then 21×/33×/21× at 100×/200×/400×. ⇒ ask for more and you get less. The hour-skip hazard was NOT reachable on this machine, because the sim's own ceiling sits far below it. ⚠️ Any speed set from the console is wiped by the HUD: `SetGameSpeedState("play")` is hardcoded to `SetGameSpeed(1)` | 2026-08-11 | 2026-08-11 | 66 | [EF-052.md](EF-052.md) |
| EF-053 | `AllMapsForEach` DOES enumerate a MIX-IN parent class — MEASURED on `DroneControl` (a `__parents` entry of `DroneHub`/`RocketBase`/`RCRover`, never a placed template): base-class walk returned **19** objects while the three concrete classes returned **13**, so the mix-in walk is not only live but finds descendants the concrete list misses. ⚠️ SEPARATE MEASURED SURPRISE from the same read: walking `RocketBase` returned **0** on a colony whose `labels.AllRockets` held **6** — a class name that looks obvious is not necessarily the placed class | 2026-08-12 | 2026-08-12 | 42 | [EF-053.md](EF-053.md) |
| EF-054 | INTER-MOD load order is the USER'S ENABLE ORDER, not alphabetical and not ours to set: `AccountStorage.LoadMods` is copied in order into the loading queue and `ModsLoadCode` iterates it. ⚠️ ONE alternate branch, also deterministic: with `AccountStorage.LoadAllMods` (or `config.LoadAllMods`) the list becomes `table.keys(Mods)` SORTED — i.e. alphabetical by mod id. Neither branch is influenceable by mod code, so a two-mod design must be order-independent, never order-assuming | 2026-08-12 | 2026-08-24 | 89 | [EF-054.md](EF-054.md) |
| EF-055 | A mod whose FOLDER is missing while its id is still in `AccountStorage.LoadMods` is skipped with a non-modal log line and is NEVER turned off in account storage — because the one routine that would do that (`WaitErrorLoadingMods`: `TurnModOff` + `SaveAccountStorage` + a blocking `WaitMessage`) hangs off `OnMsg.PreGameMenuOpen`, and Surviving Mars Relaunched overrides `OpenPreGameMainMenu` with a body that never raises that Msg. ⇒ removing a junction is a REVERSIBLE, unattended way to reach D13's state (4) — a real uninstall. ⛔ LIMIT MEASURED 2026-08-14: the 'zero-owner-cost' half did NOT hold on a round trip that spanned an owner Mod-Manager visit — the def loaded, the CODE DID NOT RUN, and it took an owner tick + restart to recover. Verify the registry in-game after any restore. ⭐⭐ SETTLED 2026-08-19 by this fact's own named experiment (pull, restore, relaunch with NOBODY opening the Mod Manager): RED — the code still did not run, twice, so the Mod-Manager visit is NOT the cause and A PULL COSTS THE ENABLE, FULL STOP. Also refuted: the missing folder produced NO log line at all, it vanished silently. ⇒ budget an owner tick after EVERY junction round trip. ⚠️ NARROWED 2026-08-19 (run B, terminal audit): a folder-for-folder swap under the SAME id, with no launch while the folder was absent, KEPT the enable — the loss binds when a launch runs with the id unresolvable; same-id swaps between sessions cost nothing | 2026-08-19 | 2026-08-19 | 145 | [EF-055.md](EF-055.md) |
| EF-056 | ⛔⛔ LOADING A COPY OF A REAL CAMPAIGN STILL RUNS THE CAMPAIGN'S AUTOSAVE, AND THE ROTATION DELETES THE OWNER'S AUTOSAVES — `Autosave()` (Savegame.lua:1484-1528) lists the EXISTING autosaves, writes a new one, then `for i = AutosaveCount, #autosaves do DeleteGame(autosaves[i]) end`, a loop that starts AT the count and so deletes ALL of them when AutosaveCount is 1. MEASURED 2026-08-12: two HELD autosaves went, one recovered only because the leg happened to hold a byte copy. 'Use a designated COPY' does NOT protect the save directory. ⭐⭐ AMENDED 2026-08-13: the enumeration is TAG-DRIVEN (`autosave = true` in the savegame header, which `Copy-Item` carries verbatim), so A BYTE COPY OF AN AUTOSAVE IS AN AUTOSAVE TO THE ROTATION — it deleted a leg's own freshly staged fixture alongside the owner's held save. Never stage a fixture FROM an autosave, and reconcile by name after EVERY launch (firing is timer-driven and unpredictable) | 2026-08-13 | 2026-08-13 | 96 | [EF-056.md](EF-056.md) |
| EF-057 | ⛔⛔ A SAMPLED EXTREMUM IS NOT AN EXTREMUM — a polled series bounds NOTHING between its samples, so 'the value never reached X' may only be claimed from an event or reason that FIRES at X, never from the min/max of the samples. MEASURED on C47 (2026-08-15): three unattended runs polled two farm buffers every 500 real ms, read minima 305–605 of 5000, and recorded 'the buffer never reached 0' as a FAILED prediction — but every one of the same runs' 2/0/4 notification adds carried `reason=Consumption`, and `GetWorkNotPossibleReason` returns that reason ONLY when `CanConsume()` is false, i.e. stored == 0 (`Building.lua:611-613`, `HasConsumption.lua:366-368`) ⇒ the buffer hit a true zero between samples on every add and the verdict was BACKWARDS. The attended sitting then sampled real zeros directly (2 of 121 at 2 s). The reversal was the owner's, not the agent's. Rule: pair every polled extremum with an event/reason witness coded to the boundary, and write 'the SAMPLED minimum was N', never 'the minimum was N' | 2026-08-15 | 2026-08-15 | 31 | [EF-057.md](EF-057.md) |
| EF-058 | ⛔⛔ THE FLATTENED-CLASS TRAP BITES METHOD WRAPPERS TOO — patching a base class's method intercepts NOTHING when DefineClass has flattened the function into subclass tables (`classes.lua:988`); a wrapper must be installed on EVERY class whose lookup resolves to the shipped function, and the wiring must be PROVEN off live instances before any number is trusted. ⚖️ AMENDED 2026-08-19 (link 8): THE TRAP IS KEYED ON INSTALL TIME RELATIVE TO `Msg("Autorun")`, and all four bites were RUNTIME-installed instruments (probe/console), never mod-load-time patches — mod code executes at `autorun.lua:423`'s direct `ModsLoadCode()` call, strictly BEFORE `classes.lua`'s `function OnMsg.Autorun()` flattens, so a patch installed at file scope or inside a `Register` apply is COPIED DOWN into the subclasses and is safe by construction; the fix pack's 50 method patches are all on that side (75/75 `Register` calls at column 0). ⛔ This narrows nothing for an install made after flattening, which is every instrument this project has ever written. MEASURED 2026-08-16 (c48-pairing launch 1): a log wrapper on `TaskRequestHub.FindTask` alone saw **0 of 25,184** calls across a 10-game-hour window because `DroneHub.FindTask` etc. are flattened copies; the repair patched **48** carrier classes (scan g_Classes for `cls.FindTask == shipped_fn`, remember own-key-vs-inherited per class for an exact restore) and printed a wiring proof — 19/19 live hubs resolving to the wrapper — before the window opened. Fourth bite of the classes.lua:988 family (wave-11 probe rawget, sampler class read, EF-057's note); the value-field converse: writing a CLASS field (the c48-brake `supply_dist_modifier`) works when written to the exact class instances belong to | 2026-08-19 | 2026-08-19 | 49 | [EF-058.md](EF-058.md) |
| EF-059 | ⭐⭐⭐ THE DRONE MATCHMAKER TREATS `rfStorageDepot`-FLAGGED SUPPLIES AS A STRICT LAST RESORT — any non-depot source (vegetation offer, loose pile, producer output) wins the pairing REGARDLESS of distance; distance only tie-breaks within a class. MEASURED 2026-08-16 at the FindTask seam, 985 witnessed pairings (`archive/c48pair2_*`): Seeds chose non-depot 479/479 with a NEARER fully-stocked ASSIGNABLE depot losing in 399 (specimen: 41,915 flown for one bush's 280 past a depot offering 180,000 at 5,733); Food obeyed the same law wherever loose food existed (81% nearer-depot losses) and used depots for the remainder (46%) only because loose food is finite. Alternatives eliminated by experiment: distance (the 150 brake, applied 100%, changed nothing), demand-side gating (a new diner bulk-filled to exactly 100% incl. a sub-load top-up ⇒ consumption's `rfWaitToFill` is a one-resource-unit rounding guard, not a carry gate), queue-bucket order (veg sits p1, depots mostly p0 with some p2/p3 — the HIGHER-bucket depots also lost). Both depot requests are branded (`StorageDepot.lua:67-68,466-467`). Corollaries, all measured: `desired_amount` withholds NOTHING from the supply side (every depot offered stored-in-full; it steers depot-to-depot rebalancing — the owner's reading, confirmed); depots RESTOCK THEMSELVES from the landscape through the same rule (125/479 pairings delivered INTO depots — a colony banks bush seeds it will never serve from storage); grown crop hexes offer FOOD via the same VegetationTaskRequesters; mega-dome food venues drain at farm rates (a Diner ~48,000/sol) and thrive on bulk. ⇒ on terraformed maps 'depots last' degenerates into 'depots never' for Seeds — the C47/C48 crumb-feeding, characterized | 2026-08-16 | 2026-08-16 | 30 | [EF-059.md](EF-059.md) |
| EF-060 | ⭐⭐ THE SANCTIONED PAIRING-POLICY LEVERS: every drone assignment crosses ONE Lua seam — `TaskRequestHub:FindTask` (`_TaskRequest.lua:72-83`, sole definition tree-wide; PROVEN to carry 25,184 calls / 985 pairings in a 10-game-hour window) — and the hub's sibling finders `FindSupplyRequest` / `FindDemandRequest` (`:54-69`) pass `ignore_flags` AND `required_flags` STRAIGHT INTO the C matcher, so a wrapper can substitute a pairing constrained BY CLASS (e.g. `required_flags = const.rfStorageDepot` ⇒ depot-only supply) while distance/reachability/restrictors/claims stay engine arithmetic. Returns of FindTask: `(request, pair_request, resource, amount, priority)` — supply first for pickups, work requests carry `rfWork`. Request userdata API used from shipped Lua everywhere: `GetSource(unit)`, `GetActualAmount`, `GetTargetAmount`, `CanAssignUnit`, `GetFlags`/`IsAnyFlagSet`, `AddAmount`, `SetReciprocalRequest`. Known flags: `rfSupply`/`rfDemand`/`rfWork`/`rfCanExecuteAlone`/`rfPostInQueue`/`rfWaitToFill`/`rfStorageDepot` (both depot requests carry the last, `StorageDepot.lua:67-68,466-467`). ⛔ Binding cautions: wrap ALL carrier classes + prove wiring (`EF-058`); FindTask returns a drone's SINGLE best task, so vetoing without substituting idles the drone that cycle; per-call work must be tiny (the seam fires ~constantly); substituted pairings must be vanilla-legal requests so saves/uninstall stay clean. Precedent: the opt-in D06 core claim gate already wraps this seam | 2026-08-16 | 2026-08-16 | 21 | [EF-060.md](EF-060.md) |
| EF-061 | BREATHABLE ATMOSPHERE IS A TWO-PARAMETER QUORUM, AND THE DOME-OPENING EFFECTS SPLIT ACROSS TWO SWITCHES. `UpdateTerraformingThreshold` (`Lua/Terraforming.lua:41-61`) counts every `TerraformingParam` that declares the same `ThresholdItem` id and fires `TerraformThresholdPassed(id, true)` only at zero still-short — so `AtmosphereBreathable` needs **Atmosphere ≥ 95% AND Temperature ≥ 50%** (`Data/TerraformingParam.lua:86` + `:143`, both `Hysteresis` 2); Water/Vegetation do not gate it. Breathability ALONE gives `disable_air_consumption` on label `Building`, −50% `electricity_consumption` on label `Dome`, clears Suffocating/Freezing, and shortens the colonist walk gate (`Terraforming.lua:304-333`, `Colonist.lua:1560`). The LAW (`SetOpenAirBuildings(true)`, `OpenAirBuilding.lua:107-165`) adds the `_Open` entity swap, +10% `InsideBuildings` maintenance and the −50% construction discount (consts `__const.lua:1836-1849`). Only two callers: the `Policy_OpenDomes` OnStart and the `NoPolitics` popup's button. ⚠️ Backslide auto-deactivates the law despite `can_be_repealed = false` (`Terraforming.lua:348-355`) | 2026-08-16 | 2026-08-16 | 58 | [EF-061.md](EF-061.md) |
| EF-062 | ⭐ A POLICY IS REACHABLE ONLY THROUGH ITS CATEGORY'S LEFT-TO-RIGHT PREPARE-CHAIN, AND A LAW BEING "UNLOCKED" SAYS NOTHING ABOUT THAT. `Legislature:RecalcPoliciesState` (`Lua/Factions/Legislature.lua:1085-1145`) walks each category in `SortKey` order and marks a slot `visible` only if it is the first non-hidden one or `are_prev_prepared` (`:1075-1084`) — where a `visible` but UNPREPARED slot returns false, blocking the rest of the row exactly as hard as `locked`. Cap: 3 per category without a Martian Assembly, effectively unlimited with one (and `Governance` renders only with one, `:1087-1098`); the `UnlockedPolicies` game rule unlocks all. `hidden` = no law passes its `Prerequisite` (`:1056-1064`) and does NOT block. A prepare force-ends the session (`:783-787`), sessions 1 Sol apart (`LegislatureBetweenSessions` 1440000ms); `EfficientAssembly` gives one free instant prepare (`:767-781`), a Law Office auto-prepares a RANDOM visible slot every ~3 sols (`DLC/thomas/Code/LawOffice.lua:41-63`). ⛔ Faction Open-Floor suggestions require `state == "visible"` (`:297-340`) and a locked hex NEVER names what it hides (`Data/XDef/LawEntry.lua:155`, `:212`) | 2026-08-16 | 2026-08-16 | 73 | [EF-062.md](EF-062.md) |
| EF-063 | ⭐ A STRING IS TRANSLATED ONLY IF ITS ID IS ENROLLED IN THE SHIPPED LANGUAGE PACK, AND YOU CAN CHECK THAT IN 30 SECONDS. The install ships NINE packs under `Local\<Language>.fpk`, each holding one file, `CurrentLanguage/Game.csv`; `IsLocalizationLanguageAvailable` builds the Options language list straight off that folder (`CommonLua/Core/localization.lua:1209-1227`). Our own `tools/flpk_extract.py` opens them. `ModTools\Game.csv` is the ENGLISH export (28,321 rows, empty Translation column) and is NOT what the game loads. An id present in a pack renders translated; an id absent falls through to its embedded English literal — and XDef-generated ids CAN be absent while their file-siblings are enrolled, so absence is a real finding and not an artefact. This is the surface that makes `EF-039` actionable in both directions: pointing a UI at an ENROLLED id is a loss-free retail text route, replacing an enrolled id's text with a new literal regresses eight languages | 2026-08-16 | 2026-08-16 | 53 | [EF-063.md](EF-063.md) |
| EF-064 | ⛔ `ProtectedPropertyObject` DOES NOT PROTECT ANYTHING IN RETAIL — `__newindex` asserts on an undeclared key and then `rawset`s it on the very next line (`CommonLua/PropertyObject.lua:1819-1823`), and by `EF-008` an assert does not unwind. So a write to a key the class never declared SUCCEEDS and execution continues to the next statement. The same shape applies to `__index` (`:1813-1817`), which asserts and then returns the class value anyway. ⇒ never explain a downstream failure as 'the protected-object assert halted it', and never read a missing class-field declaration as a behavioural cause; it is a diagnostic aid for developer builds only. This refuted another mod's stated mechanism for the Mod Manager screenshot bug (`C52`) while every line it cited was correct | — | 2026-08-16 | 41 | [EF-064.md](EF-064.md) |
| EF-065 | ⛔⛔ THE ENGINE ITSELF SHOWS THE PLAYER A MESSAGE BOX NAMING OUR MOD — but only on ONE of its two routes, and neither has a call site in our code. (a) any UNCAUGHT Lua error whose message OR call stack contains our `content_path` reaches `ReportModLuaError` (`Mod.lua:2958-2993`, live in retail because the block is gated `if not Platform.asserts`), which `ModPrint`s `Error in mod <title> (id …)` and pops `CreateMessageBox("Warning", "Mod-related problem detected in the game logic. Try disabling the mods…") + "Mod Flagged: <title>"` — once per mod id per process; (b) any throw at a code file's FILE SCOPE is caught by `pdofile`'s pcall (`lib.lua:242-251`), collected by `ModDef:LoadCode` (`:490-520`) — ⛔ CORRECTED 2026-08-19 (terminal audit): the (b) BOX IS DEAD CODE ON THIS TITLE — display is gated on `ModsPreGameMenuOpen` (init false, set nowhere) or `Msg("PreGameMenuOpen")`, which SM's `Lua/init.lua:1` override never raises (same mechanism as EF-055) ⇒ a file-scope throw is LOG-ONLY and the player sees NOTHING. `content_path` is configuration-INVARIANT (`Mod/SMR_CommunityFixPack/` packed and unpacked, `Mod.lua:1755-1758`), so (a) behaves identically in run B. MEASURED NEGATIVE 2026-08-19 is SPENT: surface (a) FIRED IN THE FIELD TWICE ON 2026-08-23/24 (F104 Passage Network, F105 vanilla landscape site) — both pass-through frames, NEITHER our defect; the box wording is now observed and OnLuaError IS confirmed raised for a thread error. The causing mod can be unnameable: PN had already returned one line before the throw. Response options: checklist 73 | 2026-08-19 | 2026-08-24 | 98 | [EF-065.md](EF-065.md) |
| EF-066 | ⭐⭐ A BUILT CLASS'S `Init` IS NEVER THE FUNCTION ANYONE ASSIGNED — `Init`/`Done` are COMBINED methods: `OnMsg.ClassesPreprocess` collects every classdef's own `Init` along each hierarchy and writes back a NEW composed closure per class (`classes.lua:1602-1676`), so a classdef-time wrapper on a PARENT is captured into every descendant's composition (why C51's one wrap reached the Zeus rocket, measured on a German screen) AND an identity check on a built class's `Init` reads "neither ours nor the original" on EVERY boot with ZERO other mods — the wave-12 probe's "a later mod has chained on top of ours" names a cause that cannot be the cause, and a wrapper genuinely lost pre-build would produce the SAME reassuring reading. Identity checks stay valid on plain methods and `_G` functions (copied by reference, never recomposed; wave-13 compared clean). ⭐ SWEPT AT LAST, MEASURED 2026-08-24 (`archive/f106_*.log`, probe `DispatchReach`): the pack declares **105** (class, method) targets — not ~60 — and 97 of them reach every descendant; the 66 (+1,328 multi-parent) unreached classes are shipped SUBCLASSES THAT RE-DECLARE the method, this watch's original question, failure mode under-coverage and never new harm. ⛔ Instantiation is NOT measured, so that is a reach gap and never a broken-fix count. ⛔ The 2026-08-24 "post-ClassesBuilt copy misses everything" sharpening is WITHDRAWN — `Register` applies inline at file-load time, so plain-method wraps patch CLASSDEFS and the builder copies ours down (F106 closed as refuted, F33 clean). ⚠️ The mirror-image defect that timing DOES cause is real and filed: capturing `prev` off a class that does not declare the method yields nil (`F107`) | 2026-08-24 | 2026-08-24 | 88 | [EF-066.md](EF-066.md) |
| EF-067 | ⛔⛔ PARADOX MODS HAS NO COMMENT CHANNEL AT ALL, and its in-game 'Report' is a MODERATION FLAG — telling a player to use it for a bug report would flag our own mod for Paradox QA… | 2026-08-20 | 2026-08-20 | 50 | [EF-067.md](EF-067.md) |
| EF-068 | ⛔⛔ ON AN UPDATE UPLOAD THE VERSION ARITHMETIC CANNOT TELL YOU WHICH PORTALS RAN — Steam's forced pre-pack save is inside the CREATE-ITEM branch, so one sitting bumps once even when both portals upload… | 2026-08-29 | 2026-08-29 | 53 | [EF-068.md](EF-068.md) |
| EF-069 | ⛔ A CLASS-LEVEL `GetPriorityForRequest` OVERRIDE ENTERS SAVES BY VALUE — `RequiresMaintenance.lua:94` writes it per obj… | — | 2026-09-01 | 34 | [EF-069.md](EF-069.md) |
| EF-070 | ⚠️ AUTOSAVE AND QUICKSAVE RUN WITH GAME TIME UNPAUSED, and at least four yields sit between `Msg("SaveGameStart")` and the persist walk — a pre-save handler cannot hold an invariant against game-time threads; two save paths skip the messages entirely | — | 2026-09-01 | 22 | [EF-070.md](EF-070.md) |
| EF-071 | ⚠️ THE C MATCHER READS THE PRIORITY BOUND FROM THE `const.TaskRequest` GROUP — not from the keys of the queue tables it is handed. Measured 2026-07-31 by the widening incident (only the group changed; the flat const stayed 3; C nil-indexed narrow tables); falsifier E-4 | 2026-07-31 | 2026-09-01 | 22 | [EF-071.md](EF-071.md) |
| EF-072 | ⭐ `TaskRequestHub:FindTask` HANDS C NINE VALUES AND READS NOTHING ELSE OF `self` but the two lap fields — a complete stand-in hub is five queue fields plus lap scratch; `Request_FindTask` is a reachable GLOBAL; and the persist walk never descends into class tables, `_G` or a mod env, so tables held only as upvalues of a class-table function cannot reach a save | — | 2026-09-01 | 25 | [EF-072.md](EF-072.md) |
| EF-073 | ⭐ A HUB-FILED MAINTENANCE REQUEST'S BAND LIVES IN THE QUEUE KEY, NOT IN THE REQUEST — `DroneControl:AddBuilding` (the override every hub/rocket/rover uses) never writes the userdata's priority; only the base `_InternalAddRequest` does, and maintenance requests are created before any centre is connected. Corollaries: shuttles key by resource; `Request_UpdateDeficits` is a fourth C consumer of the hub tables | — | 2026-09-01 | 25 | [EF-073.md](EF-073.md) |
| EF-074 | ⚠️ A DELIVERY IN FLIGHT IS RE-OPENED BY `Drone:ImproveDemandRequest` toward ANY strictly-higher-band demand of the same resource — `min_priority = GetPriorityForRequest(d_request) + 1`, and `PickUp` chains `Deliver` WITHOUT the `do_not_improve_req` flag — so an urgency scheme that elevates demands OUTSIDE the real queues must guard this seam or its deliveries can be traded up to routine player-High traffic. ⚖️ AMENDED 2026-09-01: the guard must decline ONLY while `must_change` is false — an unconditional decline loops the drone forever on an unreachable destination — and the `do_not_improve_req` alternative is unreachable without replacing `Drone:PickUp` | — | 2026-09-01 | 50 | [EF-074.md](EF-074.md) |
| EF-075 | ⚠️ GAME 1.1.0 ("Services & Science", Steam build 24995074) + the first paid DLC *Feeding the Future* shipped 2026-09-08 and the rig AUTO-UPDATED the same morning — `ModTools\Src` was overwritten (2747 of 4715 `.lua` files carry the new mtime), so the 1.0.7.396349 line-number base every `bugs/` and `facts/` citation was written against NO LONGER EXISTS ON DISK. Steam still offers 1.0.7 as a selectable branch (Properties → Game Versions & Betas), which is a dev claim about a store surface and not yet route-checked. ⛔ Entries are the record of a defect in a STATED version — never renumber their citations in place | — | 2026-09-08 | 30 | [EF-075.md](EF-075.md) |
| EF-076 | ⭐ THE 1.1.0 TARGET SWEEP: 106 of the pack's 107 declared `{class, method}` targets still resolve on their declaring class — only `Colonist.UpdateSatisfaction` (F09) is gone. The damage is in the specs the wrap harvester EXCLUDES: 5 globals (`GatherTransportableResources`, `GetCommandCenterLifeSupportGrids`, `GetGridGlobalStorage`, `PlanetaryAsteroidVisitPossible`, `RainsDisasterLoop`) and 3 consts (`MinDaysMaintenanceSupplyBeforeNotification`, `MinDaysFoodSupplyBeforeNotification`, `UnreachablesCleanupDeltaT`) no longer exist. All of them sit INSIDE `Require` gates ⇒ 6 modules self-disable cleanly and 1 (F75) latches benign; no crash path was found. ⛔ Source-read PREDICTION — only `SMRFixPack.ListFixes()` on 1.1.0 measures it ⛔ SUPERSEDED 2026-09-08 BY MEASUREMENT (`EF-078`): the real number is 13 inactive / 11 in the dialog, not 6 — this fact's prediction was WRONG BY 5 and the two method failures are recorded in EF-078 | — | 2026-09-08 | 57 | [EF-076.md](EF-076.md) |
| EF-077 | ⚠️ THE PACK CLEARS 1.1.0's MOD-COMPATIBILITY FLOOR BY EXACTLY ZERO: 1.1.0 sets `ModMinLuaRevision = 350453` (`CommonLua/Modding/Mod.lua:15`) and `metadata.lua` declares `lua_revision = 350453`. `ModDef:IsObsolete()` is a strict `<` (`:915`), so we are NOT obsolete and `ModsUIIsModCompatible` passes — no player sees an incompatibility prompt and saves do not mark the pack obsolete. ⛔ One integer of margin: any future patch that raises the floor flips the browser warning, `IsObsolete()` and the savegame mark ALL AT ONCE. Re-read this const on EVERY game update | — | 2026-09-08 | 26 | [EF-077.md](EF-077.md) |
| EF-078 | ⭐ MEASURED 1.1.0 BASELINE (first launch, 2026-09-08, log `archive/logs/first110_*.log`): game **1.1.0.403908**, `LuaRevision` **403908**, both DLCs load, the pack loads UNFLAGGED — and **13 of 80 modules go inactive**, of which the in-game dialog names **11** as "switched themselves off for safety". ⛔ THE SOURCE-READ PREDICTION (`EF-076`) SAID 6 AND WAS WRONG BY 5: it missed `MeteorFrequency`, `MeteorStormWedge`, `VacuumWalks` (path specs checked by LAST-SEGMENT NAME only — `Meteors` is a ubiquitous word, so the grep passed spuriously) and `DustSicknessDamage`, `IndependenceTerraforming`, `SaintBlessing` (preset/DATA content checks, invisible to any symbol sweep). ✅ It DID predict `LastTransmissionStorage`'s benign latch exactly. No `[LUA ERROR]`, no assert, no blame line anywhere in the log | 2026-09-08 | 2026-09-08 | 67 | [EF-078.md](EF-078.md) |
| EF-079 | ⛔ 1.1.0 CANNOT LOAD 1.0.7 SAVES AT ALL — the save list stamps them "Deprecated save file format!" and loading raises a Warning that names the ONLY route: Steam → Properties → Game Versions & Betas → 1.0.7. So the project's ENTIRE provisioned playtest save library (USA Sol 302, the F95/F59/F90 fixtures, the T1/T2 uninstall pair) is unreachable on 1.1.0 and is now BRANCH-LOCKED to 1.0.7. Any 1.1.0 leg must start a NEW colony and be provisioned from scratch — hours, not a 20-30 min warm-up. ✅ Side effect: the GAME ITSELF documents the 1.0.7 branch, which route-checks decision 98's dev claim far better than the announcement text did | 2026-09-08 | 2026-09-08 | 41 | [EF-079.md](EF-079.md) |
| EF-080 | ⭐ THE 1.1.0 OLD-SAVE BLOCK IS A CONFIG PAIR, NOT A HARD REFUSAL, AND THE DEVS SHIP AN UNBLOCKED MODE: `config.SupportedSavegameLuaRevision = 402200` gates it and `config.OldSavegameBehavior = Platform.steam and "block" or "warn"` decides whether you get a refusal or a **"Load anyway"** prompt (`SavegameMetadata.lua:154-170`) — we are blocked ONLY because we are on Steam. Either value is a one-line runtime override a mod can do with no game file touched. ⛔ But a force-loaded save is an UNATTRIBUTABLE substrate: 402200 sits deliberately AFTER the research + services rewrites, so any measurement on such a colony cannot separate our mod from a half-migrated save. Triage only, NEVER a verdict, NEVER shipped to players | — | 2026-09-08 | 52 | [EF-080.md](EF-080.md) |
| EF-081 | ⭐ MEASURED 2026-09-08 (owner rig, forced boots, log `archive/logs/forced110_*`): with the self-check override armed the pack reads **75 applied / 5 inactive**, and forcing `LowStorageWarning` produces a REAL runtime throw — `GetCommandCenterLifeSupportGrids` nil at `Fix_LowStorageWarning.lua:125`, called from `City:HourlyUpdate` (`City.lua:150`) ⇒ **hourly, on any colony, for any player**, with the rest of that tick's city update aborting with it. ⇒ **that module's self-disable is LOAD-BEARING** — the first evidence for the fail-safe design that is measured rather than argued. ⛔ The other 9 forced modules did NOT throw, but their triggers were never reached in ~57 s on a blank map — untested is NOT clean. ✅ Both refusals verified: the `test` content check (`AutomationLawCompensation`) and the 4 `DataPatch` latches stay inactive under a force | 2026-09-08 | 2026-09-08 | 78 | [EF-081.md](EF-081.md) |
| EF-082 | ⭐ `GameVar` and `MapVar` differ in `_G` in a way that is USABLE as a patch-rot discriminator: `GameVar` rawsets the global to `false` when it registers (`CommonLua/Core/lib.lua:1069-1071`), `MapVar` NEVER touches `_G` at all (`:984-1000`) — its value lives on the map and is read `map.<name>`. So `rawget(_G, "X") ~= nil` at mod-load time separates "X is a GameVar" from "X is a MapVar", and the ordering that makes it readable is STATIC: `autorun.lua:432-434` runs `dofolder("Lua")` -> `DlcsLoadCode()` -> `ModsLoadCode()`, so every top-level `GameVar`/`MapVar` call in the game tree has already run before the first line of mod code. ⚠️ This SHARPENS `EF-003`, which says a GameVar "does not exist while mods load" — the KEY exists, holding `false`; only the VALUE is unusable | 2026-09-08 | 2026-09-08 | 54 | [EF-082.md](EF-082.md) |
| EF-083 | ⭐ LANDSCAPING ON 1.1.0 IS ROVER-ONLY AND RESEARCH-GATED, BOTH NEW SINCE 1.0.7 (verified against the archived 1.0.7 tree, 2026-09-09): (1) a landscaping site posts NO work requests to command centers — `LandscapeConstructionSiteBase:ShouldAddRequestToCommandCenter` returns false, dev comment "RCTerraformer only can landscaping, no drones" (`Lua/Landscape/LandscapeConstructionSiteBase.lua:40-42`); the site warns `NoRCDozer` unless `LandscapingNanites` is researched (`:159-160`); 0 hits for `RCDozer`/`NoRCDozer` in the whole 1.0.7 tree. (2) Terrace, Ramp, Clear Waste Rock and the five surface textures are `Effect_TechUnlockBuilding` on the **Dozer Rover** tech (`TerraformingRover`, group Land, `Data/Tech.lua:4907-4931`); on 1.0.7 that tech unlocked only the rover and NO tech referenced any landscape template — they were available from sol 1. Lakes are behind Lake Crafting on BOTH branches. The Nanites breakthrough is the bypass: its effect calls `UnlockBuilding` on the same eight (`:1231-1242`, the build-menu override). ⚠️ The RC Dozer is an `RCConstructorBase` (`Lua/Units/RCTerraformer.lua:4`), so the scatter's constructor exemption covers it | 2026-09-09 | 2026-09-09 | 43 | [EF-083.md](EF-083.md) |
| EF-084 | ⭐ 1.1.0 FIXED THE DUST-DEVIL MARKER GATE, AND 1.0.7 IS THE CONTROL THAT PROVES THE OLD FINDING WAS RIGHT: on 1.0.7 the marker spawn path tested `HasDustStorm` ONLY (`Lua/DustDevils.lua:169`) while the natural scheduler tested `HasDustStorm(map) or DustStormsDisabled` twice (`:209`, `:220`) — a real sibling inconsistency. On 1.1.0 the marker path is `WaitSpawnDustDevil` and reads `if HasDustStorm(map) or DustStormsDisabled then return end` (`:170`); the scheduler still checks both (`:248`, `:267`). All three gates now agree ⇒ the `smrcf-text` chain's dust-devil marker-gate rider is REFUTED for 1.1.0 and nothing is owed. ⛔ Our own `Fix_DustDevilSpawnGate` was deleted as R-21 of the 1.1.0 REMOVE pass for a DIFFERENT reason and does not cover this | 2026-09-09 | 2026-09-09 | 53 | [EF-084.md](EF-084.md) |
| EF-085 | ⭐ FPK PARITY RE-PROVEN FOR 1.1.0.403908, AND IT IS PERFECT (MEASURED 2026-09-10, vanillahunt link 01 unit A): every one of the **2373** `Lua/` + `CommonLua/` files and every one of the **2191** `Data/` files in the archived 1.1.0 `ModTools\Src` tree ships **byte-identical** inside the live install's `Packs\Lua.fpk` / `Packs\Data.fpk` — **0 divergent, 0 absent**, against 1.0.7's 2250/2256-with-5-divergences. ⭐ `Lua.fpk` carries the Src layout VERBATIM (2374 exact-path entries) PLUS a 2106-entry FLATTENED ALIAS SET (`Train.lua` beside `Lua/Units/Train.lua`); all 2106 aliases are byte-identical to their exact-path twin, so there is no stale-alias hazard. The one fpk entry with no Src counterpart is `Lua/Config/_LuaRevision.lua` (100 B), which reads `LuaRevision = 403908` / `BuildVersion = '1.1.0.403908'` — an independent control that the extracted pack IS the archived build. `Data.fpk`'s 624 extra entries are assets (482 `ParticleSystemPreset/`, 142 `MapData/`), no Src counterpart by design. DLC ships in its own `DLC\norman.fpk` / `DLC\thomas.fpk`, outside both packs. ⛔ This proves the BYTES MATCH; it does NOT prove the game executes them (`EF-078` stands: trust runtime over source) and it says nothing about `Mars.exe` | 2026-09-10 | 2026-09-10 | 40 | [EF-085.md](EF-085.md) |
| EF-086 | ⭐ ANIMATION MOMENTS ARE LUA DATA, KEYED BY STATE NAME — and `GetAnim(channel)` returns a state INDEX. `CObject:GetAnimMomentsCount`/`GetAnimMoment`/`IterateMoments` read ONLY `Presets.AnimMetadata[<entity>][<anim name>].Moments` (`CommonLua/Classes/AnimMoment.lua:5-16`, `:35-37`, `:309-331`); there is no fallback to the `.hgacl`, and only 5 entity groups ship presets. Pass `GetStateName(obj:GetAnim(1))`, never the raw index — `preset_group[20]` is nil. MEASURED live 2026-09-10 (C74) | 2026-09-10 | 2026-09-10 | 25 | [EF-086.md](EF-086.md) |
| EF-087 | At OnMsg.LoadGame, IsValidThread can still accept a persisted cosmetic tracker that is dead by the first post-load read; a safe replace-own-tracker path must replace it, not treat the handle as proof of liveness (C74 attended, 2026-09-10) | 2026-09-10 | 2026-09-10 | 19 | [EF-087.md](EF-087.md) |
| EF-088 | 1.1.0 shader 38121decbc3eee12 is byte-proven Reflections.fx REFLECT_RAYS and exists verbatim in the cache; owner logs tie two RAYS programs to the faulting threads, intervention remains owed | 2026-09-11 | 2026-09-11 | 32 | [EF-088.md](EF-088.md) |
| EF-089 | 1.1.0 mod renderer routes: mod code follows InitRenderEngine; hidden hr values require native enumeration; persistent data is not arbitrary startup config; DlcMountFolder is an untested overlay route | 2026-09-11 | 2026-09-11 | 38 | [EF-089.md](EF-089.md) |
| EF-090 | 1.1.0 cache overlay consumed in owner bench; index names 18 RAYS records; v2 covers all, with normal-loading treatment untested | 2026-09-11 | 2026-09-11 | 56 | [EF-090.md](EF-090.md) |
| EF-091 | A landed non-player rocket launches the instant its cargo reads ready, and a drone request's GetTargetAmount goes negative after a re-size while drones hold claims (F119 sitting) | 2026-09-11 | 2026-09-11 | 18 | [EF-091.md](EF-091.md) |
| EF-092 | Residence selection refuses a DESTROYED building through an INHERITED test — GetResidenceComfort gates on ValidateBuilding — so a constant-comfort desk stub deletes that guard and manufactures a harm (cost a false F59 finding that cleared an audit) | 2026-09-11 | 2026-09-11 | 29 | [EF-092.md](EF-092.md) |
| EF-093 | ⭐ VANILLA RE-SEEDS LOCKABLE-PRESET STATE ON EVERY LOAD — `OnMsg.PostLoadGame` runs `PreProcessLockablePresets()`, which calls `ResetLockablePresetState` for any preset missing from the owner's persisted `ProcessedLockablePresets` and re-applies that preset's DECLARED `LockState`. ⇒ a mod that clears a lock reason AND drops the preset from that set leaves a save vanilla itself repairs on the next load: self-healing residue with no mod code left behind, no cleanup artifact and no player action. The engine's own comment states the intent — 'process all lockable presets, which were newly added since the save was made' | — | 2026-09-13 | 35 | [EF-093.md](EF-093.md) |
| EF-094 | ⭐ ACHIEVEMENT STATE — how to read, clear and TEST it. The flag is `AccountStorage.achievements.unlocked[id]`, persisted to `account.dat` (BPUL: 519 B of plain metadata, the rest AES+HMAC over a compressed Lua table). ⛔ NO mod and NO retail console can clear it — `AccountStorage` and friends are in `ModEnvBlacklist`, and on a `config.Mods` build the console env IS a mod env. Sync is ONE-WAY local→Steam and runs every launch, so a Steam-side reset is silently undone. ⭐ The working test route is to MOVE `account.dat` ASIDE (the game regenerates an empty achievement table), test, move it back | — | 2026-09-13 | 61 | [EF-094.md](EF-094.md) |
| EF-095 | ⭐ CHEAT TAINT — what actually condemns a save. `CheatsUsed` is a GameVar (saved INTO the savegame) appended only by `LogCheatUsed`, which is called from exactly three `NetSyncEvents` wrappers (`Cheat`, `ObjCheat`, `CheatDef`); the work bodies (`obj:CheatFill()`, `Building:CheatCleanAndFix()`, a `CheatDef.run`) log nothing. `CheatDef:Exec` sends sync presets (default `sync = true`, 83/105) through the wrapper and runs `sync = false` ones (22/105) directly. Every per-object infopanel cheat taints (ObjCheat). Showing the panel (`config.BuildingInfopanelCheats`) is NOT an achievement reason; `AreModdingToolsActive()` IS, but only while a GED app is open (evaluated live, stores nothing). ⇒ calling the leaf body directly is untainted | — | 2026-09-13 | 42 | [EF-095.md](EF-095.md) |
| EF-096 | ⭐ MOD SANDBOX REACH for a tooling mod. `ModEnvBlacklist` is a READ barrier on `_G` lookups (`ModEnvMeta.__index` returns nil for listed keys) plus a write barrier; `ModMsgBlacklist` has nine messages. BLOCKED: `load`/`loadstring`/`dofile`/`require`/`io`, every `Async*` file op, `AccountStorage`, `CanUnlockAchievement`, `ConsoleExec`, `Msg`/`OnMsg` raw (mods get `safe_Msg`), the `Pops`/`PDX`/`Steam` prefixes. OPEN (checked by name): `SaveGame`/`LoadGame`/`DeleteGame`, `SetGameSpeed`, `CheatDefs` + every `Cheat*` leaf, `CheatsUsed`/`AreCheatsUsed`/`LogCheatUsed`, `config`, `ConsoleEnabled`/`ShowConsole`/`cls`, `CopyToClipboard`, `WriteScreenshot`, `LocalStorage`/`SaveLocalStorage`, `MapForEach`/`MapGet`, `PlaceConstructionSite`, `PlaceCableLine`/`PlacePipeLine`/`PlacePassageLine`, `IsBuildableZoneQR`, `XTextEditor`, `SelectedObj`. HOOKABLE messages: `OnLuaError`, `ConsoleLine`, `GatherGameMetadata`. ⇒ a tool cannot compile code from a string at runtime, but can drive the game, save/load, clipboard and screenshot | — | 2026-09-13 | 55 | [EF-096.md](EF-096.md) |
| EF-097 | ⭐ WHY THE CONSOLE 'LOCKS' AND WHY CHEATS NEED RE-ENABLING EVERY LOAD. The console hotkey action (`DE_Console`: Enter / Alt-Shift-C) is CREATED only if `AreCheatsEnabled() or ConsoleEnabled or Libs.DevToolsPublic` holds when shortcuts are built — an `if` around `XAction:new`, not a runtime check — so with the Mod Manager closed and `ConsoleEnabled` at its default `false` the hotkey does not exist. `ConsoleEnabled` is an unblacklisted global with `ConsoleSetEnabled()`; setting it before shortcuts build makes the hotkey permanent without mod tools. `~` is a DIFFERENT action (`DE_Menu`). F9 = `cls()` is created unconditionally (owner-confirmed working 2026-09-13). `ResetGameSession()` sets `config.BuildingInfopanelCheats = false` on every trip to the main menu. Vanilla's mod-error popup fires ONCE per mod per session (`ReportedMods[mod.id]`) and only when the stack names the mod's path | — | 2026-09-13 | 31 | [EF-097.md](EF-097.md) |
| EF-098 | VANILLA CHEAT-MENU DEFECTS that explain 'the meteor cheats never worked right'. The three meteor presets pass `GetCameraLookAtPassable()` — the camera's look-at point, not the cursor — and `CheatMeteors(type, setting, pos)` is `if pos then ... end`: a nil position does NOTHING, silently. ⚠️ 13 of 105 `CheatDef` presets re-enter `NetSyncEvent("Cheat", ...)` inside their own `run` (all 11 disaster presets except `StopDisaster`/`UndergroundCaveIn`/`UndergroundMarsquake`, plus `StartMystery`/`FinishMystery`), so `def:run()` taints for those; the untainted route is the leaf global (`CheatDustStorm`, `CheatMeteors`, `CheatColdWave` …) with a cursor position | — | 2026-09-13 | 21 | [EF-098.md](EF-098.md) |
| EF-099 | ⭐ PROGRAMMATIC CONSTRUCTION has a sanctioned front door. `PlaceConstructionSite(city, class_name, pos, angle, params, no_block_pass, no_flatten)` is the function the in-game build cursor calls; it adjusts the build position, detects a dome at the point (`GetDomeAtPoint`), flattens terrain, applies to the hex grid, plays the placement FX and returns the site. `CheatCompleteAllConstructions()` completes every site and runs its pass TWICE so domes' second stage completes, calling `CheatCompleteAllWiresAndPipes()` first. Grid runs are placed by `PlaceCableLine` / `PlacePipeLine` / `PlacePassageLine (city, q, r, dir, steps, ...)`. `IsBuildableZoneQR(q, r)` answers fit per hex. All open to mods ⇒ a layout stamper is capture + ordered replay (domes → complete → interiors → complete → grids → complete), not a construction reimplementation | — | 2026-09-13 | 58 | [EF-099.md](EF-099.md) |
| EF-100 | ⭐ THE REMASTER MOVED AMBIENT WEATHER FROM PARTICLES TO PHYSICALLY-BASED ATMOSPHERE, AND THE SKY IS NOW THE WHOLE EFFECT. Every ambient weather particle FX ships `Disabled = true, group = "_inactive"` (DustStorm ×2, GreatStorm ×2, ElectrostaticStorm ×3, ColdWave ×1, MysteryDream ×1 = 9), each replaced 1:1 by a `phys_sky` LightmodelFeaturePreset (`Atmoshpere_DustStorm`, `Atmoshpere_GreatDustStorm`, `Atmosphere_ColdWave`, `Atmoshpere_Rain`, `Dreamers`). Only electrostatic LIGHTNING survives as particles (10 live entries); all storm SOUNDS stay live, so a storm is audible with no dust on every platform. The non-atmosphere delta is ≈12% exposure + a warm gamma ⇒ a storm whose `phys_sky` does not render looks like nothing happened. Already this way at 1.0.7 — not a 1.1.0 regression | — | 2026-09-13 | 49 | [EF-100.md](EF-100.md) |
| EF-101 | ⭐ THE SHADER *SOURCE* TREE SHIPS IN PLAIN TEXT AND WE CAN READ IT: `Packs/Shaders.fpk` holds 281 HLSL files (`.fx`/`.fh`/`.h`/`.hlsli`), opened by `tools/flpk_extract.py`'s `extract()` — including the complete 9-file atmosphere system. `#ifdef PSSL` ×8 are PlayStation LANGUAGE shims (`Lang.fh:19` maps `__PSSL__`→`PSSL`), type/intrinsic adapters, NOT feature gates, and the atmosphere shaders carry ZERO platform conditionals — only engine-set feature defines (`ENABLE_MULTIPLE_SCATTERING`, `EXTINCTION_EVAL_MODE`, `SKY_ONLY`). ⛔ THE BOUNDARY: the C++ that picks those defines per platform is not in the tree, `ShaderCached3d12.fpk` is the d3d12 cache only, and no console shader set ships ⇒ a per-platform rendering divergence is NOT decidable from here. Per-platform options ARE readable: PS5 `GraphicsApi = "agc"`, Xbox `"d3d12"`, and PS5Quality is an ULTRA-tier preset, not a cut-down one | — | 2026-09-13 | 49 | [EF-101.md](EF-101.md) |
| EF-102 | ⭐ THE SHIPPED DEPOT CLASS TREE FORKS INTO TWO SIBLING BRANCHES, AND `UniversalStorageDepotBase` IS ONLY ONE OF THEM. `StorageDepot` (`StorageDepot.lua:1`, parents `Building`/`ResourceStockpileBase`/`ShuttleLanding`) has two children: `UniversalStorageDepotBase` (`:329`, 22 shipped subclasses) and `MultiResourceDepotBase` (`MultiResourceDepot.lua:8`, parents `{StorageDepot, MultiResourceCubeVisuals}`, 5 distinct shipped subclasses across 7 definition sites). They are SIBLINGS — neither inherits the other — so `IsKindOf(o, "UniversalStorageDepotBase")` silently excludes 5 shipped food/ingredient depots. ⛔ The two branches also carry DIFFERENT property names: `storable_resources` is declared at `StorageDepot.lua:336` INSIDE the `UniversalStorageDepotBase` block, not on `StorageDepot`, while `MultiResourceDepotBase` uses `storable_resource_groups` — so `#storable_resources` is a discriminator for ONE branch only. Supersedes the false kernel line 'every depot is `UniversalStorageDepotBase`', which stood 09-13 to 09-14. ⚠️ AMENDED 2026-09-14 (smrtk 08b, in play): there is a THIRD family. `MechanizedDepot` (`StorageDepot.lua:834`) is NOT a `StorageDepot` at all — `__parents = { Building, StockpileController, ResourceStockpileBase, ElectricityConsumer }` — with ~12 shipped `MechanizedDepot*` subclasses, and it declares its OWN `storable_resources` (`:838`). ⇒ a `StorageDepot` guard misses that whole family exactly as a `UniversalStorageDepotBase` guard misses the other five, and the TRUE common ancestor of all three families is `ResourceStockpileBase` | 2026-09-14 | 2026-09-14 | 82 | [EF-102.md](EF-102.md) |
| EF-103 | ⭐ A STANDALONE HABITAT IS A `Community` BUT NEVER A `Dome`, SO IT IS INVISIBLE TO EVERY `labels.Dome` READER. `NaturalHabitatBase` → `MicroGHabitatBase` (`MicroGHabitat.lua:3-4`, `__parents = { LivingBase, LifeSupportConsumer, WaypointsObj, Community, MicroGHabitatAutoResolve, FoodBuilding }`) carries no `Dome` ancestor, and `AddToLabel("Dome", self)` occurs in exactly ONE place tree-wide (`Dome.lua:1103`, `Dome:GameInit`), so a habitat cannot enter that label; **70** sites in `Lua/` iterate `labels.Dome`. It IS in `labels.Community` (`Community.lua:47`) and inherits `accept_colonists = true` (`:15`), so community-keyed code does see it. ⛔ The engine nonetheless gives it dome-shaped API — `GetClusterDomes`, `ReserveResidence`/`AddResident`/`RemoveResident`, `HasFreeLivingSpaceFor` — and ⚠️ OVERRIDES `CanVisit` with a free-space test (`MicroGHabitat.lua:59-73`) that `Dome` does NOT have (Dome declares no `CanVisit` override at all), so a FULL habitat is rejected one gate earlier than a full dome. ⭐ The canonical residency predicate is vanilla's own `IsKindOf(unit.residence, "MicroGHabitatBase")` (`Workplace.lua:1453`) — key on `residence`, never `dome`, which is cleared the moment a colonist steps outside | 2026-09-17 | 2026-09-17 | 83 | [EF-103.md](EF-103.md) |
| EF-104 | ⭐ EXPEDITION CREW IS DRAFTED COLONY-WIDE AND BOARDS BY TELEPORT — NO DISTANCE, DOME, QUARANTINE OR TRANSPORT CHECK EXISTS. `CargoTransporter:GatherAvailableColonists` (`Buildings/CargoTransporter.lua:237`) pools `city.labels.Colonist` plus connected cities, drops anyone with `thread_running_destructors` (a SILENT pre-filter, live for expeditions because they never `quick_load`), then sorts into four buckets — idle+unemployed, busy+unemployed, idle+employed, busy+employed — where `idle` means only `command == "Idle" or "Abandoned"`, so a RESTING colonist counts as busy. ⛔ A demanded specialisation is a HARD filter applied before the buckets and NEVER relaxed (`FilterColonistsByTrait`, `CargoTransporterNew.lua:197-203`); only Tourist/Child/Senior are soft-filtered and progressively relaxed. Boarding has NO pathing: `Colonist:EnterTransporter` → `Unit:EnterTransporter` (`Unit.lua:1292-1306`) sets the holder and calls `Disappear`, so colonists never walk to the rocket. ⚠️ The ASTEROID lander is a DIFFERENT implementation (`LanderRocket.lua:1149`) drawing from `self.cargo_passengers`, which the PLAYER fills via the cargo dialog — it is not an auto-draft. ⛔ ONE OBSERVATION IS UNEXPLAINED (2026-09-15): with specialisation "Any", three idle+unemployed colonists were passed over while three employed were taken | 2026-09-16 | 2026-09-17 | 109 | [EF-104.md](EF-104.md) |
| EF-105 | ⛔ STEAM'S PER-BRANCH WORKSHOP DELIVERY IS OFF FOR SURVIVING MARS: RELAUNCHED, AND "REVERT TO THIS VERSION" IS NOT A SUBSTITUTE. Workshop Item Versioning would serve a player on an old game branch the matching mod version from one listing, but it needs the developer to tick "Enable Game Branch Versions". The author-side controls that would show it are absent in both places Valve's documentation names (the item's Change Notes list, and Edit Change Note). "Revert to this version" makes that version live for ALL subscribers. ⇒ an old-branch build ships only as a manual download, and the player must unsubscribe first: Workshop, Paradox and AppData/Mods feed one dedup keyed on mod id where the higher version wins. | 2026-09-08 | 2026-09-16 | 24 | [EF-105.md](EF-105.md) |
| EF-106 | ANY ENABLED MOD BLOCKS ALL ACHIEVEMENTS ON EXACTLY PLAYSTATION, XBOX AND THE MICROSOFT STORE, and this pack can neither cause nor avoid it | 2026-09-16 | 2026-09-16 | 21 | [EF-106.md](EF-106.md) |
| EF-107 | A HABITAT COPIES EVERY WORKPLACE WITHIN ITS WORK RANGE ONTO ITS OWN LIST WHEN IT IS BUILT, NEIGHBOURING DOMES' INTERIORS INCLUDED, AND A FREE SLOT IN ANY OF THEM PINS ITS RESIDENTS: the emigration scorer counts it as a job at home, the job picker refuses it | 2026-09-16 | 2026-09-16 | 52 | [EF-107.md](EF-107.md) |
| EF-108 | 'IT'S TOO COLD' IS A COLD-WAVE FEAR CHARGE, NOT A DOME TEMPERATURE: during an active Cold Wave every colonist whose dome's CENTRE point reads below the dome's freeze threshold on the OUTSIDE heat grid loses sanity every hour; the dome itself contributes no heat, and 50% Temperature ends cold waves for good | 2026-09-17 | 2026-09-18 | 42 | [EF-108.md](EF-108.md) |
| EF-109 | BuildingTemplates IS THE WRONG TABLE FOR A DATA PATCH: on a cold boot it is EMPTY at DataLoaded (filled only on ClassesBuilt and DataChanged), and its values are id-less class proxies, so `.id` is nil. Read presence from BuildingTemplates_Raw and labels from g_Classes[id] | 2026-09-18 | 2026-09-18 | 17 | [EF-109.md](EF-109.md) |
| EF-110 | ⭐ SAVEGAME METADATA IS READABLE WITHOUT DECODING: a `.savegame.sav`'s first ~200 KB carries plain-text `map`/`mission_sponsor_id`/`orig_lua_revision`/`active_mods`/`platform.cheats`; match `orig_lua_revision`, not `lua_revision` (which repeats once per mod first); `platform.cheats` is a save-time hint, not the decoded `CheatsUsed` taint record (`EF-095`), and a short 3 KB read produces a false negative. The owner's real save folder is `C:\Users\stkot\Saved Games\Surviving Mars Relaunched\<steamid>`, exposed in-repo via junctions `saves/game` / `saves/backup` / `saves/reporters/`; `%APPDATA%\Surviving Mars\<steamid>` is an agent-backup/test decoy, not the owner's play history. Taint is a property of a colony's LINEAGE, not of one save | — | 2026-09-22 | 46 | [EF-110.md](EF-110.md) |
| EF-111 | ⭐ METHOD for testing a 'missing sound/FX' defect live: ear A/B alone is not a reading, because sibling sounds on the same start/end moments play regardless and can be mistaken for the missing one. Learn the sound by hand-firing PlayFX next to the SELECTED object (a far one is inaudible and reads as missing), prove it fires with a log-only PlayFX hook, isolate a busy scene with SetAllVolumesReason plus a STRICT hook, A/B a preset fix by emptying its .Moments in place, and check the moment id and phase before calling a null result a refutation. Worked out in the 2026-09-10 sitting that produced C77 | — | 2026-09-22 | 35 | [EF-111.md](EF-111.md) |
| EF-112 | BROKEN-TRACK DRONE WORK IS CONSTRUCTION: Drone:Work supplies the request protocol, while TrackConstructionSite delegates to ConstructionSite:DroneWork, whose visual states are constructStart/constructIdle/constructEnd with Construct FX; building maintenance instead uses repairBuilding*/Repair | — | 2026-09-22 | 28 | [EF-112.md](EF-112.md) |
| EF-113 | DRONE WORK RADIUS GATES AUTOMATIC REQUEST DISCOVERY, NOT A UNIVERSAL PHYSICAL CAPABILITY: track connectivity creates no task coverage; manual Work can bypass FindTask but still needs request assignment/approach. Broken track is construction, station self-service fills maintenance material only, and dust cleaning is the effect of maintenance rather than a separate clean job | — | 2026-09-22 | 34 | [EF-113.md](EF-113.md) |
| EF-114 | ForEachConnectedTrack IS ONE-HOP AND HIDES BROKEN EDGES: it calls GetDestStation, which returns false while any element is under construction; a broken-track construction site enters that array. Physical endpoint getters remain available, so repair reachability needs its own visited graph plus tunnel links—not train routes or helper emptiness | — | 2026-09-22 | 34 | [EF-114.md](EF-114.md) |
| EF-115 | DRONE BATTERY/PALETTE/CONTROL SURFACES: battery_max defaults to 80000 stored units; idle seeks charge at <=12000 and work interrupts at <=6000, absolute rather than percentages. Building.SetPalette recursively accepts any valid colorizable object. CanBeControlled is the shared gate for both reassign buttons | — | 2026-09-23 | 39 | [EF-115.md](EF-115.md) |
| EF-116 | THE GAME'S LUA DIVIDES INTEGERS AS INTEGERS: shipped code coerces with `a * 1.0 / b`, `x + 0.0` or DivAsFloats() before any fractional division, and Min/Max/Clamp are documented integer helpers; a standard-Lua mock (lupa) divides to a float, so it cannot show `remaining / 1000 == 0` or a sampler stuck on span endpoints | — | 2026-09-23 | 35 | [EF-116.md](EF-116.md) |
| EF-117 | GetSpotBeginIndex RAISES ON A SPOT NAME THE ENTITY LACKS: `HGE::l_GetSpotBeginIndex: Invalid spot`, not -1; the exported doc's -1 is for an object that does not exist. Shipped Lua guards with HasSpot first and never tests the index against -1, so a `< 0` check after the call is dead code and a loop that stops on -1 never stops | — | 2026-09-25 | 23 | [EF-117.md](EF-117.md) |

