---
id: "EF-103"
seq: 103
summary: "⭐ A STANDALONE HABITAT IS A `Community` BUT NEVER A `Dome`, SO IT IS INVISIBLE TO EVERY `labels.Dome` READER. `NaturalHabitatBase` → `MicroGHabitatBase` (`MicroGHabitat.lua:3-4`, `__parents = { LivingBase, LifeSupportConsumer, WaypointsObj, Community, MicroGHabitatAutoResolve, FoodBuilding }`) carries no `Dome` ancestor, and `AddToLabel(\"Dome\", self)` occurs in exactly ONE place tree-wide (`Dome.lua:1103`, `Dome:GameInit`), so a habitat cannot enter that label; **70** sites in `Lua/` iterate `labels.Dome`. It IS in `labels.Community` (`Community.lua:47`) and inherits `accept_colonists = true` (`:15`), so community-keyed code does see it. ⛔ The engine nonetheless gives it dome-shaped API — `GetClusterDomes`, `ReserveResidence`/`AddResident`/`RemoveResident`, `HasFreeLivingSpaceFor` — and ⚠️ OVERRIDES `CanVisit` with a free-space test (`MicroGHabitat.lua:59-73`) that `Dome` does NOT have (Dome declares no `CanVisit` override at all), so a FULL habitat is rejected one gate earlier than a full dome. ⭐ The canonical residency predicate is vanilla's own `IsKindOf(unit.residence, \"MicroGHabitatBase\")` (`Workplace.lua:1453`) — key on `residence`, never `dome`, which is cleared the moment a colonist steps outside"
updated: "2026-09-17"
verified: "2026-09-17"
lines: 83
derived_at: "game 1.1.0.403908 build 24995074"
---
- **MEASURED 2026-09-15 from the live `ModTools\Src`.** The class, and what it is not:
  - `MicroGHabitatBase` — `Lua/Buildings/MicroGHabitat.lua:3-4`,
    `__parents = { "LivingBase", "LifeSupportConsumer", "WaypointsObj", "Community",
    "MicroGHabitatAutoResolve", "FoodBuilding" }`. **No `Dome` anywhere in that list.**
    `NaturalHabitatBase` descends from it, so one test covers Naturalist and Micro-G alike.
  - `grep -rn 'AddToLabel("Dome"' <Src>/Lua` returns **one** line, `Dome.lua:1103`, inside
    `Dome:GameInit`. ⇒ a habitat can never enter `labels.Dome`.
  - `grep -rn 'labels\.Dome\b' <Src>/Lua | wc -l` ⇒ **70**. ⛔ Those 70 are NOT all defects —
    many legitimately mean actual domes (dome cap, placement, dome UI). What is established is
    that a habitat is absent from every one of them; whether each absence is wrong needs reading
    that site. ⛔ **No blanket repair on the strength of this count.**
- ⭐ **It IS community-visible, which is why it is not universally excluded.**
  `Community:GameInit` does `self.city:AddToLabel("Community", self)` (`Community.lua:47`) and
  `accept_colonists = true` is declared on `Community` itself (`:15`), inherited unchanged. Any
  sweep over `city.labels.Community` sees a habitat; any sweep over `labels.Dome` does not.
- ⚠️ **The asymmetry that bites: `CanVisit`.** `Dome` declares **no** override, so it answers
  `Community:CanVisit` — `not self.destroyed and not self.demolishing` (`Community.lua:420-422`).
  `MicroGHabitatBase:CanVisit(colonist)` (`MicroGHabitat.lua:59-73`) adds a capacity test and
  returns `self.free_spaces.inclusive > 0` when called with **no** colonist. ⇒ **a full dome stays
  in a candidate list; a full habitat is dropped.** ⭐ Its early-out —
  `if IsValid(colonist) and (self.reserved[colonist] or table.find(self.colonists, colonist))
  then return true` — is correct for a returning resident but **never fires from a caller that
  passes no colonist**, e.g. `GetDomesReachableByColonists` (`_GameUtils.lua:399`).
  ⚠️ Free space counts reservations as occupied: `Residence:GetFreeSpace` is
  `capacity - closed - (#reserved + #colonists)` (`Residence.lua:232-233`).
  **MEASURED 2026-09-17, unattended:** the C95 return-home A/B isolated a full
  nearby habitat missing from the reachable list despite `walk=true`; the unshipped
  selector admitted its held resident and they physically returned, while the
  control assigned a dome. Controlled native boarding/unloading, not a flown
  mission; build 6a91a190, code/evidence `194f4c3`, [report and logs](../reports/C95_RETURN_HOME_EXPLORATION.md).
- **Dome-shaped API it DOES implement** (so "it will throw on dome methods" is not a safe
  assumption): `GetClusterDomes` (`MicroGHabitat.lua:103`), `ReserveResidence` / `AddResident` /
  `RemoveResident` (`:77-100`), `HasFreeLivingSpaceFor` (`:177-182`),
  `MicroGHabitatAutoResolve:IsSuitable` = `GetScoreFor(colonist) > 0` (`:173-175`), so residency
  is score-gated and re-evaluated rather than permanent.
- **Isolation flags, all on `MicroGHabitatBase`:** `allow_work_in_connected = false` and
  `allow_service_in_connected = false` (`:11-12`), `can_be_safety_dome = false` (`:14`),
  `dome_forbidden` on the templates. Turning a habitat off calls `KickAllColonists` (`:192-203`)
  and anyone who cannot reach a dome is left Stranded.
- ⛔⛔ **THREE OF THOSE FLAGS ARE PLAYER POLICIES — NEVER KEY A PREDICATE ON THEM** (measured
  2026-09-16 against the archived 1.0.7 tree). `allow_work_in_connected` and
  `allow_service_in_connected` are **not new**: they existed on `Dome` in 1.0.7 and are toggled from
  the dome UI (`Dome.lua:849`, `:883` → `Community:TogglePolicy`), as is `accept_colonists`
  (`Community.lua:147`). 1.1.0 only **moved the two up to `Community`** so a habitat can decline
  them. ⇒ any test of the form "this community forbids connected work" also matches **a dome whose
  player switched the policy off**. ⭐ `can_be_safety_dome` alone is genuinely new in 1.1.0 and is
  **not** toggleable — but it means "may shelter the homeless", not "is a habitat".
  ⇒ **The class test is the predicate; the flags are only the reasoning.**
- ⭐ **What it actually takes for a habitat to enter `GetDomesReachableByColonists`'s list**
  (`_GameUtils.lua:399-414`), in order — the first loop walks `city.labels.Community`, so the
  habitat IS a candidate there: ① `community:CanVisit()` (the capacity gate above) · ② `dist >= 0`,
  a foot route exists · ③ **`is_walking`** · ④ `is_welcoming_community` — a **file-local**
  (`:382-384`, present in 1.0.7 too, ⛔ not wrappable) requiring `accept_colonists` and `ui_working`
  and `HasLifeSupport()`. ⛔ **And nothing else can rescue it:** the elevator leg recurses into this
  same function, but **the train sweep iterates `s.labels.Dome` and `GetClusterDomes()`**
  (`:452-456`), so a rail route can never add a community. ⇒ **for a habitat, walking distance is
  the only distance qualification**. Capacity is gate ①, before walking distance,
  so a full habitat may fail there even when it is nearby. This describes list
  construction, not physical travel: Colonist:SetCommand can dispatch an already
  chosen habitat through a native train route (`ColonistTransport.lua:204-207,381-442`).
- ⭐ **Vanilla's own idiom for "home community, dome or habitat" is `residence.parent_dome or
  residence`** (`Colonist.lua:5051`, `:5069`). ⚠️ `Colonist:CancelRestrictedReservedExpeditionWorkplaces`
  (`:5061-5073`, **new in 1.1.0**) builds `home_dome` that way and then branches on
  `home_dome.allow_work_in_connected` — so 1.1.0 expedition code already treats a habitat as its own
  home community.
- ⭐ **Employment already carves habitat residents out, and supplies the predicate.**
  `IsSuitableWorkplace` (`Workplace.lua:1446-1462`) tests
  `IsKindOf(unit.residence, "MicroGHabitatBase")`, then rejects any workplace with a valid
  `parent_dome` and any workplace beyond `unit.residence:GetOutsideWorkplacesDist()`.
  ⚠️ **They are NOT unemployable** — an outside workplace sited near the habitat is a legal job.
  ⛔ Key on `residence`, not `dome`: `Colonist:SetDome(false)` clears `dome` whenever they step
  outside, while a resident's `residence` persists.
- **Build data, which is why it reads as a dome to a player:** both habitat templates declare
  `build_category = "Domes"`; `NaturalistHabitat.lua:26` has `group = "Domes"` so it appears in
  the Domes build menu beside real domes, while `MicroGHabitat.lua:31` has `group = "Asteroids"`
  and does not. ⛔ Menu membership is not class identity — Passage, Passage Hub and Passage Ramp
  share that menu and are not domes either.
- **Falsifier, one command each.** `grep -rn 'AddToLabel("Dome"' <Src>/Lua` must stay at one hit
  in `Dome:GameInit`; `MicroGHabitat.lua:3-4` must still lack a `Dome` parent; `Dome` must still
  declare no `CanVisit`. **Stale if** a patch gives a habitat a `Dome` ancestor, adds a second
  `Dome`-label writer, or gives `Dome` its own `CanVisit`.
- **Consumer:** `bugs/C95.md` (expedition return), which is filed against the draft rather than
  against any of this.
