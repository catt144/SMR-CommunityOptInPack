---
id: "EF-078"
seq: 78
summary: "⭐ MEASURED 1.1.0 BASELINE (first launch, 2026-09-08, log `archive/logs/first110_*.log`): game **1.1.0.403908**, `LuaRevision` **403908**, both DLCs load, the pack loads UNFLAGGED — and **13 of 80 modules go inactive**, of which the in-game dialog names **11** as \"switched themselves off for safety\". ⛔ THE SOURCE-READ PREDICTION (`EF-076`) SAID 6 AND WAS WRONG BY 5: it missed `MeteorFrequency`, `MeteorStormWedge`, `VacuumWalks` (path specs checked by LAST-SEGMENT NAME only — `Meteors` is a ubiquitous word, so the grep passed spuriously) and `DustSicknessDamage`, `IndependenceTerraforming`, `SaintBlessing` (preset/DATA content checks, invisible to any symbol sweep). ✅ It DID predict `LastTransmissionStorage`'s benign latch exactly. No `[LUA ERROR]`, no assert, no blame line anywhere in the log"
updated: "2026-09-08"
verified: "2026-09-08"
lines: 67
derived_at: "game 1.1.0 build 24995074"
derivation_basis: "c81e6f69 observation; archive/logs/first110_Mars.exe-20260908-15.20.28-6a91a190.log:56 records 1.1.0.403908; Steam identity recorded by EF-075; historical mod-state scope retained"
---
- ⭐ **THE FIRST MEASURED 1.1.0 READING.** (Owner launch 2026-09-08 15:20;
  archived verbatim at
  `docs/archive/logs/first110_Mars.exe-20260908-15.20.28-6a91a190.log`.)
  `Build version: 1.1.0.403908` · `Lua revision: 403908` · `Assets revision:
  33006` · build hash `6a91a190` (1.0.7 was `6a22b86d`). Both DLCs load:
  `norman (Feeding the Future)` and `thomas (Interplanetary Codex)`.
- ✅ **`EF-077` CONFIRMED AT RUNTIME.** `[mod] Loaded mod def Relaunched Fix Pack
  (id SMR_CommunityFixPack, v1.00-005) unpacked from appdata` — the pack loads
  with **no obsolete or incompatible flag**, and the real `LuaRevision` is
  **403908**, so `ModsUIIsModCompatible`'s `350453 >= 350453 and 350453 <=
  403908` holds on measured numbers rather than inferred ones.
- ⭐ **13 OF 80 MODULES INACTIVE — the measured list, with the engine's own
  reasons.** Nine fail on the FIRST pass, four more on a later (post-DataLoaded)
  pass:
  - `MeteorFrequency` — Meteors thread function not found
  - `VacuumWalks` — walk-distance constants not found
  - `DroneUnreachableForever` — the unreachables expiry mechanism is gone
  - `TouristSatisfaction` — `Colonist.UpdateSatisfaction` not found
  - `LowStorageWarning` — `GetCommandCenterLifeSupportGrids` not found
  - `AsteroidLanderAvailable` — `PlanetaryAsteroidVisitPossible` not found
  - `GridGlobalStorage` — `GetGridGlobalStorage` not found
  - `MeteorStormWedge` — MeteorStorm thread function not found
  - `RainsDeadlock` — `RainsDisasterLoop` not found
  - `SaintBlessing` — no dome-colonists trait presets *(2nd pass)*
  - `DustSicknessDamage` — `TraitPresets.DustSickness` has no
    param/daily_update_func *(2nd pass)*
  - `IndependenceTerraforming` — `Independence_TerraformingProjects` no longer
    modifies `SpecialProjectResourcesModifier` *(2nd pass)*
  - `LastTransmissionStorage` — the shipped presets are already correct *(2nd
    pass, benign)*
  ⇒ **67 of 80 active.** The player-facing dialog names **11** of these as
  "switched themselves off for safety" — the 13 minus `SaintBlessing` and
  `LastTransmissionStorage`, i.e. only the ones that set `update_suspect`.
- ⛔ **`EF-076`'s SOURCE-READ PREDICTION OF 6 WAS WRONG, AND THE TWO REASONS ARE
  METHOD FAILURES WORTH MORE THAN THE NUMBER.**
  1. **Path specs were checked by their LAST SEGMENT'S NAME, not as a path.**
     `{ path = {"GlobalGameTimeThreadFuncs","Meteors"} }` was "verified" by
     grepping for `Meteors`, a word that appears all over the tree ⇒ a spurious
     pass. Same for `MeteorStorm`, and for `VacuumWalks`' `const` walk-distance
     entries, where `DefineConstInt("Colonist", "ColonistMaxDomeWalkDist", …)`
     was found and read as proof that `const.ColonistMaxDomeWalkDist` resolves —
     **it does not**. ⇒ **A path spec must be verified as a PATH.**
  2. **Preset/DATA content checks are invisible to any symbol sweep.**
     `SaintBlessing`, `DustSicknessDamage` and `IndependenceTerraforming` fail on
     the *shape of shipped preset data*, which no grep for function or const
     names can see. ⇒ **Only a runtime read measures these. Ever.**
- ✅ One prediction landed exactly: `LastTransmissionStorage` takes the **benign
  latch** ("the shipped presets are already correct"), as `EF-076` derived from
  `ScriptCheckGridGlobalStorage` being gone.
- ✅ **NO ERRORS.** Zero `[LUA ERROR]`, zero asserts, zero `EF-065` blame lines
  in the whole log. Neither `F111` nor `F113` fired — consistent with their
  triggers (a staffed automated metals extractor with overtime; an automode
  rocket landed at Earth), neither of which a short menu session reaches.
- ⚠️ **ALL 11 TRAIN/TRACK MODULES REPORT `applied`** — `TrainMinors`,
  `TrackConnectorPingPong`, `TrackTunnelPowerBridge`, `TrackSalvageWipe`,
  `TrackSalvageRefund`, `TrainCargoDumping`, `TrainWaitTime`, `TrainsToVoid`,
  `BrokenTrackSalvage`, `TrainPlatformWedge`, `DestroyedTunnels`. So `F114`'s
  candidate set is fully active and none of it self-disabled.
- ⚠️ **THE OPT-IN PACK IS ENABLED AND APPLYING** (9 `[CommunityOptInPack] …:
  applied` lines: ClassicRockets, AcknowledgedWarnings, ResidencyControl,
  MultipleSuns, DroneOverhaul, CohortHousing, DroneStatDials, …). `STATE` and
  checklist 43 record it as OFF — **that record is stale**, and a second pack
  touching drones is a **confound for any `F114` train leg**.
- ⛔ **There is NO `fix pack present: N/N` line at boot.** That line is the
  TestKit's (`TestKit/Code/00_TestCore.lua`) and only appears when the suite is
  run — do not wait for it in a boot log. The per-module `applied` / `inactive`
  lines ARE the boot read; `SMRFixPack.ListFixes()` is the console read.
