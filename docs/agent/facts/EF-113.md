---
id: "EF-113"
seq: 113
summary: "DRONE WORK RADIUS GATES AUTOMATIC REQUEST DISCOVERY, NOT A UNIVERSAL PHYSICAL CAPABILITY: track connectivity creates no task coverage; manual Work can bypass FindTask but still needs request assignment/approach. Broken track is construction, station self-service fills maintenance material only, and dust cleaning is the effect of maintenance rather than a separate clean job"
updated: "2026-09-22"
verified: null
lines: 34
derived_at: "game 1.1.0.403908 build 24995074"
---
- **SOURCE — automatic discovery.** `DroneControl:ConnectTaskRequesters` finds requesters within
  drone-node radius and working extenders (`Lua/Buildings/DroneControl.lua:375-416`);
  `TaskRequester:FindDroneNodes` filters by `node.work_radius >= HexAxialDistance(...)`
  (`Lua/_TaskRequest.lua:286-323`). `AddCommandCenter` then files that requester's queues with the
  controller (`CommonLua/TaskRequest.lua:149-161,298-348`). Track connectivity is absent from this
  path. `Drone:Idle` calls the controller's `FindTask` only when it can command drones
  (`Lua/Units/Drone.lua:672-704`; `Lua/Buildings/DroneControl.lua:288-290`).
- **SOURCE — range is not a ban inside `Work`.** Manual interactions can issue `Work`/`PickUp`/
  `Deliver` without `FindTask` (`Drone.lua:2341-2355,2820-2832,3087-3089`), but `Drone:Work` still
  requires request assignment, vanilla approach, fulfilment and target callbacks (`:983-1022`).
  Therefore the supported claim is "not automatically discovered outside coverage", not "cannot
  physically work outside coverage"; native assignment/pathfinding limits still need a game probe.
- **SOURCE — work kinds.** A broken track is a construction group/site, not `RequiresMaintenance`
  (`Lua/Buildings/Track.lua:623-650`; `Lua/Buildings/TrackElement.lua:139-174`). Station maintenance
  has a material demand and a separate `repair` work request (`Lua/RequiresMaintenance.lua:64-99,
  187-210`). `Station:SelfService` can transfer its own stock into the demand, but
  `StartWorkPhase(nil)` leaves repair work for a drone (`Lua/Buildings/Station.lua:482-510`;
  `Lua/RequiresMaintenance.lua:196-205,616-623,655-682`).
- **SOURCE — dust.** `CleanNeeded()` aliases `RepairNeeded()` and maintenance `Repair()` resets
  accumulated maintenance and dust visuals (`Lua/Buildings/Building.lua:1061-1068`;
  `Lua/RequiresMaintenance.lua:397-425,480-491`). `DroneResourceUnits.clean` exists, but existence
  is not a task producer (`Lua/Units/Drone.lua:784`). Ordinary intact track and track-tunnel
  templates expose no maintenance resource; tunnel exceptional maintenance is a separate scripted
  case (`Lua/RequiresMaintenance.lua:22,97-99,581-601`).
- **MEASURED ABSENCE, decoded archived tree.** `rg --hidden --no-ignore` over `Src/**/*.lua` for
  `(?i)(AddWorkRequest.*clean|AddRequest.*clean|clean_work_request|work_request.*clean)` returned 0;
  compressed-input extensions `zip|gz|zst|lz4|hpk|xz|7z` returned 0. The positive-control filter
  `DroneCleanAmount|CleanNeeded` returned exactly 4 members: `Lua/__const.lua:591-592`,
  `Lua/Buildings/Building.lua:1066`, `Lua/Units/Drone.lua:784`. Any new producer hit or changed
  positive set falsifies the absence claim.
- **BOUNDARY.** Construction-group coverage can extend through a covered member, and controller
  repair, dome coverage, extenders and manual commands are explicit exceptions. A track graph is
  not a general authorization to dispatch destructive clearing, upgrades, refabrication or repair
  of unrelated nearby units.
