---
id: "EF-091"
seq: 91
summary: "A landed non-player rocket launches the instant its cargo reads ready, and a drone request's GetTargetAmount goes negative after a re-size while drones hold claims (F119 sitting)"
updated: "2026-09-11"
verified: "2026-09-11"
lines: 18
derived_at: "game 1.1.0 build 24995074"
derivation_basis: "5056a001 observation; archive/logs/f119sitting110_Mars.exe-20260911-14.02.05-6a91a190.log:53 records 1.1.0.403908; EF-075/EF-085 record Steam identity; C-side arithmetic remains inferred"
---
- **MEASURED 2026-09-11 (F119 sitting, attended; log `archive/logs/f119sitting110_*`):** a landed
  `UniversalTradeRocket` (`RocketType` Trade) in `CmdLoad` took off the moment its fuel reached the
  need — seen on two rockets. SOURCE: `UniversalRocketBase:CmdLoad` holds before take-off only when
  `self:IsPlayerControlled()` (`Lua/UniversalRocket.lua:507-512`); every other rocket leaves as soon
  as `IsCargoReady` passes (`:500-502`, `:514-515`).
  ⇒ an in-play trigger aimed at a landed non-player rocket must land WHILE it is still loading.
- **MEASURED:** straight after a fuel-cost drop re-sized a rocket (fuel 40, need 30), its fuel
  demand request read `GetTargetAmount() = -10000`, although both updaters set it with
  `SetAmount(Max(0, ...))` (`Lua/CargoTransporterNew.lua:1458`; the pack's copy
  `Code/Fix_RocketDroneChurn.lua:140`).
- **INFERRED, not measured:** the target is the set amount net of units already claimed by drones
  in flight; the fix-off leg read 5000 against a 9000 gap, consistent with ~4000 in flight. The
  claim arithmetic is C-side and was not read.
- **SOURCE:** the shipped code treats a negative demand target as "cancel": `Lua/Units/Drone.lua:1466`
  drops the delivery, `Lua/CargoTransporterNew.lua:1526-1528` refuses the unload, and
  `Lua/UniversalRocket.lua:1939-1941` interrupts inbound drones on a re-size when auto mode is off.
  The watched outcome agreed (excess fuel removed, rocket left).
- ⇒ never read a negative demand target as a defect on its own; read what the drones DO next.
