---
id: "EF-051"
seq: 51
summary: "Steam Cloud RESTORED deleted savegames at launch, which is why the save-dir close-out gate 'failed' twice. ✅ CLOSED 2026-08-11 (retirement audited) — held 08-12→08-14 while the owner re-ticked cloud ON (the hold window restored all 17 named strays at the first launch, third full sample of the mechanism). ✅ HOLD LIFTED 2026-08-14: re-untick reported, strays deleted (888 MB), 76 .sav re-listed by name, zero strays; falsifier = any of the 17 returning at the next launch. ⚠️ **2026-08-15: the falsifier did NOT fire across two more launches (nothing created in the save dir at either), but the 08-14 listing was INCOMPLETE — the extensionless `U2RT1` was and is present, missed because the sweep globbed `*.sav` and EF-050 lets this project write savegames with no extension.** ⚠️ Baselines must be recorded as NAMES, never a count — and enumerate the DIRECTORY, not the glob"
updated: "2026-08-15"
verified: "2026-08-14"
lines: 176
---
- **Deleting a savegame agent-side, with the game closed, does NOT stick.** The
  next Steam launch writes it back. MEASURED 2026-08-11 (`unattended-2`), and
  the measurement is the file system's own metadata:

  | | before launch 1 (01:14) | after launch 2 (01:28) |
  |---|---|---|
  | `.sav` files in the save dir | **55** | **69** |
  | `CB1STAGE` / `CB2STAGE` / `CORUN0` / `CORUN1` / `U1STAGE` | absent | present |
  | `CB2F85` / `CB2PKEY` / `CB2PKEY2` / `CB2UNINSTALL` | absent | present |
  | `U1C0PROOF` / `U1C1HEAL` / `U1C2PT35` / `U1C6FORCED` / `U1C6HEALED` | absent | present |

  **Fourteen files**, and every one of them carries `CreationTime` between
  **01:17:04 and 01:17:33 on 2026-08-11** — inside the launch-1 window — while
  its `LastWriteTime` is its ORIGINAL date (2026-08-03, -08-04, -08-10),
  preserved. A file created tonight whose contents are dated a week ago was
  written tonight from a stored copy.

- ⭐ **The restore happens BEFORE the game process starts.** Steam was invoked
  at 01:17:00.8, the 14 files appeared 01:17:04–01:17:33, and `Mars.exe`'s
  process start time is **01:17:34**. So this is Steam's pre-launch sync, not
  anything the game does. `steam_autocloud.vdf` sits in the same directory and
  was rewritten at each launch.

- ⚖️ **This CLOSES the open hypothesis and clears two sessions that were
  recorded as having failed.** The save-dir close-out gate was recorded as
  "FAILED TWICE (same 4 files)" — batch-1's audit and batch-2's prep both found
  staged saves they had deleted. **They had deleted them.** Steam put them back.
  The remedy is not more diligence at close-out; the deletion is being undone by
  something outside the repo and outside the game.

- ⛔ **Consequence for every close-out from here.** A save-dir listing taken
  with the game closed proves the deletion happened; it does **not** prove the
  file stays gone, and the next launch is where it comes back. The listing rule
  (WORKFLOW "Co-runs") still stands — it is what caught this — but a session may
  only claim "deleted", never "gone for good", until Steam Cloud is off for this
  title. **Prediction, with its falsifier:** `U2STAGE`, `U2RT1` and `U2RT2`,
  deleted 2026-08-11 01:28, will be back after the next launch. If they are not,
  the mechanism above is wrong and this fact needs re-deriving.
  ⭐ **CHECKED THE SAME NIGHT, AND IT HELD.** The owner launched at ~01:57 to
  re-enable the pack; all three were on disk again by **01:57:47**, creation
  stamps from that minute, original modification dates preserved — plus a fourth,
  the extensionless `U2RT1` artifact from run 1. So the mechanism is confirmed on
  two independent launches by two different people, and the prediction that would
  have falsified it was written before the check.

- **The remedy is one owner tick**, and it is on `PLAYTEST_CHECKLIST.md`:
  Steam → Surviving Mars: Relaunched → Properties → General → **untick "Keep
  game saves in the Steam Cloud"**, then delete the strays once more.

- ✅ **TICKED 2026-08-11** (owner: *"Steam settings done"*, same day as the
  measurement). The 14 strays were deleted immediately after — 732 MB — and
  the directory verified at **55 `.sav` files**, the exact pre-restore
  baseline, with both protected fixtures MD5-verified untouched.
  ⛔ **Still owed: one post-untick launch.** The untick is the owner's setting
  change, not yet a sampled behaviour — the first launch after it is the
  falsifier. Listing still 55 → the restore mechanism is dead and close-outs
  may record "gone" again (the WORKFLOW clause retires). A stray back → this
  fact needs re-deriving.

- ⭐⭐ **THE POST-UNTICK LAUNCH HAS ALREADY HAPPENED, AND NOTHING CAME BACK
  (observed 2026-08-11 by `corun-pt15` prompt 1, from file-system metadata).**
  It was not our launch and it was not planned as the test — the owner simply
  played. That is arguably a better sample than a staged one, and the
  prediction was written down first.

  | evidence | value |
  |---|---|
  | untick + stray deletion recorded | commit `d03e4ef`, **09:43:57** |
  | `steam_autocloud.vdf` rewritten (the launch marker this fact records) | **09:47:15**, 3 min 18 s later |
  | the owner's play session that followed | `Autosave Sol 11` created 10:53:44 · `PT-15.savegame.sav` 10:58:44 · `Autosave Sol 16` 11:11:56 |
  | **any of the 14 named strays present** | ⭐ **NONE** |
  | any file carrying the restore signature (created now, modified days ago) | none, except `CP15STAGE.savegame.sav` — our own `Copy-Item`, which preserves `LastWriteTime` |

  ⇒ **On the direction this fact actually tested — do deleted files come
  BACK — the answer is no.** The mechanism adds files; none were added.
  ⚠️ **Note for the next reader: a byte-copy has the SAME metadata shape as a
  Cloud restore** (fresh `CreationTime`, old `LastWriteTime`). The 2026-08-11
  01:17 measurement's signature is only diagnostic when nobody has been copying
  saves. Diff against the expected-survivor list, not against the shape alone.

- ✅ **THE TWO-FILE SHORTFALL IS RESOLVED — it was autosave rotation, and a
  PREVIOUS SESSION HAD ALREADY RECORDED THE FILES.** The gap: the directory held
  **56** `.sav` before this session staged its copy; three were created that
  morning (two autosaves + `PT-15`), leaving **53** pre-existing against a
  **55** baseline — two short.
  * ⛔ **Owner asked and answered, 2026-08-11: *"I haven't deleted any old
    saves."*** The easy explanation is therefore REFUTED, not merely unchecked.
  * ⭐ **The archive had the missing names all along.** `archive/SESSION_LOG.md`
    (corun-batch-1 close, describing that sitting's window): *"the game wrote
    its own `Autosave Sol 351/356` … rolling autosaves of the staged lineage,
    in the owner's save list; **they rotate out on their own**."* Those two were
    in the 55. The new PT-15 colony's autosaving wrote `Autosave Sol 11` and
    `Autosave Sol 16` and rotated them out.
  * **It reconciles exactly:** 55 − 2 (rotated out) + 2 (new autosaves) + 1
    (`PT-15`) = **56**. And the cadence matches independently — the old pair is
    5 sols apart (351/356), the new pair is 5 sols apart (11/16): the same two
    rolling slots on the same interval.
  * ⇒ **Nothing is missing, nothing was restored, and no file left the
    directory except by the game's own rotation.**

- ⚠️ **THE METHODOLOGY LESSON STANDS ANYWAY, and it is the reason the above took
  archaeology instead of a diff: RECORD NAMES, NEVER A COUNT.** The 55 baseline
  was written down as a number. A number cannot survive one ordinary play
  session — two files rotating out and three being created nets to +1 and hides
  both movements. Had the baseline been a list, this would have been a
  five-second comparison instead of a search through an old session record.
  Every close-out from here lists names.

- ⇒ **DISPOSITION — ✅ RULED 2026-08-11 by the `corun-pt15` terminal audit: the
  caveat is RETIRED.** The audit re-checked the working rather than inheriting
  it: the untick commit stamp verified against git (`d03e4ef`, 09:43:57); the
  55−2+2+1=56 arithmetic re-derived; the corun-batch-1 archive quote read at
  its source (`archive/SESSION_LOG.md`, *"they rotate out on their own"*); and
  the close-out directory re-listed at audit time — **59 `.sav`, every name
  matching the sitting's list, none of the 14 strays present, no extensionless
  file, all three protected saves MD5-identical.** That listing is itself a
  **third data point**: the strays were still absent after the sitting's own
  15:09:29 launch, the second post-untick launch sampled. The WORKFLOW
  "never say gone" clause is retired by dated edit; close-outs may record
  "gone — verified by a NAMED listing". ⛔ Two lessons survive the retirement:
  baselines/listings are NAMES, never a count; and the restore *signature*
  (fresh `CreationTime`, old `LastWriteTime`) is not diagnostic once anyone has
  byte-copied a save. **A stray ever returning re-opens this fact and the
  clause with it.**

- ⚠️ **HOLD 2026-08-12 — the owner re-ticked Steam Cloud ON, deliberately and
  temporarily** (*"I need cloud save access for a hypothesis I am testing
  independantly, I will inform an agent when I turn it back off"*). The
  restore mechanism this fact measured is therefore ARMED again for the
  duration: agent-deleted saves may reappear at any later launch, and that is
  the mechanism working, owner-armed — **attribute, inventory for the
  post-untick cleanup, never file as a finding.** Close-outs revert to
  "deleted, listing verified" (WORKFLOW carries the matching dated hold).
  The 08-11 retirement itself is unaffected as a ruling; it resumes when the
  owner reports the re-untick and one post-untick listing verifies. ⛔ Until
  then no session may quote this fact's CLOSED status as license to say
  "gone".

- ✅✅ **HOLD LIFTED 2026-08-14 — the owner reported the re-untick ("steam
  cloud is off") and the post-untick cleanup ran in the same session. And the
  hold window sampled the mechanism a THIRD time, at full scale:** all **17**
  named 08-11 strays (the 14 + `U2STAGE`/`U2RT1`/`U2RT2`) were back on disk,
  every one carrying the exact restore signature — `CreationTime`
  2026-08-12 12:29:54–12:30:35, the first launch after the re-tick, with its
  original `LastWriteTime` (08-03/-04/-10/-11) preserved — **888 MB**. (The
  three 08-14 sitting fixtures `CSHOST`/`CSSWEEP`/`D13SWEEP` were NOT present:
  their cloud copies were deleted at that sitting's close-out re-deletion, so
  the store no longer carried them — consistent with the mechanism syncing the
  store, both directions.) **Deleted 2026-08-14 with the game closed
  (process checked), directory re-listed: 76 `.sav`, zero of the 20 historical
  stray names present, all held saves intact and untouched** (`CP60RT`,
  `Autosave Sol 311` + its two pre-copies, `CP15PT15`, `SPCONRT`). The 08-11
  retirement RESUMES; WORKFLOW's dated hold is lifted by dated edit.
  ⛔ **Prediction, with its falsifier, same as 08-11:** none of the 17 return
  at the next game launch. Any one returning means the cloud is not actually
  off for this title — re-open this fact and the WORKFLOW clause, and tell the
  owner which name came back.

- ⚠️ **2026-08-15 — THE 08-14 CLOSE-OUT LISTING WAS INCOMPLETE, and the gap is
  the extension, not the cloud** (`unattended-3` prompt 02, two launches). The
  08-14 bullet above says *"zero of the 20 historical stray names present"*.
  One IS present and has been all along: the **extensionless `U2RT1`**
  (54,311,838 bytes), the `EF-050` savename-verbatim artifact. It carries
  `CreationTime` **2026-08-12 12:29:59** — inside the 08-12 restore window —
  with `LastWriteTime` 2026-08-11 01:18:18, so **neither launch tonight created
  it and the falsifier did NOT fire**: nothing at all was created in the save
  directory during either launch (checked by creation-time window, both times).
  It survived the 08-14 sweep because that sweep enumerated `*.sav` and this
  file has no extension. ⇒ **A stray listing must enumerate the DIRECTORY, not
  the `.sav` glob** — `EF-050` guarantees this project can produce
  extensionless savegames, so the two facts have to be read together. Left in
  place and ROUTED rather than deleted here: it is unattended-2's artifact, not
  this leg's, and 54 MB of someone else's close-out is a call to make in
  daylight. The 08-11/08-14 retirement is otherwise unaffected — the mechanism
  this fact is about (deleted files coming BACK) was sampled twice more tonight
  and did not fire.
