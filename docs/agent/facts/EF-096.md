---
id: "EF-096"
seq: 96
summary: "⭐ MOD SANDBOX REACH for a tooling mod. `ModEnvBlacklist` is a READ barrier on `_G` lookups (`ModEnvMeta.__index` returns nil for listed keys) plus a write barrier; `ModMsgBlacklist` has nine messages. BLOCKED: `load`/`loadstring`/`dofile`/`require`/`io`, every `Async*` file op, `AccountStorage`, `CanUnlockAchievement`, `ConsoleExec`, `Msg`/`OnMsg` raw (mods get `safe_Msg`), the `Pops`/`PDX`/`Steam` prefixes. OPEN (checked by name): `SaveGame`/`LoadGame`/`DeleteGame`, `SetGameSpeed`, `CheatDefs` + every `Cheat*` leaf, `CheatsUsed`/`AreCheatsUsed`/`LogCheatUsed`, `config`, `ConsoleEnabled`/`ShowConsole`/`cls`, `CopyToClipboard`, `WriteScreenshot`, `LocalStorage`/`SaveLocalStorage`, `PlaceConstructionSite`, `PlaceCableLine`/`PlacePipeLine`/`PlacePassageLine`, `IsBuildableZoneQR`, `XTextEditor`, `SelectedObj`. HOOKABLE messages: `OnLuaError`, `ConsoleLine`, `GatherGameMetadata`. ⇒ a tool cannot compile code from a string at runtime, but can drive the game, save/load, clipboard and screenshot"
updated: "2026-09-24"
verified: null
lines: 79
derived_at: "game 1.1.0 build 24995074"
---
- **SOURCE, the mechanism.** `ModEnvMeta.__index(env, key)`: `if env_blacklist[key] then return end`, else
  `rawget(original_G, key)`, else assert "undefined global" (`CommonLua/Modding/Mod.lua:1560-1570`).
  `__newindex` drops writes to blacklisted keys and asserts on creating a NEW global unless `Loading`
  or the key is in `PersistableGlobals` (`:1572-1578`) — a mod file creates its namespace at load time
  (the TestKit's `SMRTest = rawget(_G, "SMRTest") or {...}` pattern, `00_TestCore.lua`).
- **SOURCE, the list.** `ModEnvBlacklist = { ... }` at `Mod.lua:1280-1416`, extended at `Autorun` by
  `ModBlacklistPrefixes` (`:1455-1471`): `Pops`, `PDX`, `WaitPops`, `AsyncPops`, `AsyncPdx`
  (`CommonLua/Libs/Paradox/ParadoxMods.lua:288-294`) and `Steam`, `OnSteam`, `AsyncSteam`
  (`CommonLua/Platforms/steam/SteamMods.lua:68-72`). ⛔ **No `Cheat` prefix is blacklisted.**
- **MEASURED, blocked names that matter to a tool** (each `grep -E "^\s+<name> = true"` on `:1280-1470`):
  `load`, `loadstring`, `loadfile`, `dofile`, `require`, `io` — ⇒ **no runtime compilation of code carried as
  a string**; a tool's actions must be real functions in a mod file (or typed into the console, which IS the
  mod env per `EF-094`). `AsyncFileToString`, `AsyncStringToFile`, `AsyncListFiles`, `AsyncFileFlush`,
  `FileToLuaValue`, `LoadLuaTableFromDisk`, `SaveLuaTableToDisk` — ⇒ **no file I/O**; persistence routes are
  `LocalStorage` + `SaveLocalStorage` (open; `CommonLua/Core/GlobalStorageTables.lua:337`) and the clipboard.
  `AccountStorage` and friends (`EF-094`). `ConsoleExec`. `Msg`, `OnMsg` (raw) — mods receive `safe_Msg`
  (`:1596-1600`) and their own `OnMsg` table; the only hooks denied are `ModMsgBlacklist`'s.
- **MEASURED, `ModMsgBlacklist`** (`Mod.lua:1443-1453`, nine entries): `PersistGatherPermanents`, `PersistLoad`,
  `PersistSave`, `ModBlacklistPrefixes`, `ModBlacklistGather`, `DebugDownloadExternalMods`,
  `DebugCopyExternalMods`, `PasswordChanged`, `UnableToUnlockAchievementReasons`. ⇒ `OnLuaError`
  (`Mod.lua:3019`, `Savegame.lua:1608` are vanilla listeners), `OnThreadError`, `ConsoleLine`
  (`CommonLua/UI/Dev/uiConsoleLog.lua:123`, listened to by the debugger at `luaDebuggerOutput.lua:85`) and
  `GatherGameMetadata` (`CommonLua/SavegameMetadata.lua:79`) are **all hookable by a mod**.
- **MEASURED, open names a tool needs** (same grep, each absent from the list). ⛔ **Open is a
  reachability verdict, not a call shape** — it says the blacklist does not hide the key, and nothing
  about whether the name is a global function, a class, a variable or a method (see the 2026-09-24
  correction): `SaveGame`, `LoadGame`,
  `DeleteGame`, `SaveAutosaveGame`, `ListSaveGames`, `SetGameSpeed`, `CheatDefs`, `CheatsUsed`,
  `AreCheatsUsed`, `LogCheatUsed`, `config`, `NetSyncEvent`, `FlushLogFile`, `OpenDialog`, `XDialog`,
  `XTextEditor`, `XEdit`, `CreateRealTimeThread`, `ConsoleEnabled`, `ConsolePrint`, `ShowConsole`,
  `ShowConsoleLog`, `cls`, `dlgConsoleLog`, `AddConsoleLog`, `org_print`, `print`, `CreatePrint`,
  `CopyToClipboard`, `GetClipboardText`, `SetClipboardText`, `terminal`, `WriteScreenshot`,
  `TakeScreenshot`, `SelectedObj`, `Selection`, `PlayFX`, `AddCustomOnScreenNotification`,
  `GetCursorWorldPos`, `GetTerrainCursor`, `LocalStorage`, `SaveLocalStorage`,
  `PlaceObj`, `PlaceObject`, `UICity`, `Cities`, `WorldToHex`, `HexToWorld`, `HexRotate`,
  `GetDomeAtPoint`, `GetObjectHexGrid`, `PlaceConstructionSite`, `PlaceCableLine`, `PlacePipeLine`,
  `PlacePassageLine`, `CheatCompleteAllConstructions`, `CheatCompleteAllWiresAndPipes`, `BuildingTemplates`.
- ⚠️ **Open ≠ working.** Open means the lookup returns the function; each route is still a source read until a
  leg runs it. `ConsolePrint` is a native export (`CommonLua/LuaExportedDocs/Global/LuaSharedLib.lua:7`), so
  whether it reaches Lua's `AddConsoleLog` (and thus `ConsoleLine`) is **unverified** — the fallback tap is
  wrapping `print`, which is open.
- **CORRECTION / omitted boundary, 2026-09-13 (SMRTK 01).**
  `CanUnlockAchievement = true` is explicitly blacklisted at `Mod.lua:1403`.
  The eligibility-reasons message is also blacklisted (above), so a mod cannot
  ask vanilla for the achievement-eligibility verdict. `GetAchievementFlags`
  returns unlocked/secret flags only (`Achievement.lua:39-41`), not that verdict.
  The toolkit reports `UNAVAILABLE:sandbox`; a clean taint read is not an OK
  eligibility read. Re-check: `rg -n 'CanUnlockAchievement = true'` on the
  installed `CommonLua/Modding/Mod.lua` → `1403: CanUnlockAchievement = true,`.
  No runtime achievement or account-storage mutation was used to test this.
- **CORRECTION 2026-09-14 (smrtk 99, verified in one command each on the installed `Mod.lua`).** The list spans
  **`:1280–1441`**, not 1280–1416 (grep the `ModEnvBlacklist = {` line and the next `}` at column 0 → 1280, 1441);
  `os = true` is in it at `:1438`, and it does not matter: `LuaModEnv` rawsets `env.os = { time = os.time }` at
  **`:1620`** before attaching the metatable, so `os.time()` works in a mod (03B cited 1618; it is 1620). Also:
  `OnMsg.UnableToUnlockAchievementReasons` has **five** handlers, not `EF-095`'s four —
  `CommonLua/Features/GameRules.lua:75` is a second game-rule handler.
- ⭐⭐ **CORRECTION 2026-09-24 — three "open" names are NOT globals in 1.1.x, and the whole list was
  re-derived by call shape.** An attended sitting handed the owner `MapGet(...)` as a console line and
  it errored at the keyboard: the open list had been read as "callable bare". **Measured on the archived
  1.1.1.405907 tree** (`SMR-Shared/SMR-SrcArchive/1.1.1.405907/Src`), all 62 names on this entry,
  both shapes each:
  `grep -rhoE "(^|[^:.A-Za-z_])NAME\(" <tree> --include=*.lua | wc -l` against
  `grep -rhoE ":NAME\(" <tree> --include=*.lua | wc -l`.
  **Exactly three are method-only**, each a field of `DefineClass.MapInterface` (declared
  `CommonLua/Core/map.lua:13`) and none a global: **`MapGet`** 0 bare / 150 method (`map.lua:14`),
  **`MapForEach`** 0 / 286 (`map.lua:19`), **`MapCount`** 0 / 14 (`map.lua:16`). ⚠️ `MapForEach`'s one
  bare hit is a comment (`CommonLua/Libs/Resources/ResInventory.lua:3`) — counted, then read. They are
  removed from the open list above; reach them as `<obj>:MapGet(…)`, never bare.
  ⚠️ **A stub run is not API evidence** — the stub defines the global it is testing, so it passes for a
  name that does not exist in the game. Count the call shape instead.
  ⛔ **Four kinds of name sit on this list and 0/0 does not mean missing:** global functions; classes
  (`XDialog`, `XTextEditor`, `XEdit` — each has a `DefineClass.<name>`, never called with `(`);
  global variables (`SelectedObj` 76 assignments, `ConsoleEnabled` 2, plus `config`, `terminal`,
  `LocalStorage`, `Cities`, `UICity`, `Selection`, `CheatDefs`, `dlgConsoleLog`, `CheatsUsed`); and
  native exports with no Lua definition (`WorldToHex` 291 bare, `HexToWorld` 177, `HexRotate` 50,
  `GetObjectHexGrid` 74, `FlushLogFile` 5). ⛔ The blacklist half of this entry is still
  `derived_at` 1.1.0 and was NOT re-measured here; only call shape was, on 1.1.1.405907.
- Found 2026-09-13 designing the SMR Tool Kit (`prompts/smrtk/`). Companions: `EF-095` (taint), `EF-097`
  (console), `EF-099` (construction route). Re-check: `python tools/doccheck.py --emit-fingerprint` (HOLDS ⇒ no re-read).
