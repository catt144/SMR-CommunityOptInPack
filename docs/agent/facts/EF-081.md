---
id: "EF-081"
seq: 81
summary: "⭐ MEASURED 2026-09-08 (owner rig, forced boots, log `archive/logs/forced110_*`): with the self-check override armed the pack reads **75 applied / 5 inactive**, and forcing `LowStorageWarning` produces a REAL runtime throw — `GetCommandCenterLifeSupportGrids` nil at `Fix_LowStorageWarning.lua:125`, called from `City:HourlyUpdate` (`City.lua:150`) ⇒ **hourly, on any colony, for any player**, with the rest of that tick's city update aborting with it. ⇒ **that module's self-disable is LOAD-BEARING** — the first evidence for the fail-safe design that is measured rather than argued. ⛔ The other 9 forced modules did NOT throw, but their triggers were never reached in ~57 s on a blank map — untested is NOT clean. ✅ Both refusals verified: the `test` content check (`AutomationLawCompensation`) and the 4 `DataPatch` latches stay inactive under a force"
updated: "2026-09-08"
verified: "2026-09-08"
lines: 78
derived_at: "game 1.1.0 build 24995074"
derivation_basis: "ed2b17df/f4dc62ab observations; archive/logs/forced110_Mars.exe-20260908-15.43.37-6a91a190.log:54 and unforced110_Mars.exe-20260908-15.57.09-6a91a190.log:54 record 1.1.0.403908; EF-075 records Steam identity"
---
- ⭐ **THE FIRST MEASUREMENT OF WHAT A SELF-DISABLED MODULE ACTUALLY DOES.**
  (Owner rig, 2026-09-08, two forced boots: `15.42.00` menu-only and
  `15.43.37` with a fresh 1.1.0 colony on `BlankBig_02`; the latter archived
  verbatim at `docs/archive/logs/forced110_Mars.exe-20260908-15.43.37-6a91a190.log`.)
  The self-check override (`SMRFixPack_Force` / `SMRFixPack.ForceApply`, pack
  commit `be4c99e`; armed by the Test Kit's `Code/97_ForceInactive.lua`) made
  the modules that had reported their targets missing install anyway.
- ⭐ **THE FINDING: `LowStorageWarning`'s SELF-DISABLE IS LOAD-BEARING, AND
  THIS IS THE FIRST EVIDENCE FOR THE FAIL-SAFE DESIGN THAT IS MEASURED RATHER
  THAN ARGUED.** Forced on, it does not merely fail to install — it throws in
  play, from the CITY'S HOURLY UPDATE:

      [LUA ERROR] Mod/SMR_CommunityFixPack/Code/Fix_LowStorageWarning.lua:125:
        attempt to call a nil value (global 'GetCommandCenterLifeSupportGrids')
        Fix_LowStorageWarning.lua(125): method GatheredResourcesOnHourlyUpdate
        Lua/City.lua(150):  method HourlyUpdate
        Lua/City.lua(542):  <>  ->  [C] procall  ->  cthreads.lua(20) Msg
        Lua/DayTime.lua(72)
      [mod] Error in mod Relaunched Fix Pack (id SMR_CommunityFixPack, v1.00-005)

  ⇒ every game hour, on ANY colony, for ANY player. And because the throw
  aborts the handler mid-body, **the remainder of that tick's
  `City:HourlyUpdate` silently does not run** — so a forced rig is not merely
  noisy, it is a colony with part of its hourly update failing. Had the
  self-check not caught this, 1.1.0 players would have had an hourly Lua error.
- ⭐ **THE RECURRENCE IS MEASURED, NOT INFERRED FROM THE STACK.** The archived
  log carries **6 occurrences** at `:278 :302 :325 :349 :372 :395`, at engine
  timestamps `0:00:56:892 · 0:01:26:894 · 0:01:56:891 · 0:02:26:892 ·
  0:02:56:892 · 0:03:26:892` — a flat **30-second period** across 2½ minutes
  of play, i.e. it fires on every tick of the hourly update for as long as the
  colony runs. ⛔ **A CORRECTION WORTH KEEPING AS METHOD:** the first archived
  copy of this log was taken while the game was STILL RUNNING and captured only
  the first throw, which read as a one-off. A log copied mid-session is a
  PARTIAL log — re-copy after the process exits before drawing a rate from it.
- ⛔ **DO NOT READ THE OTHER NINE AS SAFE.** Only one of the ten forced modules
  threw, but the session was ~57 seconds of game time on a blank map: the
  triggers for the rest (asteroids, a rocket landed at Earth, meteor storms,
  rain, tourists, drone unreachables) were never reached. **Untested is not
  clean** — the absence is a sampling gap, not a result.
- ⚠️ One forced module CHANGED GAME STATE rather than throwing:
  `MeteorFrequency: one-shot heal — persisted Meteors thread was not created;
  restarting onto vanilla's body (latch false -> 1.0.5)`.
- ✅ **BOTH OF THE OVERRIDE'S DELIBERATE REFUSALS VERIFIED AT RUNTIME.** Both
  boots read **75 applied / 5 inactive**, and the 5 are exactly the ones the
  tool refuses to force: `AutomationLawCompensation` (a `test` CONTENT check —
  a healthy decline, never named by the dialog, and forcing it would CREATE
  the asymmetry C39 exists to remove) plus the four `DataPatch` latches
  (`SaintBlessing`, `DustSicknessDamage`, `LastTransmissionStorage`,
  `IndependenceTerraforming`), whose pass had already run and found no target.
  10 modules were forced, over **13** override lines — `LowStorageWarning`
  fails three checks and `VacuumWalks` two, so override lines count CHECKS,
  not modules.
- ⭐ **THE ARITHMETIC CONFIRMS THE PREDICTED UNFORCED COUNT WITHOUT AN UNFORCED
  BOOT.** After `2efbcf6` an unforced 1.1.0 boot should read **15 inactive /
  12 named** (`EF-078` measured 13 / 11; the F113 shape gate adds
  `LanderCargoRatchet` to both, the F112 content check adds
  `AutomationLawCompensation` to the inactive count only). 15 would-be-inactive
  minus the 10 forceable = the 5 observed. ⛔ That delta is OURS, not the
  game's — reconcile against it before calling a count change a game change.
  ✅ **AND IT IS NOW MEASURED, NOT ONLY DERIVED** (unforced boot 15.57.09,
  archived `archive/logs/unforced110_*`): **65 applied / 15 inactive**, and
  `update report: 12 fix(es) deactivated over a game-code change`. The split
  lands exactly where the two gate KINDS predict — `LanderCargoRatchet` is
  inactive AND named (F113, a `{class, method}` shape gate ⇒ `update_suspect`),
  `AutomationLawCompensation` is inactive and NOT named (F112, a `test` content
  gate ⇒ never `update_suspect`). Two sessions predicted 12 independently and
  the log agreed. Zero errors on that boot; the dialog is back, confirming the
  override is fully disarmed.
- ⛔ **A FORCED RIG IS INVALID FOR ANY A/B**, `F114` included: forcing changes
  what the pack does, so a forced boot cannot answer "does the shipped pack
  cause this?". Disarm by commenting `"Code/97_ForceInactive.lua"` out of the
  Test Kit's `metadata.lua`. Note also that the hourly `City:HourlyUpdate`
  abort above is a confound for ANY observation made on a forced colony.
- ✅ Both boots also confirm the dialog suppression is honest: `update report:
  2 fix(es) deactivated…` is written to the log FIRST, and only then
  `update dialog suppressed on this rig (SMRFixPack_NoUpdateDialog)`.
  Suppressing the popup never suppresses the record.
- Scanned with `tools/logscan.py`, written this sitting for exactly this job.
