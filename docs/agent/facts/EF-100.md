---
id: "EF-100"
seq: 100
summary: "⭐ THE REMASTER MOVED AMBIENT WEATHER FROM PARTICLES TO PHYSICALLY-BASED ATMOSPHERE, AND THE SKY IS NOW THE WHOLE EFFECT. Every ambient weather particle FX ships `Disabled = true, group = \"_inactive\"` (DustStorm ×2, GreatStorm ×2, ElectrostaticStorm ×3, ColdWave ×1, MysteryDream ×1 = 9), each replaced 1:1 by a `phys_sky` LightmodelFeaturePreset (`Atmoshpere_DustStorm`, `Atmoshpere_GreatDustStorm`, `Atmosphere_ColdWave`, `Atmoshpere_Rain`, `Dreamers`). Only electrostatic LIGHTNING survives as particles (10 live entries); all storm SOUNDS stay live, so a storm is audible with no dust on every platform. The non-atmosphere delta is ≈12% exposure + a warm gamma ⇒ a storm whose `phys_sky` does not render looks like nothing happened. Already this way at 1.0.7 — not a 1.1.0 regression"
updated: "2026-09-13"
verified: null
lines: 49
derived_at: "game 1.1.0 build 24995074"
---
- **SOURCE, named trees:** live install `A:\SteamLibrary\steamapps\common\Project Spark\ModTools\Src\`
  and `…\Packs\`; the 1.0.7 control is `B:\Dev\SMR\SMR-Shared\SMR-SrcArchive\1.0.7.396349\Src\`.
- **SOURCE, a weather event has exactly two visual channels.** (1) An ActionFX class fired by
  `PlayStartDustStromFX` (`Lua/DustStorm.lua:203-227`) —
  `PlayFX{actionFXClass = "DustStorm"/"GreatStorm"/"ElectrostaticStorm", actionFXMoment = "start"}`.
  (2) A lightmodel-list swap: `OnMsg.DustStorm` / `OnMsg.ColdWave` → `DisasterEventLightmodelHandler`
  → `SetDisasterLightmodelList` (`Lua/Lightmodel.lua:107-134`), the list chosen by
  `GetDisasterLightmodelList` (`Lua/MapSettings.lua:244-260`). There is no third channel.
- **SOURCE, channel 1 is parked.** Every AMBIENT weather particle FX carries
  `Disabled = true, group = "_inactive"` in `Data/FXPreset/ActionFXParticles.lua`:
  **DustStorm ×2, GreatStorm ×2, ElectrostaticStorm ×3** (ambient dust + a camera FX),
  **ColdWave ×1, MysteryDream ×1** — **9** in all. `Disabled` is honoured at registration
  (`CommonLua/Classes/ActionFX.lua:1044`); the property's own help (`:917`) reads
  "Disabled FX are not played". The particle systems themselves still ship — all four
  referenced presets are present under `Data/ParticleSystemPreset/` (10 `DustStorm_*` files).
- **MEASURED, `_inactive` is a curated house parking lot, not a storm quirk:** it holds
  **141 of 2,672** top-level ActionFX entries, spanning Spawn ×22, Select ×21+9,
  ConstructionCursor ×17+6, Working ×10, Moving ×8, rocket land/launch, building placement,
  Clean, Scan, even a main-menu hover ⇒ retiring an FX here is routine housekeeping.
  ⚠️ **Counting gotcha:** a naive `grep -c "PlaceObj('ActionFX"` returns **3,788** because
  `ActionFXEndRule` objects are nested INSIDE other entries (**1,116** of them).
  Anchor at column 0 (`^PlaceObj('ActionFX`) for the real top-level total; 2,672 + 1,116 = 3,788.
- **SOURCE, the 1:1 replacement.** Each retired ambient effect has a `phys_sky`
  `LightmodelFeaturePreset` in its place (`Data/LightmodelFeaturePreset.lua`):
  `Atmoshpere_DustStorm` and `Atmoshpere_GreatDustStorm` (both `atmosphere_advanced = true`,
  driving Rayleigh/Mie scattering coefficients), `Atmosphere_ColdWave`, `Atmoshpere_Rain`,
  plus the `Dreamers` lightmodel group for MysteryDream ⇒ the remaster **moved** weather from
  particles to physically-based atmosphere; it did not delete it. (Preset ids are spelled
  "Atmoshpere" in the shipped data — the typo is theirs, grep for both spellings.)
- **SOURCE, what survives as particles:** **10** live storm particle entries, every one of them
  ElectrostaticStorm LIGHTNING — `ElStorm_Lightning_01..03` / `ElStorm_Clouds` on moments
  `hit-moment1..4`, plus `ElectrostaticStormArea` — i.e. discrete strikes, which a scattering
  model cannot express. All storm SOUNDS remain live (`ActionFXSound.lua`: wind howl, wind sand,
  thunder, disaster stingers) ⇒ **a dust storm is audible but dustless on every platform.**
- **SOURCE, the non-atmosphere delta is cosmetically tiny.** `TheMartian_Noon` → `DustStorm_Noon`
  (`Data/LightmodelPreset.lua`) differs in six properties only: `preset_phys_sky`,
  `exposure` 32→16, an added warm `gamma` (≈RGB 141,116,104 against the neutral 128,128,128
  default), `shadow` 900→800, `construction_exposure`, `wind`. `exposure` is in **1/100 EV**
  (`CommonLua/Classes/Lightmodel.lua:673`), so 32→16 is ≈**12%** brightness, and autoexposure
  (`ae_key_bias` identical in both) compensates further ⇒ **the sky IS the effect**; a storm
  whose `phys_sky` does not render is indistinguishable from no storm.
- **CONTROL, not a 1.1.0 regression:** the same **seven** storm entries are already
  `Disabled` / `_inactive` in the archived 1.0.7 tree
  (`…\1.0.7.396349\Src\Data\FXPreset\ActionFXParticles.lua`), with the electrostatic lightning
  already live beside them. ⛔ Falsifier for this fact: any live (non-`Disabled`) ambient weather
  particle FX, or a weather visual that survives neutralising `preset_phys_sky`.
- ⇒ **Test route:** trigger with the leaf cheat globals `CheatDustStorm` / `CheatColdWave` and a
  cursor position — `def:run()` taints for the disaster presets (`EF-098`). Companions:
  `EF-084` (dust-devil `HasDustStorm` gates), `EF-101` (reading the atmosphere shaders).
