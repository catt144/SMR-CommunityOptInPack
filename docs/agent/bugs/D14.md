---
id: "D14"
seq: 10
row: 10
title: "Train hub dev build: save waiter defect and siding departure deadlock"
status: "open"
status_source: "tag"
priority: "dsgn"
evidence: "high"
row_status: "open — save/departure fixes pass native controls; wider audit residue remains"
updated: "2026-09-25"
copies: []
---
### D14 — Train hub dev build audit `[open — wider audit residue; save and siding fixes verified in owner fixture]`

Dev-only content module, not a shipping module. Authority: owner 2026-09-23,
the train audit brief; confirmed defects in the dev mods may be fixed by the active sitting.

**(a) Global dwell hook displaces the native waiter's save permanent.** On archived
1.1.1.405907, `CommonLua/Core/cthreads.lua:466-478` registers the current global
`WaitWakeup` as `cthread.WaitWakeup`. The old hub hook replaced that global but
delegated to its captured native C function. Suspended native wait frames therefore
lost that mapping. `DroneControl:UpdateDeficits` (`Lua/Buildings/DroneControl.lua:129`)
and `MarkFlight` (`Lua/Flight.lua:835`) both wait there; either can be the first
persist traversal to encounter it. Neither stack identifies the owning defect.
The save error is measured in the owner's logs; the mechanism is reproduced by
the archived collector in `tests/dwell_smoke.py`. The later native guard controls
are recorded below; the initial source-only audit did not run the game.

**Withdrawn repair `3a0faff`:** stop replacing the global. Copy the two train commands from archived
1.1.1.405907, changing only the hub's timeout; foreign stations call the captured
original. No new persisted key or changed key. The command copies use save-policy
layer 2; native saves and removal behaviour have not been tested here. Existing
placed-hub removal remains unsupported under FIX_POLICY §0.

The withdrawn repair passed `dwell_smoke.py` while the `d73d701` control failed
its native-permanent assertion. That test did not cover legacy serialization.
`move_smoke.py` checks elapsed boarding time, minimum wait, foreign station/thread
and live tuning on the 1.1.1 source. A replacement save repair must cover native
old→new→save→reload; a fresh-process save logging the same error falsifies it.

**Owner follow-up, 2026-09-24:** old autosave and known-good templates assert in
`luaSPersist.cpp:1272`, then crash after Ignore All. `3a0faff` also changed the
existing `cthread.WaitWakeup` mapping from Lua closure to C function; the test
missed old-save compatibility. Restored the exact legacy dwell section from
`d73d701` as containment. Its original save defect remains, and `dwell_smoke.py`
was RED again after rollback. The owner then loaded the version-15 template and `Autosave Sol 31(3)`
without assertions (`10.45.34` and `10.49.56-6aad2d75.log`); both processes exited
normally. The later autosaves still log the original C-function persist error.
Input saves were backed up, not rewritten or certified repaired. The report's
§8 preserves logs, backup locations, the limited type-mapping proof and migration
obligations. Dev version alone cannot distinguish the old/new waiter mapping.

**Snapshot guard `102f5f0`, native controls passed 2026-09-24:** the owner proposed removing our change
at save time and restoring it at load. The implemented narrower guard chains
PersistGame, restores the native waiter only around its snapshot, and restores
the runtime wrapper on success, returned error or thrown error. New metadata
`SMROptIn_hub_native_waiter=1` chooses the native mapping before EngineLoadGame;
unmarked existing hub saves retain their legacy Lua mapping. The wrapper body
itself is unchanged. Saves without a hub mod record select the native mapping.
Pre-wrapper hub saves are an older ambiguous case, not guessed by mod version.
The updated dwell smoke tests archived snapshot/collector bodies and rejects
broken snapshot and legacy-load mappings. The owner's 128× run loaded the legacy
save, generated/reloaded an autosave, then made a manual save and loaded it after
a full restart. The closed `12.06.00-6aad2d75.log` has no persist/load/crash failure
markers; `12.35.05`'s flushed prefix confirms the fresh-process load. The manual
save's marker and game time match the restart readout. This verifies the reported
save defect's repair in that fixture; ck215 is consumed. Autosave-specific cold
reload was not separately run, nor were ambiguous pre-wrapper fixtures or the
broader ship matrix. The existing ArtSpec startup error and Braze DNS failures
remain. Report §§11–12 hold exact evidence and limits. D14 stays open for its
other findings. This does not unload buildings or erase
saved command frames.

**(b) Cross-map graph ownership — design decision open.** `HubTrackGraph` follows
any reciprocal TrackTunnelBase.linked_obj; `register_remote_stations` then adds
the hub controller. `30_TrainHubDrones.lua`'s stock FlightGoto receives XY on the
drone's current map. A shaft can therefore enroll unreachable stations and
schedule repair visuals on the wrong map. The scope of the owner's network-wide
rule needs a map ruling, OI-27; no restriction was silently added. Control: a hub
connected through a shaft, with a far-map station, must be compared against the
same-map tunnel case. No such fixture ran in this audit.

**(c) Nanite completion early return — conditional source risk.** Archived
1.1.1.405907 `Lua/Buildings/ConstructionSite.lua:2673-2675` can return without
completing a queued leader. The hub's `complete_job` pays first, calls Complete,
and returns done unconditionally; discovery can see the still-live leader again.
Control: put a real repair leader into the vanilla nanite completion path and
check payment once, with the ordinary-completion case beside it. Not established
as the owner's incident and not fixed without that reachability check.

**(d) Split after discovery — conditional source risk.** The 1.1.1 track split
body in `TrackElement.lua` retargets repair sites to their new tracks. The hub's
pending job caches its original track and `job_for_site` suppresses rediscovery
of that leader. A waiting job may stay gated on a stale track reference. Control:
discover a short-stock job, split/salvage the track while its site survives on
the connected side, replenish stock, and compare with an unsplit track. Native
reachability/result remain open; no claim that this happened in the stalled save.

**(e) Imported EntitySpec load path.** The 2026-09-23 retail logs contain the
ArtSpecEditor.lua:573 nil EntitySpecPathToEntity error; the stack's `self` is
`EntitySpec 'SMROptInTrainHub6'`. That method body is unchanged between archived
1.1.0 and 1.1.1; the definition is in SceneImport.lua, unavailable on retail.
It is vanilla's editor/mod-load path reached by our imported source data, not a
train/repair command. The dev mod now skips this editor post-load for its three
entity specs only when the editor path helper is absent; other specs and editor
loads delegate to vanilla. The focused smoke passed on 2026-09-25. In the
owner's `Mars.exe-20260925-16.15.16-6aad2d75.log`, literal `Select-String`
finds `[TrainHubDev]` and no `EntitySpecPathToEntity`; the retail recurrence
is absent in that session. The owner's MarsDebug Mod Editor save wrote
`SourceData/ArtSpec-mod.lua` (`16.15.52` log:286), then reloaded without that
error (:370); the generated file is clean in Git afterward. This covers the
normal mod-editor save/reload. The EntitySpec is generated source data, not a
separate item to reopen in the Mod Editor; no mesh re-import is owed here.

**(f) Siding departure deadlock — fixed; owner confirms trains unstuck.** Native
reads on hub 6430 found opposite parked pairs waiting to depart through each
other's connector: slots 1↔2 and 3↔4. All connected tracks could run, route
membership held, command threads were live and both crossing records were clear.
The exit-contact guard from `2606719` predates the owner's later off-line siding
design (`99a8cd9`; spec §9). It still treated a siding reservation as occupying
the running rail. Earlier records had noted mutually blocked departures as a
limitation; the later siding implementation had not updated that guard.

`HubExitClear` now exempts only a train parked at this hub with the matching
arrival slot and no crossing ownership. Unknown, incoming and moving reservations
still block; native outgoing-track exclusion and crossing serialization remain.
No routing/queue policy or persisted name changed. The paired-siding regression
in `move_smoke.py` passes and fails against `2035dbb`'s guard. After restarting
with the correction the owner reported, "They are now unstuck." Native visual
clearance was not explicitly confirmed; this is the observed colony, not every
layout. Details, members and controls are in the report's §9.

**(g) Launch accounting deletes another hub's live entries — reproduced offline.**
At `cc3d6f1`, `20_TrainHub.lua:2226`'s `rising_count(self)` shares a weak-keyed
registry across hubs. Its `else` deletes every entry that does not belong to
`self`, including valid Wasps still launching for another hub. That hub's next
count can therefore omit its in-flight launches and authorize replacements.
L6 executed the exact extracted Lua: same-hub live and dead/released controls
pass, but counting hub A removes hub B's live entry. A memory-only change that
cleans only released/invalid entries passes the paired-hub control. Production
code is unchanged; no native excess-fleet magnitude is claimed. Next active hub
build owns the fix and a regression that alternates hub ticks while both have
pending launches, retains the other hub's entries and respects each ceiling.
Evidence: `docs/archive/drones_l6_20260925/audit.json`, command-bearing actual-source
reproducer alongside it. This is dev fleet accounting, not a save-corruption finding.

**(h) Repair notification permanent missing on cold load — native historical warning.**
Recovered `Mars.exe-20260924-17.07.20-6aad2d75.log:225` reports
`Unpersist missing permanent: Preset:NotificationPreset.SMROptInTrackRepair`,
falling back to `BreakthroughDiscovered`. The process later completes repairs
and exits normally; the warning alone establishes no crash or corrupted save.
Current `ensure_hub_notification` is called by dispatch, CityStart, LoadGame
and InitHubTrackWork (`20_TrainHub.lua:2435,2453,3208,3217,3225`), not a proven
pre-unpersist registration. Next active hub build reproduces a fresh-process
load of a save with the repair notification active, with an ordinary preset
control; check the permanent and displayed notification before choosing a fix.
Current-build recurrence is unverified. The full original log is late-archived
under `docs/archive/drones_l6_20260925/` with its hash and recovery provenance.

**(i) Flight table fails on first strict-global load — repair pending native proof.**
The owner's build 1.1.1.405907
`MarsDebug.exe-20260925-16.15.52-6aad2de6.log:98-99` raises undefined
`SMROptInHubFlight` at `30_TrainHubDrones.lua:57`: the reload cleanup reads
the global before its first assignment. A later reload in the same log does
not raise it because the table then exists. `flight_smoke.py` reproduced the
cold failure with a strict-global control, then passed after `cb14c13` used
`rawget(_G, "SMROptInHubFlight")`. The source-pinned Blender clearance receipt
was regenerated; its measured routes and clearances match the prior receipt.
The debug session's absence of the earlier power-inheritance error and map
assert does not establish a complete hub run while flight code failed to load.
Control: a fresh MarsDebug load with `[TrainHubDev]` present, no undefined
flight-global error, a map and train run. The owner's retail
`Mars.exe-20260925-16.24.10-6aad2d75.log` on build 1.1.1.405907 loads the
dev mod and `BlankBig_01`; literal `Select-String` finds `[TrainHubDev]` and
no `[LUA ERROR]`, old inheritance error, map assert, art-spec error or flight
global error. In that run the owner directly saw the train dwell, the hub's
power display and no disconnected-grid warning while isolated. This is the
retail behavior check; the debug assert still needs a fresh MarsDebug colony
run after `cb14c13`.

Terminal drones audit and routed correction work:
`docs/agent/reports/drones_chain/L6_QA_20260925.md`.

Audit evidence and remaining source-coverage limits:
`docs/agent/reports/TRAIN_HUB_AUDIT_111_20260923.md`. Every item above is dev-only;
none upgrades a module to tested or claims 1.1.1 is clean.
