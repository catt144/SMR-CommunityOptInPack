---
id: "D12"
seq: 9
row: 9
title: "Homeless strand in specialist domes; emigration ties never move them"
status: "speced"
status_source: "tag"
priority: "dsgn"
evidence: "med"
row_status: "**speced — no status beyond `speced` is claimed until PT-62 completes. BUILT 2026-08-02 (chain prompt 10) as `Opt_NoHomeless`; PT-62 PARTLY RUN (core seam + suite + exemption established; P4/P6/P12/P13 owed); ⭐ ADJUDICATED 2026-08-03 (chain-12 QA, job 9): the build STANDS — all five live-review decisions upheld, the veto's D07-independence verified from code.** ⚠️ **CODE CHANGED AGAIN 2026-08-03 AFTER that adjudication (owner-approved): the FREE-WORK DOOR.** The symmetric veto built on 2026-08-02 refused `need_work` — which is exactly the set vanilla moves toward an open job (`better_work`, `Colonist.lua:2679`), so a flagged dome could never be STAFFED from outside and its services would go dark unreplaced. Derived from Src, never observed. A flagged dome now neither pushes out nor refuses anyone while it holds a free workplace they could take, applied on BOTH sides because an entry-only door re-creates the PT-62 loop in miniature; `count_movable` and the rollover carry it, and the stale narrow-reading registry title was corrected in the same pass. **The re-run gains P14** (does an open slot in a flagged dome FILL) — the one unmeasured assumption in the build, and `movable 0` on a recruiting dome is the door WORKING, not a miss Premise RE-DERIVED first: the tie is byte-verbatim in pinned Src and the [S36] \"1.0 fixed homelessness\" claim is FALSE for this half. The open question is DECIDED — ⚠️ row re-synced 2026-08-03 (it had frozen the superseded first design; caught by doccheck's first baseline run): the rule is vanilla's **`need_work` predicate, NEITHER specced option** (\"narrow\" was built first and falsified live the same evening — entry §4); `C40`, filed en route, supplied the third reason (entry)"
updated: "2026-08-03"
copies: []
donor_seq: 109
donor_row: 74
from: "SMR-BugFixPack docs/agent/bugs/D12.md @ 0efb87e, moved 2026-08-12"
---
### D12 — Homeless strand in specialist domes; emigration ties never move them  `[SPECED 2026-07-30, user-approved — ⭐⭐ **BUILD UNHELD 2026-08-01** (Tier 1 verified `c6180ad`; Tier 2 verified by PT-58). ✅ **BUILT 2026-08-02 (chain prompt 10) as `Opt_NoHomeless` — own module, Opt_ResidencyControl as donor PATTERN only, zero shared gate. ⚠️ PARTLY RUN: the leg is `PLAYTEST_CHECKLIST.md` PT-62 (core seam + suite + exemption established; P4/P6/P12/P13 owed) and no status beyond `speced` is claimed. The open question is DECIDED — ⚠️ tag corrected 2026-08-03 (chain-12 QA): the rule is vanilla's `need_work` predicate, NEITHER specced option ("narrow" was built first and falsified live the same evening — see §4). ⭐ ADJUDICATED 2026-08-03 (chain-12 QA, job 9): the build STANDS — all five contested decisions upheld, the veto's D07-independence claim verified independently from code (subject sets disjoint by construction). The D07-unwind remains the design RATIONALE and is not asserted as verified]`

---

## ⭐ PRE-BUILD RE-DERIVATION (chain prompt 10, 2026-08-02) — read this before the spec below

The build session re-derived the premise from Src before writing a line, because
the project has been wrong in both directions in one week about *routes* whose
individual citations were all correct. Results:

**1 · The premise HOLDS, byte-verbatim.** The tie gate is exactly as this entry
describes it in the pinned 1.0.7.396349 Src: `Colonist.lua:2675` `if new_eval >=
eval then`, the homeless-permitting comment at `:2676`, the strict-improvement
requirement at `:2680-2681`, the quarantine early-out at `:2632-2635`, and the
declaration at `:2581`. **The routed [S36] claim that the devs "squashed
homelessness" with 1.0 is FALSE for this half** — third-hand, hotfix-1.0.3-era,
and nothing in the current source touches the tie. Project rule satisfied:
*"fixed in Relaunched" only from current Src*.

**2 · ⚠️ ONE CITATION IN THIS ENTRY IS OFF BY A HOP, and the substance survives.**
The Mechanism section says `FindEmigrationDome` is *"called every heavy update
from `Colonist.lua:1894-1898`"*. It is not called there. **Its only caller in Src
is `Colonist.lua:1603`, inside `Colonist:TryToEmigrate`** — which the heavy-update
block calls at `:1898`, and *also* calls off-cycle at `:1900-1902` when
`user_forced_dome` is set. So "no new scheduling" is correct and the seam is
correct; the line reference named the caller's caller. Corrected here rather than
silently, per chain rule 4b.

**3 · ⭐ THE ROUTED PLAYER HYPOTHESIS WAS RIGHT — and it decided the open
question rather than restating the premise.** The brief carried a Reddit player's
proposed cause for the same symptom (*"homes at their capacity would regularly
fluctuate because of staffing in the law spire"*) with the instruction *check it,
do not adopt it*, and the warning that if capacity is recomputed from live
staffing then **D12's premise needs restating before a line of code is written**.
It is: `Policy_ExtraResidentialSpace` grants +3 `Residence.capacity` gated on the
Ministry of Culture's live `working` flag, and a withdrawal EVICTS residents
colony-wide. Filed as **`C40`** with the five-link route.

**What that does and does not do to D12:**

* It does **NOT** overturn the premise. C40 is a second, independent **producer**
  of homeless colonists; the tie rule is what **strands** them once they exist.
  Both a shortage-produced and a churn-produced homeless colonist are stranded by
  the same gate, and D12 is a **mobility** fix, not a shortage fix.
* It **DOES** bound what D12 may ever be said to fix. The reported *flickering*
  is a churn symptom and **D12 does not address it**. No pack document may claim
  D12 fixes "homelessness" — it fixes colonists **stranded in a dome that cannot
  house them**. `MOD_DESCRIPTION.md` wording is bound by this.
* It **DOES** decide the open question, below.

**4 · THE OPEN QUESTION IS DECIDED, AND THE ANSWER IS NEITHER OPTION IT
OFFERED.** The entry asked: ALL homeless, or only colonists the dome can never
house? ⛔ Both are wrong, and it took the OWNER'S ACCOUNT OF HOW THE FEATURE IS
USED to settle it (2026-08-02) — no amount of source reading would have.

> *"This is a player control toggle specifically because no automated system can
> fully tell. Generally when people get to the size they want a child / senior
> dome, they set up 2 domes out of the way … one to either be a retirement dome
> + services or nursery + services … It keeps the minimal amount of housing to
> staff those services, because you expect your retirement dome and the nursery
> to eventually be filled with non-workforce people. So you also need to keep
> homeless out of both, because if either gets filled with homeless they cannot
> do their job any more, because migration turns off."*

* ⛔ **NARROW fails outright.** "Minimal housing to staff the services" means the
  dedicated dome HAS ordinary residences by design, so everyone in it is
  housable-in-principle and the structural test never fires. It was built first,
  and the owner's own colony disproved it in one reading: **nursery dome
  `ordinary 0` → fires; retirement dome `ordinary 5` → never fires.** One of the
  two domes the feature exists for was unprotected.
* ⛔ **BROAD fails too, and for a reason source cannot show:** a homeless Senior
  in a retirement dome is **information**, not a defect — *"it signals to them
  that they need to build more retirement homes"*. Pushing them out deletes the
  signal and hides the shortage.
* ⛔ **And "push the non-workforce" is the trap inside BROAD**, caught by the
  owner before it was written: **"outside the workforce" is a status Children
  and Seniors carry BY DEFINITION** — the game prints `T(4364, "Outside the
  workforce (Child)")` / `T(4365, "… (Senior)")` at `Colonist.lua:3146-3148` and
  distinguishes it from `T(4366, "Unemployed")` three lines later. Targeting it
  would sweep up the exact cohorts these domes hold.

⭐ **THE RULE, IN VANILLA'S OWN PREDICATE:** a flagged dome pushes out a homeless
colonist **iff `need_work` is true for them** —
`can_work and not IsValid(workplace) and not user_forced_workplace`,
`Colonist.lua:2623-2624`, computed by the very function this module wraps, two
lines above the tie it breaks. `CanWork()` (`Colonist.lua:2114-2126`) is false
for Children, for Seniors unless `g_SeniorsCanWork`, for Tourists, and for the
Earthsick / StressedOut / UnableToWork / unfit. In the game's own words:
**move the UNEMPLOYED; never the employed, never anyone outside the workforce.**

That maps onto the described pattern exactly: the service staff are EMPLOYED and
stay, so the dome keeps working; homeless Seniors and Children stay, so the
build-more-housing signal survives; the graduates and leftovers — workforce-age,
jobless, housed by nothing — are what is left, and they are the pile-up that
drives the dome over `IsOverpopulated` and switches its migration off.

⚠️ **Deliberately NOT duplicated:** relocating a homeless Senior or Child toward
cohort housing elsewhere is **D07 `Opt_CohortHousing`'s** job and it already does
it. The two divide cleanly — D07 moves the cohort toward its housing, D12 moves
the workforce-age unemployed out of a dedicated dome.

⚠️ **What it costs versus narrow:** narrow was immune to the C40 capacity churn.
The employed exemption recovers most of that on a better key (churn-evicted
service staff are employed, so exempt). Residual: an unemployed workforce-age
resident of a flagged dome, transiently evicted, moved out for good over an
outage that ends by itself. Recorded, not guarded — the guard needs per-colonist
dwell state and the exposure does not justify it.

**4c · A RECORDED LIMIT OF THE RULE (owner observation, 2026-08-02).** Domes
allow "use Passages for work", so a colonist can live in one dome and work in
another — which is why a flagged dome's homeless list contains `job yes` rows.
Two consequences:

* **There is no "works somewhere unreachable" case to separate**, so the
  in-dome-versus-commute question the build first raised is void: vanilla only
  assigns a workplace reachable from the colonist's own dome, so *employed
  anywhere* and *employed reachably from here* are the same set. Moving an
  employed colonist would risk severing a commute the game already validated.
* ⚠️ **But `IsOverpopulated` counts `#labels.Homeless` RAW** (`Dome.lua:1026-1035`)
  — employment does not enter it. So a dome whose homeless are mostly its own
  **staff** can still latch and switch its migration off, and this policy will
  not clear it. **That is consistent, not broken:** it is the same shape as the
  homeless-Senior signal — a dome full of homeless workers is telling the player
  their staff have nowhere to live, and clearing it would hide a housing
  shortage. Recorded as a limit rather than guarded, and **PT-62 must not read
  a failure to clear `overpopulated` as a P6 miss without first checking how
  many of the remaining homeless are employed.**

**4b · THE ROW STATES ITS OWN CONSEQUENCE (owner, same day).** *"This could
easily be mistaken for just hitting a button to get rid of homeless … not all
players will look at a tooltip, they will just be confused why their retirement
dome isn't working."* So the row does not rely on its rollover:
**title `Nursery / Retirement Dome` in both states** (the recognition words lead,
per *"if I am a new player working on a retirement home or a nursery that jumps
out at me, jobseeker doesn't"*), with the state and the live count on the right
via `TitleRight` — `2 moving out` / `off (2 would move)`. On a retirement dome
full of homeless Seniors it reads **`0 would move`**, which answers that exact
confusion in place, before the click. ⛔ **An icon-polarity defect was fixed in
the same pass:** the first build rendered the OFF state — which is VANILLA — in
red, so a player who had never touched the row saw an alarm and would press it to
clear it. It now mirrors D03's proven pairing (permissive green/on, restriction
yellow/limit).

**5 · One deliberate departure from the spec text, recorded.** The Mechanism
section says the destination is *"the nearest reachable community that does NOT
carry the flag and that `CanAcceptNewColonists()`"*. The build adds a third
filter — **the destination must contain housing that is `IsSuitable` for this
colonist** — because sending a grown Youth from one nursery-only dome to another
achieves nothing. It is a suitability test, **not** a free-space test:
free non-cohort slots colony-wide were **zero** in the origin scenario, so
requiring free space would have made the module inert in the exact case it was
built for (the shipped eval permits the no-free-space move for the same reason,
`:2676`). Among qualifying destinations, one that can house them **now** is
preferred, then the nearest. `community.overpopulated` is **not** filtered
(D07 does filter it) — draining an overpopulated dome is the point.

---

## ⭐ OWNER DISPOSITION 2026-08-03 — "a win that needs a description and an FAQ", and NOT a launch blocker

Owner, at the end of the PT-62 sitting: *"it seems to be working much better …
I think its in a much better spot. Mark it for probably needs more testing, but
I do not want it to hold up the bug fixes since its an opt module."*

**Three things that follow, and they bind later sessions:**

1. ⛔ **D12 IS NOT A RELEASE GATE.** It is `optional = true`, off by default,
   and a player who never enables it is untouched by every open question below.
   **No release step waits on it.** A future session that finds P12/P13/P14
   still owed must not read that as "the pack is not ready" — the pack is not
   what is unfinished.
2. **Status stays `speced`.** The entry's own rule holds: nothing beyond
   `speced` is claimed until PT-62 completes, and it has not. P4/P6 passing is
   recorded as the result it is, not promoted into a status.
3. **It has crossed from "knock" to "win with caveats"** — which is what earns
   it the description line and the FAQ below, per the owner's framing: the
   shuffling is real, players WILL ask, and the answer is honest rather than
   defensive.

### `[FAQ]` — "Why are my colonists shuffling around constantly?"

⚠️ Draft for the launch-time FAQ collection (`WORKFLOW.md` "`[FAQ]`" — this tag
is a bookmark, not work, and it dies with the behaviour if the behaviour
changes). Player-facing wording, ready to lift:

> Your Nursery/Retirement Dome policy is moving homeless **jobseekers** out —
> Colonists of working age with no home and no job. It sends them to the nearest
> Dome with housing they could *use*, which is not the same as housing that is
> *free*: in a colony with no spare beds anywhere, requiring a free bed would
> mean nobody ever moved and the policy would do nothing at all.
>
> So if your colony is short of housing overall, they arrive still homeless and
> the game keeps re-evaluating where they belong. **The shuffling is a housing
> shortage becoming visible — this is a mobility fix, not a housing fix.**
>
> A Dome with an **open job** will also pull jobseekers back in. That is
> deliberate: without it a dedicated Dome could never replace the staff running
> its own services.
>
> **To resolve it:** build more housing — that is the actual fix. Or switch the
> policy off on that Dome and the homeless will simply stay. And check the row
> before blaming it: it reads **"N would move"** or **"N moving out"**, and if
> that number is **0** this policy is not moving anyone.
>
> Never moved, in any case: Seniors, Children, anyone employed there, and anyone
> under a manual Dome order of yours.

### Mod-page detail line (for the `MOD_DESCRIPTION.md` rebuild at launch prep)

> Optional, off by default. On a Dome with a Nursery or Retirement Home, moves
> homeless working-age jobseekers to a Dome that has housing they can use — so
> graduates and leftovers stop piling up until the Dome counts as overcrowded
> and stops accepting the Children or Seniors it was built for. Employed staff,
> Seniors and Children stay. Most useful when spare housing exists somewhere in
> the colony.

⚠️ **The last sentence is load-bearing and was nearly written the other way.**
The sitting's own numbers earned it: colony homeless fell to 16 while the two
flagged domes alone had held 38, because the beds existed elsewhere and the tie
was the only thing in the way. Had colony homelessness stayed flat, the honest
line would have been *"changes which colonist lacks a bed, not how many"* — keep
that sentence ready in case a later colony behaves that way. **Do not publish
the stronger claim as a measurement; it is uncontrolled** (§ PT-62 re-run).

---

## ⭐⭐ THE FREE-WORK DOOR (2026-08-03, owner-approved) — the over-correction the symmetric halves introduced, repaired BEFORE the leg re-runs

**Derived from Src, never observed** — filed and fixed the same session because
the fix is small and the alternative was a wasted sitting. Owner's call:
*"if we can allow the wall to open when there is a job opening that would solve
it … even if theoretically the OG plan works this would just be extra hardening
of the fix."*

**The defect in the 2026-08-02 response.** The symmetric veto refuses the class
this policy pushes out, and that class is `need_work`. But `need_work` is
**exactly the set vanilla moves toward an open job**:
`better_work = not work_available and new_work_available` (`Colonist.lua:2679`)
is reachable only when `need_work` is true (`:2623-2624`). `ChooseDome` closed
the other entrance. So a flagged dome **could never be staffed from outside** —
and for the pattern the module exists for (*"2 domes out of the way of their
resources and major production domes"*) there is no passage, so migration is the
only staffing route. Its service staff age out or die unreplaced, the services go
dark, and the player sees *"my retirement dome isn't working"* — the exact
confusion §4b designed the row to prevent, reached from the opposite direction.
⚠️ This was a **more plausible harm than the ping-pong**, because the ping-pong
now has two guards and this had none.

**The rule: a flagged dome neither pushes out nor refuses a colonist while it has
a free workplace THEY could take.** Vanilla computes it —
`Workforce:HasFreeWorkplacesAround` (`Workforce.lua:43-51`), reachable on every
community here since `Community.__parents = { "Workforce" }` (`Community.lua:8-9`)
and both `Dome` (`:376-377`) and `MicroGHabitatBase` (`MicroGHabitat.lua:3-4`)
parent Community. At the arrival seam there is no colonist to ask, so it is
vanilla's colonist-independent `GetFreeWorkplacesAround` (`_GameUtils.lua:443-455`)
instead — coarser, and erring toward letting people in, which is the safe
direction there.

⛔ **IT IS ON BOTH SIDES, AND THAT IS THE WHOLE DESIGN.** An entry-only door
re-creates PT-62's loop in miniature: admitted for the job → loses the slot to
someone else → now a push subject → pushed straight out → slot still free so the
entry door is still open → offered again. Bounded by work slots rather than the
~97-scoring cohort slots, but the same defect shape. With the **same predicate on
both sides** every branch terminates:

* a slot they qualify for exists → not pushed → they stay a candidate for it;
* they win it → employed → `is_push_subject` false for good → dome staffed;
* someone else wins it → no free slot → the entry door shuts **in the same
  moment** they become pushable → they leave **once**, with no re-offer possible.

Race (two admitted for one slot) resolves the same way: the loser is pushed once
when the slot closes. One-way, terminating. This is the module's existing idiom —
one predicate shared by both halves so the two can never disagree, as
`is_push_subject` already is across push and veto, and `has_cohort_housing` is
across wrapper and UI.

⚠️ **THE COST, AND IT IS PT-62's TO MEASURE, NOT ARGUED AWAY.** N homeless
jobseekers in a flagged dome with ONE open slot means **all N are held**, because
each individually sees a free slot. The expectation — colonists seek work in
their own dome first, so "unemployed resident + a slot they qualify for" resolves
quickly — is an **ASSUMPTION, not a measurement**. If the leg finds the slot does
not fill, this door needs a dwell bound.

**Three consequences carried in the same pass, so nothing contradicts:**
* `count_movable` applies the door too, or the row would read `5 moving out`
  while five people stay — the disagreement between number and behaviour that
  function exists to prevent. A recruiting dome now reads `0 would move`.
* The rollover states it in the player's words.
* ⚠️ The registry `title` was **stale independently of this change** — it still
  described the NARROW reading (*"colonists the dome cannot house"*) falsified on
  2026-08-02. `ListFixes()` prints that string, and a leg reads `ListFixes()` as
  account truth, so it was corrected in the same commit.

## ⭐ PT-62 RE-RUN, 2026-08-03 (attended) — THE DRAIN WORKS, AND THE LEADING INDICATOR WAS BLIND

**Live numbers, two flagged domes.** `Sacagawea #2` (handle 5276, DomeMega,
retirement): homeless **23 → 10** under the policy, `employed 2`, `freework 1`,
`overpop true`. Its row read **`Nursery / Retirement Dome (8 moving out)`** —
correct title, correct polarity (yellow/restriction), and it **reconciles
exactly**: 10 homeless − 2 employed-exempt = 8 movable. `Clark #1` (6396):
homeless 15, `employed 0`, `freework 0`, `overpop false`, flagged.

⚠️ **The free-work door never fired, and the log says why — CORRECTING A CLAIM
MADE LIVE IN THIS SITTING.** Mid-sitting I explained the drain running at
`freework 1` as "the coarse count says a slot is free but the per-colonist
`CanWorkHere` test says none of them can take it, so the fine test governs".
**The log refutes that**: the two readings are not the same moment, and the
owner had cleared the slot in between —

```
0:32:52  Sacagawea #2 5276 homeless 23 employed 1 freework 1 overpop true  flagged true
0:33:09  Sacagawea #2 5276 homeless 10 employed 2 freework 0 overpop false flagged true
```

`freework` was **0** by the time 8 were movable. The door was simply INERT, and
the fine-vs-coarse story was a rationalisation of two conflated readings. The
distinction it describes is real in the code and still governs — but it was not
what happened here and is not evidence. ⛔ **P14 remains entirely UNMEASURED:
the door has never fired in play**, so the N-held-by-1-slot cost is still an
argument, not a reading. (The Medic arrival mid-sitting was a candidate firing
and was never confirmed either way before the slot was cleared.)

⛔⛔ **THE LEADING INDICATOR COULD NOT FAIL HONESTLY, AND IT NEVER COULD HAVE.**
The loop check counts colonists whose `emigration_dome` is a flagged dome. But a
flagged dome **must keep receiving Seniors and Children** — HARD CONSTRAINT 1,
the cohort delivery this module exists to protect, which the veto deliberately
never touches. So the counter cannot distinguish a leaked push subject from
correct cohort delivery. Tonight it read `genuinely-inbound 8`, which looked like
a leak and was not:

```
inbound SUBJECTS = leak 0 · inbound non-subjects = correct 7
```

**Zero leaks.** Split by the subject test (`CanWork` and no workplace and no
`user_forced_workplace` and not Tourist), every inbound colonist was a
non-subject. The symmetric veto and the `ChooseDome` filter did not leak.

⚠️ **AND THE SAME BLINDNESS SITS UNDER THE 2026-08-02 FINDING.** *"Six colonists
were en route INTO flagged domes"* was recorded as **"Measured, not inferred"**;
the measurement was genuinely taken, but with **this same counter**, which could
not tell a returning jobseeker from a Senior being delivered correctly. The
mechanism written up beside it (dome drops below `IsOverpopulated` → free cohort
slots score ~97 → vanilla offers the dome back) is real and does apply to
subjects — but **the number that justified building both guards carries the same
ambiguity as tonight's 8, and it was never split.** This does not retro-invalidate
the guards; the over-correction they caused was real and the free-work door that
repairs it is real. It does mean the indicator has never once been read in a form
that could have failed. ⛔ **The blind form is retired: the split counter above is
the loop check from now on.**

### ✅✅ P4 AND P6 PASS — the retirement dome cleared completely

`Sacagawea #2` ran **homeless 23 → 10 → 0**, and **`overpop true → false`**. The
knife-edge requirement is satisfied by the sequence itself: a monotonic decline
across three readings while flagged, with the row showing `8 moving out`
mid-drain, is the DRAIN clearing the dome and not the attrition the 2026-07-30
note warned about. The D07 deadlock this module was built to break is broken —
`overpopulated` cleared, so cohort delivery can resume unaided.

⭐ **And they were largely HOUSED, not merely relocated.** Colony-wide homeless
read **16** at the end, while those two flagged domes ALONE held 23 + 15 = 38
earlier in the same sitting. The destination preference (a community that can
house them NOW wins over one that cannot) did real work: the beds existed
elsewhere and the vanilla tie was the only thing keeping colonists from reaching
them, which is exactly this entry's thesis. ⚠️ **Not a controlled measurement** —
there is no pre-flip colony-wide baseline, and time passed — so it is stated as
strongly suggestive, not proven.

⚠️ **`Clark #1` (the NURSERY) is the slow one, and the owner's reading of why is
the likely one:** 15 → 14, `overpop false` throughout, `freework 0`, `employed 0`.
A nursery graduates Children into workforce-age Youths continuously, so it
manufactures new subjects as fast as the policy removes them. **That is a steady
state, not a failure** — and holding BELOW the overpopulation threshold with a
continuous graduation inflow is precisely the outcome the module exists for,
since a latched nursery is one D07 can no longer deliver into. Not proven,
though: a longer observation is owed to separate "steady state" from "barely
moving".

**Still owed:** the split counter through a **rocket landing** (the only thing
that tests the `ChooseDome` half — a single at-rest reading does not); a longer
`Clark #1` observation; **P14** (never fired); P13; P12.

✅ **P11 HOLDS — verified against the log file, not the screen.**
`Mars.exe-20260803-13.30.54` carries **exactly one** `[LUA ERROR]` across the
whole sitting, and it is **operator instrumentation, not pack code**:

```
[LUA ERROR] attempt to index a nil value (field 'labels')
  Lua/_GameUtils.lua(446):  global GetFreeWorkplacesAround
  [string "CreateRealTimeThread(function() local d = Sel..."](1)
Locals:  dome | object SmartApartmentsCCP1 at (473500,444258,5245) on map 1
```

The snapshot line was run with a **residence** selected instead of a dome, so
vanilla's helper got a building with no `labels`. (A second on-screen instance
was the same line with nothing selected at all.) The module's own call site
guards `type(community) ~= "table"` AND checks `labels.Workplace` before calling
out, which is why the pack never produced this. ⚠️ **Lesson for the instrument,
not the module:** an operator snapshot line must validate `SelectedObj` is a
Community before calling a Community helper — mine did not, twice.

✅ **THE PROBE CASES RAN GREEN ON THE RETAIL BUILD, 2026-08-03** (PT-62 re-run,
attended): first suite run after the change read **`78 PASS, 0 FAIL, 9 SKIP,
0 ERROR`** — the pre-change baseline, unmoved — and the NoHomeless probe's PASS
string carried the new clause verbatim: *"the free-work door holds on BOTH sides
so a flagged dome can still staff itself."*
⛔ **WHAT THAT DOES NOT CLAIM.** These are stand-in-table assertions: they prove
the wrapper's LOGIC branches as designed, and nothing about live colonist
behaviour. **P14 is still owed in full** — whether an open slot in a flagged
dome actually FILLS is the assumption under test, and no probe can answer it.

**Probe:** cases 3b (push side) and 13b (entry side) in `60_Probes_Opt.lua`. The
stand-in communities gained an explicit `HasFreeWorkplacesAround` stub defaulting
FALSE — the module tolerates the method being absent (these stand-ins are exactly
why), but a probe leaning on that tolerance would assert the fallback rather than
the behaviour.

---

**Origin: found in play 2026-07-30**, by deliberately stressing homelessness and
unemployment in a dedicated child dome. Observed live: nurseries at 5/26 and
3/26 (**68 free Child slots**) with **28 homeless in the dome — 26 Youth, 2
Adult** — while a Child in a neighbouring dome commuted in to the school and
went home to a Smart Apartment. `accept_colonists` read `true`, so the
quarantine was NOT involved.

**Root cause — a vanilla tie rule, not a mod defect.** The shipped emigration
eval explicitly permits a homeless colonist to move to a dome with no free
housing (`Colonist.lua:2676`, comment: *"if homeless, try changing community
even if doesn't have living space available"*). But the gate above it requires
the candidate to score **strictly better** unless home or work improves
(`:2675`, `:2680-2681`). With zero non-cohort free slots colony-wide
`better_home` is false everywhere; with unemployment saturated `better_work` is
false too. Every candidate **ties**, and ties never move anyone. This is the
same tie rule D07's header already cites as the reason cohort members never
consolidate — it strands the graduates for exactly the same reason.

**Why it compounds (the D07 deadlock).** The stranded homeless push the dome
over `IsOverpopulated` (`#labels.Homeless >= g_Consts.OverpopulatedDome`,
`Dome.lua:1026-1035`), and D07's own cross-dome `consider()` skips
`community.overpopulated` (`Opt_CohortHousing.lua:194`) — so the child dome is
permanently excluded as a destination and no new Children ever arrive. **The
module poisons its own destination, and it tightens with every child it
delivers.** D12 breaks the loop at the cause: drain the homeless, the flag
clears, D07 resumes unaided.

**Rejected alternatives (recorded so they are not re-proposed).** Dropping
`or community.overpopulated` from D07's `consider()` was the obvious one-clause
fix; the user rejected it — it only stops D07 *noticing* the problem and leaves
homeless colonists stranded. Refusing to deliver children into domes with no
non-cohort housing was also rejected: it does not heal an already-poisoned dome.
Both are still on file if D12 proves impractical.

**Also settled, and NOT a defect:** the homeless youths themselves.
`non-cohort free slots colony-wide: 0`, so total homelessness is set by
(population − housing capacity). D07 changes *which* colonist lacks a bed, not
how many, and arguably improves utilisation by freeing ordinary slots for
adults. No F-number.

## WHAT D12 SHIPS

A per-dome / per-habitat **"no homeless residents"** policy: when set, homeless
colonists in that dome are moved to the nearest community that accepts them,
using the shipped emigration machinery.

**Hard constraint (the trap that killed the first design).** The flag must be
its **own field with its own gate** and must **NOT** route through
`Community:CanAcceptNewColonists`. D03's existing "closed to new residents" row
wraps exactly that method (`Opt_ResidencyControl.lua:35-37`), and D07's
`consider()` calls it (`Opt_CohortHousing.lua:193`) — so reusing D03's gate
would block cohort delivery into the very dome we are trying to protect. The two
controls must act in **opposite directions on the same dome simultaneously**:

| Flag | Child dome | Effect |
|---|---|---|
| `SMRFixPack_closed_to_new_residents` (D03, existing) | **off** | children can still migrate in |
| `SMRFixPack_no_homeless` (D12, new) | **on** | graduates are pushed out before they pile up |

**Mechanism — reuse D07's proven wrapper shape:**
* Post-wrapper on `Colonist:FindEmigrationDome` (declared on Colonist,
  `Colonist.lua:2581`; called every heavy update from
  `Colonist.lua:1894-1898`, so no new scheduling). When the colonist
  `IsHomeless()` and their own dome carries the flag, pick the **nearest
  reachable community that does NOT carry the flag** and that
  `CanAcceptNewColonists()`, bypassing the tie. Reuse the shipped candidate
  gathering and `FindTransportationModeToCommunity` exactly as D07 does —
  walk / passage / shuttle / train / elevator.
* **Never expel to the surface.** If no candidate qualifies, return the shipped
  answer unchanged and the colonist simply stays. Best-effort only. A colonist
  put outside with no dome dies (F53 territory) — this is the one failure mode
  the design must make structurally impossible.
* Ping-pong guard: only consider destinations WITHOUT the flag, so two flagged
  domes can never trade the same colonist back and forth.
* UI: a second toggle row on the dome / MicroG-habitat infopanel via the shipped
  `Community:TogglePolicy` / `SetPolicyState` machinery (`Community.lua:77-100`)
  — same idiom as D03's row, including Ctrl+click broadcast and the rogue-dome
  UI lock.

**Packaging decision (user, 2026-07-30): its own module**, with
`Opt_ResidencyControl` as the **donor pattern only** — copy the row-append and
policy-toggle approach, share no gate. Rationale: D03 is `tested` and shipping
(PT-49 in full, plus PT-55), and duplicating the infopanel-row machinery is
cheaper than re-qualifying a shipped module. The two behaviours are distinct
concepts — block entry vs. force exit.

**Defaults and safety:**
* Every dome defaults to **allowing** homeless (flag absent = vanilla). A module
  that stranded a whole colony on first enable would be unshippable.
* Opt-in module, off by default, per-call `IsActive` gate, file-scope install
  (the A2 lesson — FIX_POLICY §5).
* Savegame footprint per FIX_POLICY §3: `SMRFixPack_no_homeless` on the
  Dome/Habitat object, absent-tolerant both ways; the unmodded game never reads
  it and a save carrying it loads fine with the module or the pack removed.

**Precedence:** quarantine (`accept_colonists` false) wins absolutely — a sealed
dome neither admits nor releases anyone, matching the shipped early-out at
`Colonist.lua:2632-2635`. Player-forced dome (`CheckForcedDome`) wins over the
policy. The D03 row composes independently.

**Open question for the build:** whether the push should apply to ALL homeless
or only to colonists the dome can never house (i.e. no residence in the dome is
`IsSuitable` for them). The narrow reading is more surgical and is what the
child-dome case actually needs; the broad reading is simpler to explain to a
player. Decide before coding.

**PT owed → WRITTEN 2026-08-02 as `PLAYTEST_CHECKLIST.md` PT-62**, predictions
P1-P9 recorded before any run. Original text, kept because the checklist item is
derived from it: a new item covering — set the flag on a nursery-only dome with
stranded graduates and watch them drain; confirm the dome's `overpopulated`
clears and D07 resumes delivering children unaided; confirm NO colonist is ever
left outside a dome; confirm a flagged dome with no valid destination leaves
its homeless in place rather than expelling them; toggle off = instantly
vanilla; ~~save with it ON, reload with the module OFF, clean load~~ **→ save with
it ON, reload with the pack disabled in the MOD MANAGER, clean load.**
⚠️ **METHOD CORRECTED 2026-08-01 — a toggle CANNOT answer an uninstall question.** With the module merely switched off the mod env is still present and the hooks are still installed, so any captured frame resolves `SMRFixPack`, reads inactive and no-ops: **it reads clean by construction, whether or not the module leaks.** `Opt_DroneOverhaul` leaked at 98 errors/session with its own toggle OFF — that is how F86 Site 2 was found. Use **Mod-Manager-disable** (measured equivalent to a real uninstall, PT-20: 98 vs 98 on the same save). `ENGINE_FACTS.md`, "OFF" IS THREE DIFFERENT THINGS.

**CHAIN CONFIRMED END TO END (2026-07-30).** The inferred link (step 4) was
measured live: `g_Consts.OverpopulatedDome` = **20**, and the child dome read
`overpopulated=true homeless=20`. So the flag is latched by the stranded
graduates exactly as traced, and nothing in the chain is now inference.

**Knife-edge, and useful for testing D12:** the threshold is `>=`, and the dome
sat at **exactly 20** (down from 28 earlier the same sitting, as a few drained
or died). Two more departures would clear `overpopulated` and D07 would resume
delivering unaided. So the deadlock is genuine but marginal — a D12 test must
confirm the drain is what cleared it, not natural attrition. Take a homeless
count immediately before setting the flag and immediately after.

**2026-08-03 — PT-62's re-run requirements moved HERE from the checklist**
(the redesign; the partly-run record of 2026-08-02, predictions P1-P13 and the
four setup traps are preserved verbatim in `PLAYTEST_ARCHIVE.md`'s pre-redesign
snapshot — the agent running the remainder reads them there first). What is
still owed: **P4 and P6** (the drain clearing the dome, unpolluted by inflow),
**P12** (Mod-Manager uninstall), **P13** (the repaired per-call lever). The
re-run MUST:
1. ⛔ **Restart first** — all three post-sitting changes (the symmetric
   `FindEmigrationDome` refusal, the `ChooseDome` arrival filter, the repaired
   `SMRFixPack_Disabled.NoHomeless` lever) need it.
2. `*r SMRTest.RunAll()` — expect the PT-62 baseline (`78 PASS, 0 FAIL,
   9 SKIP` at the time; re-read against current STATE counts).
3. **The loop check, now the leading indicator** — must reach 0 and STAY 0
   through a rocket landing (the emigration veto alone would not survive one;
   this is what tests the `ChooseDome` half): [NEVER RUN post-changes]
   `*r local bad = 0 for _, city in ipairs(Cities) do for _, c in ipairs(city.labels.Colonist or empty_table) do local d = c.emigration_dome if d and d.SMRFixPack_no_homeless then bad = bad + 1 end end end ConsolePrint(print_format("heading INTO a flagged dome", bad))`
4. Only then are P4/P6 meaningful — on a stable colony where the drain is not
   fighting an inflow.
   ⛔ **THE D03 INSTRUCTION IS WITHDRAWN (2026-08-03, owner challenge).** The
   2026-08-02 re-run plan said to run P4/P6 with D03's "Closed to new
   residents" on so landings could not refill the dome. **It works, and it
   would still have invalidated the leg.** Two reasons:
   * ⛔ **It would MASK the guard under test, at both seams.** D03 wraps
     `ChooseDome` (`Opt_ResidencyControl.lua:208-229`) and
     `CanAcceptNewColonists` (`:88-93`); D12's arrival filter and symmetric
     veto sit on those same two paths. With D03 on we would measure D03's
     veto and learn nothing about D12's — and the loop check, which is the
     leading indicator, would be trivially 0 for the wrong reason.
   * ⛔ **It is not a shape a player would ever be told to use** (owner,
     2026-08-03): it blocks **Seniors**, i.e. the cohort delivery HARD
     CONSTRAINT 1 exists to protect.
   ⚠️ **CORRECTION, same day, recorded rather than quietly fixed.** This block
   first claimed D03 "does not block landings at all" — that arrivals read the
   raw field `community.accept_colonists` via `is_welcoming_community`
   (`_GameUtils.lua:342-344`) and never call the METHOD D03 wraps. **Both
   citations were right and the route was wrong:** D03 carries its OWN
   `ChooseDome` wrapper for exactly this reason, so it covers the landing path
   after all. A follow-on "D03 under-delivers its label" lead was raised on
   that false route and is **RETRACTED — there is no gap.** The vanilla
   raw-field read is a SAFEGUARD, not an oversight (owner, 2026-08-03: a player
   who had closed their only dome in walking distance would otherwise kill
   every arriving colonist), and D03 already honours it by passing
   `safety_dome` and tourists through unfiltered — as does D12's own filter.
   **Run it clean.** A dome that refills anyway is a FINDING — the ChooseDome
   filter or the symmetric veto leaked — not a fixture problem to suppress.
5. P6 caveat from §4c above: do not read a failure to clear `overpopulated`
   as a miss without first checking how many remaining homeless are employed.
6. ⭐ **P14 — THE FREE-WORK DOOR (new 2026-08-03, and it is the one unmeasured
   assumption in the build).** Flag a dome that has an **open service work
   slot** and read whether the slot **FILLS**. Two things are under test at
   once: that the dome can still staff itself (the door works), and that the
   hold is short-lived (the cost is acceptable). ⚠️ **`movable` reading `0` on
   a recruiting dome is the door working, NOT the module failing** — that is
   the reading most likely to be misfiled as a miss. If the slot never fills
   while unemployed homeless sit in the dome, the door needs a dwell bound and
   D12 does not ship as built.


---

## ⚠️ 2026-08-05 — the module's own self-check reports an AUTHORING ERROR at apply time

Found by the corun-batch-1 sitting's PT-22 log review, not by looking for it.
Log `docs/archive/cb1sitting_Mars.exe-20260805-14.28.49.log`, lines 161-162 and
185, all at the MAIN MENU before the first load:

```
[CommunityFixPack] NoHomeless: self-check targets Community but Workforce declares
                   HasFreeWorkplacesAround — authoring error, not a game update (FIX_POLICY §2)
[CommunityFixPack] NoHomeless: inactive (Community.HasFreeWorkplacesAround not found
                   (game update changed the Workforce mixin?) — without it a flagged dome
                   could never be staffed)
...
[CommunityFixPack] NoHomeless: applied
```

**This is the F64 lesson repeating** — the same mistake `Fix_BombardmentSpread`'s
header warns about in its own comments: *check the DECLARING class, not the class
you expect to call it on.* `Opt_NoHomeless.lua:807-808` preflights
`{ class = "Community", method = "HasFreeWorkplacesAround" }`, but the method is
declared on the **Workforce mixin**; at apply time classes are not flattened, so
the lookup misses and the module deactivates itself.

**Net effect this session: none.** It applied at line 185 and `ReadConditions`
after the load read **81/81 active**, with `NoHomeless` not in the not-active
list. The runtime path is independently guarded anyway —
`Opt_NoHomeless.lua:417` does `if type(community.HasFreeWorkplacesAround) ~=
"function" then return false end` before calling it.

**Why it is still worth an entry.** A self-check that can wrongly deactivate a
module is a defect in the guard, not in the guarded code: it logs a scary
`inactive` line naming a *game update* as the suspected cause, on a build where
nothing changed. The pack's own diagnostic distinguishes the two correctly and
said so — that message did its job — but the preflight target should name the
declaring class so the false negative stops happening at all.

⛔ **Not built, not changed.** The corun-batch-1 scope fence forbids building
here (deciding ≠ building). Recorded so the next session that sees an `inactive`
line for `NoHomeless` recognises it in seconds instead of chasing a game update.
