# Train hub: look step 1 — the road surfaces as polished black glass

**LIVE, one-off. FIRE IT, THEN DELETE THIS FILE AND ITS MAP ROW in the fire commit.** Assets work
in `B:\Dev\SMR\SMR-Assets\trainhub\blender`, plus the owner's Mod Editor import. Authoring sha:
SMR-Assets `df9bb50`. An empty `git diff --stat df9bb50..HEAD -- trainhub/` means this brief's
facts hold. Start with `git log`, `git pull` in both repos.

## Authority

The owner, 2026-09-21, settled all of this; spec §9 carries it (paragraphs "The AI texturing trial
is CLOSED", "The owner's visual handoff", "Pad direction", "Staged"):

- **The look goes by bake onto our frozen UVs.** The model is FINAL and frozen at radius 6. Any
  geometry or UV change is a stop. `concept_guard.verify_scene()` before every bake, as in
  `TRAIN_HUB_LOOK_high.md` (read it; its facts on the maps, SI and the one-material rule hold).
- **The transport surfaces are to be "a highly polished metallic black, that's almost glass
  looking"**, not gunmetal grey. This step is those surfaces and **nothing else**: outer platforms,
  portal approaches, radial paths, the centre plate. The owner inspects in game, then rules on
  the next step (structure, then lights, then fine normal).
- The owner's handoff package (`C:\Users\stkot\Downloads\TrainHub_Visual_Handoff_v1\TrainHub_Visual_Handoff_v1`)
  is **reference only**: `04_MATERIAL_PALETTE.png` and `01_HUB_LOOK_TARGET.png` show the intended
  neighbours of the pad. Its `maglev_pad` values are exactly what the owner finds too grey. Do not
  use its GLB, its Blender materials or its overlay geometry.

## End state

The four maps regenerated with the deck texels changed and every other texel **unchanged**, ready
for one map re-import (the body FBX is not re-imported: same UVs). Two pad variants for the
owner to judge by eye in game; the spec §9 proposal, UNTESTED, is base about `#05080B`, roughness
0.04-0.08, and metalness 0-0.3 (a dark dielectric, Fresnel sheen at the oblique camera) against
metalness near 1.0 (a mirror). **Your call** on the final values and on making the swap cheap —
if the variants differ only in RM, the owner swaps one map, which beats two full imports.

Done means: the owner can look at the hub in game, day and night, and say which variant is nearer
"polished black glass" and what to change.

## Leads, not the route

- `paint_concept.py:30` `DECK` and `:96-117`: deck texels are today painted from `Track_*` and
  `CentrePlate` up-facing faces (normal z > 0.8). The RM map holds 5 distinct values and metalness
  0 everywhere (spec §9, sampled 2026-09-21; falsify with `validate_concept_maps.py` or a
  histogram of `SMROptInTrainHub6_RM.tga`). **First question, before baking anything: how are
  pad faces identified today, and does that set match the owner's "road surfaces" exactly?**
  Report it in the first commit message.
- The light strips and floor curves sit on the deck and are the owner's approved shapes. Keep
  their shapes and the SI mask as they are; only the surface under them changes.
- The game has no clearcoat channel. Gloss is roughness only. Whether the game's shader reflects
  enough for glass is NOT measured; that is the owner's look to give.
- Resolution: keep today's 2048. Do not enlarge maps.

## Scope

In: the deck surfaces' base colour, RM and, only if the glass read needs it, a very restrained
normal on the deck. Out: shell, rails, trim, portal rings, lights, glass entity, reactor, dome,
Lua, the dev mod's code. Out-of-scope findings go in the report.

## Stops (report instead of pushing on)

1. The pad faces cannot be separated from the rest by an existing mesh, material or normal
   test without a geometry or UV change.
2. `verify_scene` or the UV fingerprint fails, or any non-deck texel differs from the
   pre-change maps.
3. The two variants cannot both be delivered as a one-map swap **and** a clear owner-facing
   naming; say what it costs instead of shipping something unclear.

## Do not claim

Do not claim it looks like glass in the game; a Blender render is not the game. Claim what you
measured: the deck set, the changed and unchanged texel counts per map, the values chosen and
the render you compared.

## Deliver

1. Todo list first, one item per commit-and-verify unit.
2. Maps regenerated by the script (its knobs named, never hand-edited output), variants labelled.
3. Prove non-deck texels unchanged: per-map count of differing texels outside the deck set.
4. Owner import, about five steps, naming the slot for each map. In-game smoke only (design
   pass); the full battery is for the final build.
5. Record in spec §9 with `doc-editing`, commit both repos with pathspecs.
