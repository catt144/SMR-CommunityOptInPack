# gamepatch/done/ — entries acted on

Each file here is a `gamepatch/` entry whose verdicts are filed in `docs/agent/bugs/`; it was moved
here with `git mv` in that commit. Kept as the record of which patch was swept against which build.
