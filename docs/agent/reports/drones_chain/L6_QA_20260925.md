# Drones chain L6 — terminal QA, 2026-09-25

**PASS WITH CORRECTIONS for chain close-out; not release certification.** The repair mechanism,
persisted kind field, engine-flight choice and later owner rulings landed. The sampled native
repair/reload evidence supports the bounded smoke. A new multi-hub fleet defect reproduces,
some claims remain owner observations rather than instrumented results, and the ship matrix is
unrun. Corrections below are work for the next active hub link, not changes made by this audit.
No finding here establishes that the current single-hub fixture is unsafe to keep; no game ran.

Audited base: Opt-In `cc3d6f1078cbc224ea10e0024b2960792a297f13`, Fix Pack `c41ccb1`.
Both `git log` / `git pull` calls completed, already up to date. Peer commits `ce064ed`, `bed0268`
and `64e3132` subsequently changed knowledge-sync/tooling and an owner-list pointer, not the audited Lua. The initial dirty template, generated
template, metadata and UI, and later dirty `pack_predict.py` / `sync_from_fixpack.py`, are excluded
from this change. Attribution is by diff. Executed model: **Codex, based on GPT-6**, as exposed
in this transcript; no more specific runtime model identifier was exposed. No subagents.

## Evidence boundary

The command-bearing receipt [audit.json](../../../archive/drones_l6_20260925/audit.json) records
HEAD, source hash, historical samples, retirement commits, receipt checks and the actual-Lua
reproduction. Reproduce its checks from the repository root with
`python docs/archive/drones_l6_20260925/drones_l6_audit.py`; the captured run was on `bed0268`.
The script is an audit instrument, not a production test or a native game fixture.

The named L5 game logs were **not archived in its closing commit**. `git ls-files` in both repos
and `git log --all --name-only -- docs/archive` found only the earlier audit's partial
`12.35.05` log among the sampled names. Compressed archive candidates were absent; the receipt
lists both name inventories' matches. Late recovery found the original L5 files still on disk.
`python scratch/drones_l6_logs.py` copied them byte-for-byte, with a before/after equality check;
the command is preserved beside its [log receipt](../../../archive/drones_l6_20260925/log_receipt.json).
That receipt reconciles **8 files / 491427 bytes** against its named members. All have Lua revision
405907 and normal shutdown markers. These logs are now committed in this audit, including the bad
door build. This repairs preservation, not the historical failure to archive with the verdict.
They carry game revision and mod version, **not a source SHA**; exact running-tree provenance
cannot be reconstructed from version alone.

`python tools/doccheck.py --emit-fingerprint` read installed build **25390750**: older game
groups MOVED, the 1.1.1.405907 group HOLDS. The brief's blanket “every group MOVED” is now stale
because EF-117 has landed. Old citations below were checked in the versioned archive they name;
this audit does not substitute for the queued gamepatch sweep.

## Backward audit: implementation and handoffs

`git log --diff-filter=D --name-only -- <03_Drones>` plus each deletion commit's
`git diff-tree --name-status` is captured in `audit.json`. The destination text, not just the
changed filename, was sampled where the downstream instruction mattered.

| link | strongest sampled claim and primary check | verdict / destination |
|---|---|---|
| L1 | Archived **1.1.0.403908**, `ConstructionSite.lua:2659,2693` dispatches group completion to members; `TrackElement.lua:841` declares the concrete completion. Read with `rg -n` and `Get-Content`; these support the concrete route rather than DESIGN's generic name. Survey lenses are committed at `ac32389`, fact allocation/mirror at `38022d1` / `079a347`. | SOURCE PASS for sampled completion claim. `b8ab545` delivers L2/L4/L6 and retires L1. Geometry remains a historical measurement, not current live clearance. |
| L2 | `git show 0edc0c9:.../30_TrainHubDrones.lua`: `init_with_command=false`, its real-time driver and route are present; no economic completion belongs to that prototype. Current `flight_smoke.py` still exercises scripted graph/tunnel/recall cases under tagged dials. | PASS as prototype, overtaken for flight/save authority. Same commit delivers L3/L4/L6 and retires L2. |
| L2R | `0ad2e3d` retains OI-25's crest and adds under-deck/outside points. Its committed receipt's flight SHA matches that commit; triangle and leg totals reconcile against members in the audit command. | SOURCE/receipt PASS, no native-clearance promotion. Same commit delivers L3/L6 and retires L2R. Door ruling later overtakes the exit. |
| L2M | `74b1e4a` issues timed `SetPos` / `SetRollPitchYaw` from its real-time sampler; historical receipt's source hash and member totals reconcile. It did not call the engine flight solver as the movement mechanism. | PASS for implementation, owner rejected its feel. `6a56be0` delivers L3/L6 and retires L2M. Time-refinement and reverse-timing corrections remain in its drift table. |
| L2M2 | `b84f106` has the game-time driver and coercing `div`; current flight smoke's integer/source gates pass. Tagged file is byte-equal to `b84f106`, checked with `git show` bytes. | PASS for corrected mechanism; **unverifiable** that L3 ran the requested `7/2` control before tuning. L3 reports have no result for it. Deletion actually landed in `6d89b1a`; report/maps/handoffs in `1e146a6`, not the same commit as claimed. Historical lifecycle violation, now drained. |
| L2E | `9a540dd` queues stock `WaitUninterruptable` after `FlightGoto`; current smoke exercises save-stage selection and hold takeover. Current file writes only the stock command tokens allowed by that test. | PASS under the mock's command model; native long-hold/Lost remains open. `bbcd8ad` delivers L3/L4/L6 and retires L2E. |
| L3 | `6cd20f3` hands engine mode, outside handoff, remote maintenance membership and dispatch-side reachability to L4. Its code still defaulted to **crest**, confirmed by `git show ... | Select-String 'HandoffAt ='`; `1b32bc8` fixes the mismatch. `6574794` adds the flight ModItemCode to the source item tree. | PASS WITH CORRECTION already implemented by L5. L5 already had OI-26 in its brief, and the later L4 handoff carries remaining probes. The retirement commit itself changes L4, not L5, despite its message claiming both. Source registration is confirmed; no independently archived editor-round-trip result. |
| L4 | `b556035` has `SMROptIn_track_work`, `kind="repair"`, the 50/100 rate, request claim/release and outstanding payment before `leader:Complete()`. Current repair smoke passes the economy, graph, dispatch, controls, filter and adoption cases. Current code `20_TrainHub.lua:2314` always delegates allowed station requests to the captured original. | PASS in scoped mock; D14(c,d) retain conditional completion/split risks. `0fbaeae` delivers L5/L6 and retires L4. `F.Adopt` was a declared necessary API addition despite the internals fence, not an unreported redesign. |
| L5 | Recovered `21.01.51` log has dispatch at :422, slot save :435, load :517, work-stage completion :528; `13.43.07:615` matches the reported 1161 m / 27193 ms dispatch. `ad5113b` replaces the throwing door probe with `CObject.HasSpot`; current flight smoke uses the raising mock. | PASS for those native events, with provenance and coverage limits below. `d6135b8` delivers L6, records spec/hub rulings and retires L5. |

Scripted mode is knowingly retained as a console alternative and at
`drones-scripted-flight-20260923` → `eedb69b`; **engine** is the live default at
`30_TrainHubDrones.lua:81`. L4 adopts engine Wasps; it did not build against a different flight.
The current clearance receipt explicitly bounds **tagged scripted** geometry. Its matching
current source hash does not turn it into an engine-leg or door-route clearance certificate.

## Native claims sampled against recovered logs

Log names below are under `docs/archive/drones_l6_20260925/`, with suffix `-6aad2d75.log`.
`audit.json` records anchored patterns, counts and every matching line, avoiding duplicate
SMRTK echoes when counting a save or load.

| claim | result |
|---|---|
| L5's long-trip speed / launch measurement | `20260924-17.07.20:305,316,363` gives dispatch t=31613981, outward arrival t=31636709 and leg duration 18784: launch remainder **3944 ms** by subtraction. Reported 1371 m / 7296 and return 7310 are literal log values. This is that trip, not a general ETA guarantee. |
| L5 “0 ms after deadline, stage work” | `20260924-21.01.51:338,528,603` supports the mechanism; :387 also reports **49 ms**, so not every completion was exactly zero. Later door-build completions are late relative to the predicted timer but still at work, e.g. `20260925-14.14.01:552`. |
| Mid-trip save/reload | `21.01.51:422–528` supports a dispatched repair surviving slot save/load and completing at work. No serializer failure marker in this closed log under the archived broad filter. Engine serialization is not proved by the Lupa smoke alone. |
| Door-build mid-trip reload | The owner's recorded acceptance in `cdce4b4` stands. The sampled `14.14.01:365,427` pair supports the **demolished-hub** save/load; it does not by itself prove a mid-trip adoption. `13.43.07` has a dispatch but no completion/save pair. Retain the door adoption as owner-observed, not independently certified by these samples. |
| Menu save after dispatch, cited to `12.35.05` | That full log has completed loads and no broad persist failure marker, but no repair dispatch/completion instrumentation. Cannot independently certify this stronger drone claim from that citation. The earlier D14 save-guard evidence remains valid in its own bounds. |
| Door crash count | Closed `20260925-13.34.56` has **37** anchored `[LUA ERROR] HGE::l_GetSpotBeginIndex: Invalid spot` events; line members are in `audit.json`. L5's 36 was an earlier count. Corrected its report line without changing its verdict. |
| “No persist errors” | The sampled post-fix logs lack `Persist error: Attempt to persist`, but `20260924-17.07.20:225` has **Unpersist missing permanent: Preset:NotificationPreset.SMROptInTrackRepair**, falling back to `BreakthroughDiscovered`. A clean load claim must include this class. Filed as D14(h), historical native warning; current-build reproduction owed. |
| Autosave | Owner closed the drone autosave item by equivalence (`7f34df8`), not another drone run. Archived **1.1.1.405907**, `CommonLua/Savegame.lua:1035–1061,1448–1451` confirms the shared `DoSaveGame` path. This does not certify autosave-specific cold reload, ambiguous legacy saves, or every failure window. Do not reopen the owner's accepted smoke merely by renaming that distinction. |
| Heavy load, recalls, no cargo loss, far upkeep, no charging, train restart | Owner observations in L5 remain observations in that fixture. Logs of normal completion/recall do not measure all of these. The ship matrix, multi-hub ceiling and long-run command behavior are not established by them. |

## DESIGN and the bans

| obligation | implementation / explicit later disposition |
|---|---|
| Pad launch and constant fleet | Pad removal `51b89a5`, OI-25 offset in code :77; pit rise retained. Owner's door launch `3c47ce2` and 5/15/25 plus repair reserve `cf14791` supersede the pad/constant-30 and under-deck rules. Spec §10 “Drones L5” is the obeying role's current ruling home. |
| Graph-bound repairs, cheap outstanding cost, toggle, short stock | `HubTrackGraph`, `outstanding_cost`, `hold_cost`, `can_dispatch`, `complete_job`; mocked repair suite passes. Graph remains unrestricted across map-linked shafts: OI-27 and D14(b) open, not silently narrowed. |
| Timer-only completion and absolute deadline reconstruction | Explicitly overtaken by owner `c764ea4` / `89330ed`: flight completion, relaunch on loss, timer fallback. `on_work_done` / `service_job` implement it. **Qualification:** `FlightGrace=100` can still complete a live-but-stuck flight at twice its predicted trip, before work ends. That is the recorded fallback, not unconditional “only at work.” |
| Follow rails, tunnel hide/show, above passing trains | Owner `ffff0e6` makes connectivity dispatch's job and permits engine free flight. Scripted tunnel behavior remains only in the optional mode. OI-26's fixed engine ride **kept as built** is recorded in spec §10 and orchestrator by `d6135b8`; it is not still an owner question despite historical README/DESIGN wording. No measured train clearance is implied. |
| Save guard | Engine legs/holds stay under stock commands; scripted stages and driver removed on save, spawns gated, load adopts. Original blanket-removal prose is overtaken by the hybrid owner decision and recorded L2E/L4 mechanism. D14(a)'s separate native-waiter guard preserves legacy mapping; ambiguous pre-wrapper saves remain unresolved. |
| Vanilla Wasps, top-up, local work, no reassignment or orphan adoption | Flight Create uses vanilla class, refill in UpdateEngine, fleet `on_released`; scoped CanBeControlled chain at :3037. `cafcaea` prevents hub adoption of orphans. Mocks support scope; UI/rocket/long-run and dead-grid observations remain owed. |
| Destroyed hub / carried cubes; malformed or unpowered hub | `HubDespawnRepairDrones` / `Finalize` drop before removal. Dispatch uses switch, not IsWorking. Malfunction observed; dead grid not run. Meteor exception and ruin behavior carry later owner rulings. |
| Train construction acknowledgment; palette | §6 train construction and full-hub retained cargo explicitly deferred by owner to import/export and dials (`119a224`, `94a9210`, `d6135b8`); palette moved to brief 05 (`642906b`). No assertion that either was tested. |
| Report, inventory, future kind | `FIX_POLICY.md:172` exactly matches `TRACK_WORK` at code :2129 and job construction :2355. Shape is exercised by repair smoke, including `kind`. Native-waiter marker at inventory :173 matches code :137,147. No persisted rename. |

Ban check: `rg -n SMRFixPack tools/devmods/train_hub -g *.lua` exits 1, with no error;
the decoded-input scan lists all Lua members, no compressed inputs, and positive `SMROptIn`
controls. The shipping module inventory is untouched. This establishes the dev hub's lack of
fix-pack executable references, not the unrun standalone shipping test.

## Corrections and owed work, with takeable homes

| work | evidence / owner / trigger |
|---|---|
| **C1 — isolate launch accounting per hub** | **D14(g)**: current `rising_count` deletes valid records owned by another hub. The archived probe executes the extracted Lua; same-hub live and dead/released controls pass, cross-hub retention fails, an in-memory counterfactual passes. No production edit. Next active hub build owns the fix and a multi-hub regression before claiming the shared ceiling. Actual excess-fleet timing has not been measured in game. |
| **C2 — resolve notification permanent warning** | **D14(h)**: observed load warning above; current notification registration is CityStart/LoadGame/InitHubTrackWork, not a demonstrated pre-unpersist registration. Next active hub build reproduces a cold load with an active repair notification, compares an ordinary preset control, then fixes if current. No claim of crash or save corruption from this warning. |
| **C3 — repair stale traffic verification** | `python tools/devmods/train_hub/tests/traffic_smoke.py` remains RED: `-10800 != 0` at its arrival-heading assertion (`:186`). `move_smoke.py` passes current siding movement. Next movement/code owner reconciles the obsolete geometry premise while retaining incoming/moving/crossing controls; do not count traffic as green or change expectations without the geometry basis. |
| **C4 — source/ruling drift at the next brief** | Build 5 still inherited “constant 30”, “pad” and tunnel script language. One precedence line now points to spec §10 “Drones L5” and this report; no owner ruling rewritten. DESIGN itself stays untouched by its explicit authority. Existing source comments that still describe a console-only prototype, old load-word tiers or deadline-based adoption are correction work for the next code owner. |
| **C5 — evidence that cannot be recovered here** | L3 integer-control result and independently recorded editor round-trip remain unverified; L3's pallet log `20260923-16.09.06` was not in the archive name inventories. A report's claim is not a substitute. Do not repeat the obsolete flight sitting merely to manufacture history; retain the limit and capture these controls when their surfaces next change. |
| **C6 — native residue / ship gate** | Shared Fix Pack checklist **ck217**, under the owner's one-game/both-mods rule, carries the next attended hub run. Reassign and rocket refusal; far-station go-home bounce and no balancing; long-run Lost including a hold beyond 60 s; no-grid repair; storage cap in play; actual door clearance/work pose. At ship: with and without a named released Fix Pack version, both module-toggle directions including mid-session enable/disable, restart where required, packed import and old→new→save→reload controls. No off/removal guarantee for placed content. |
| Cross-map, nanite, split, editor startup, ambiguous saves | Existing **OI-27 / D14(a–f)** retained in their actual passages, not refiled as new incidents. Source-field/citation sweep remains with `prompts/perma/gamepatch/`. Cold autosave-only reload and drone-flight adoption are separate from the general colony save control. |
| Deferred construction/cargo / palette | Spec §10 “Drones L5” and L5's ready test retain the exact owner deferral; orchestrator owns briefing the import/export and dials work when it is commissioned. Brief 05's “Handed over” passage owns palette judgment. No new unscheduled sitting or new feature brief created. |

The verdict does not reimpose build 5's lifted hold. The owner chooses when to fire it; its active
worker can take C1–C4 under the existing ruling that fixes from a closed link belong to the working
link. This QA is scoped to evidence and records, so the fixes remain listed work.

## Drift and retirement reconciliation

The consumed brief at `git show cc3d6f1:docs/agent/prompts/Train_Hub_Project/03_Drones/6_QA_high.md`
is the full upstream inbox. Its eight `###` blocks were enumerated with
`re.findall(r'^### (.+)$', text, re.M)` before deletion. Every block has a destination below;
the L1/L2 material nested under “Audit drift” is explicitly included.

| inbox block | preserved disposition |
|---|---|
| L5 handoff | Native samples and C1–C6 above; L5 report retains all owner observations, deferrals and ready train-construction test. Storage, door guesses and ship gap remain open. |
| L5 drift | Default handoff, empty-request dispatch and absent XTemplate fixes sampled in code/smokes; timer/fleet rulings in spec; misattributed persist error in D14(a). Console mistakes remain in the upstream grave and L5 record, not successful native evidence. |
| Audit drift, including L1/L2 | D14(a–f) and audit report §§8–12 retain withdrawn repair, native controls, stale traffic premise and source-coverage limits. L1/L2 reports retain Assets' no-remote condition, quoting failures, code-registration losses, mock assumptions and historical geometry limits. Backward audit above checks completion path and handoffs. |
| L2E handoff | Stock-command/save claims checked by source and current smoke; no native long-hold promotion. DespawnNow decision, spawn-instant recall repair, solver-spy version and receipt EOL drift remain in its report. |
| L2R handoff | Receipt/input reconciliation checked; crest/under-deck interpretation overtaken by door ruling. Metadata source now registered; import proof remains bounded. Read/quoting mistakes retained in its report. |
| L2M handoff | Interpolation source and receipt checked; coarse-hull refinement, reverse timing, curved recall, abandoned runs and changed mock assumptions remain explicitly historical in its report. Owner's motion rejection is not erased. |
| L2M2 handoff | Numeric/source gate checked; mock-green/game-red correction preserved, control result unverified, lifecycle split newly recorded. Dial meaning, game-time driver, signed bank and instrumentation changes remain in its report. |
| L4 handoff | Payment/shape/control/filter/adoption checked; scope extension explicit, failed settled-lane clearance never promoted, fleet launch and XTemplate claims overtaken by L5. Peer edits excluded; mock/native distinction retained. |

Thus **8/8 inbox blocks traced, zero unhomed blocks dropped**; total reconciles against table
members. The non-heading L1/L2 material is not excluded by that count. The brief measured
**19956 bytes** at the base blob (`audit.json`); it is removed, not compressed into another inbox.
Its deletion and manifest-row removal share this report's commit. The folder gate is an exact
filename comparison against `DESIGN.md` and `README.md`, checked after retirement.

Own execution drift: initial nonexistent placeholder read and PowerShell wildcard-path searches
failed without writes; corrected to explicit paths / `-g`. Broad tool output was truncated and
needed focused reads. A formatted PowerShell log table elided text; literal line output and the
JSON receipt replaced it as evidence. The first doccheck read the unstaged deleted brief from
the tracked-file index and went RED; staging the authorized deletion is required before its
final run. Regeneration found the bug/fact indexes already fresh. The Fix Pack checklist gate
rejected long bullets and external Home paths; shortened them and added a local routing receipt
`reports/OPTIN_DRONES_QA_20260925.md`, keeping technical findings here. The new archive's
`.gitattributes` working copy is whole-CRLF (non-blocking doccheck warning); the native logs are
preserved byte-for-byte with `-text -diff`. No todo tool was available; the commit units were stated
in commentary. Peer HEAD/tooling changes were rechecked before writes. No code, art, game state,
existing archive bytes or settled DESIGN ruling was changed.

Verification: flight and repair smokes PASS at `cc3d6f1`; dwell PASS there; move/look and dev Lua
parse PASS at `ce064ed`; the peer diff contains no tested Lua/test change. Traffic RED as C3.
Current flight smoke also verified receipt inputs. Documentation gates and exact folder members
are checked at commit; no native run, release, upload or new chain was started.

Next queued implementation is build 5, after the owner starts it (brief 02 also remains independently
live in the project map). Kickoff:
`task docs/agent/prompts/Train_Hub_Project/04_TRAIN_HUB_BUILDTRACK_high.md`
